# fix_plan — QA / correctness audit

A **discovery** loop. One task per iteration = audit ONE dimension per
`specs/07-qa-audit.md`: grep + read the relevant code/data, VERIFY each issue against
the source (and RT canon for rules claims), append numbered findings to
`QA_FINDINGS.md`, append any newly-found sub-areas as tasks here, gate green
(`build:check` + `npm test`), commit. **File findings; don't fix** (except a
trivially-safe, gate-green change). Don't re-file `BUGS.md` (fixed) items. Re-verify
CLAUDE.md's old audit list against current code — many are DONE.

## Active list

- [x] QA-DH-RULES — Audited. 3 findings (QA-001..003): DH2 background/homeworld/role/divination pipeline still runs in live `prepareData()` (dormant but latent-leak), `divinations.mjs` = Emperor's Tarot (no RT analog), `elite-advances` dead-store. All DH2 modules confirmed live-imported by `acolyte.mjs` + `config.mjs` but no UI/pack data sets the legacy bio fields. (QA-AUDIT)
- [x] QA-DH-SCHEMA — Audited. 5 findings (QA-004..008): aptitude system still live (acolyte field + `aptitude` Item type + Aptitudes panel on the active V2 sheet) (QA-004); talent schema DH2 `aptitudes`/`tier` rendered on item sheet, `tier` consumed by nothing (QA-005); fixed dangling `"Inf"` (removed Influence char) on `charm.characteristics` (QA-006, trivial fix, gate green); `threatLevel` getters with no consumer (QA-007); not-a-bug rollup — `subtlety` correctly absent, `influence` characteristic removed, parry-as-skill a deliberate documented deviation (QA-008). (QA-AUDIT)
- [x] QA-DH-NAMING — Audited. 1 rollup finding (QA-009): 21 `DarkHeresy*` sheet classes + the `DarkHeresy` config-namespace const (backs `CONFIG.rt`), ~91 identifier occurrences; 6 "Dark Heresy" strings (4 console in `hooks-manager.mjs`, 1 **in-app tour UI** `main-tour.mjs:7`, 1 data comment); stale `DH_CONTAINER_ID` + leftover `console.log('DarkHeresyItemContainer:…')` in `item-container.mjs`. `en.json` clean, no `.dh` global, no lowercase variants. Cosmetic — strings autofixable, bulk class rename deferred per CLAUDE.md §J. (QA-AUDIT)
- [x] QA-DH-SKILLS — Audited. 4 findings (QA-010..013). Two real governing-characteristic errors FIXED (trivial, gate green): `commerce` Int→Fel (QA-010), `survival` Per→Int (QA-011) — RT canon CoreBook-1-200 :3793/:3826; both were tested vs the wrong characteristic. Not-a-bug rollup (QA-012): skill list 48/48 canon-complete (+parry deviation) and ALL specialist specialty lists match RT canon exactly — CLAUDE.md deviation #5 (specialty drift) is RESOLVED. Filed QA-013: vestigial multi-entry `characteristics` arrays diverge from RT's single governing char (no roll impact; primary drives rolls). (QA-AUDIT)
- [x] QA-WEAPONS — Audited. 7 findings (QA-014..020). Real automation gaps: Force-weapon psyker bonuses unwired (QA-014, +PR dmg/pen + Focus bonus all missing); `Unstable` unwired AND absent from the `attackSpecials()` catalog (QA-015); `Recharge` not enforced — fires every Round (QA-016); Power-Field parry-destroy unautomated (QA-017); `Customised` ½-reload ignored (QA-018); target-side qualities (Concussive/Toxic/Shocking/Snare/Flame/Blast/…) are descriptive-only `addEffect` text, no forced test/condition (QA-019 rollup); Defensive help text says +10, code/canon = +15 (QA-020 cosmetic). NOTE: Inaccurate/Defensive/Scatter/Storm/Spray/Tearing/Lance/Maximal/Razor Sharp/Proven/Primitive/jam-overheat thresholds/Best-never-jams/Poor↔Good craftsmanship-reliability ARE correctly wired (`rules/attack-specials.mjs` + `damage-data.mjs` + `action-data.mjs`). (QA-AUDIT)
- [x] QA-ARMOUR-FIELDS — Audited. 6 findings (QA-021..026). Real bug FIXED (trivial, gate green): force-field Poor overload 15→**20** per canon Table 3-10 (QA-021, ItS :318 / Faith&Coin Table 3-5). `maxAgility` confirmed DH2-leftover — RT armour table has no Max-Ag column, zero code consumers (QA-022). Automation gaps: Poor armour −10 Ag unwired (QA-023), Good armour +1 AP first-hit/round unwired (QA-024), Primitive armour half-AP vs non-Primitive weapons unwired — `assign-damage` never reads armour `type` (QA-025), per-field force-field conditions/coverage (Mirror Shield arm/body-only, Power Field melee/3m exception, Conversion photon-flash) all unwired (QA-026). NOTE: Best craftsmanship (+1 AP, half-weight), mixing-armour highest-AP-per-location, and force-field overload Common/Good/Best ARE correctly wired. (QA-AUDIT)
- [x] QA-CYBERNETICS — Audited. 5 findings (QA-027..031). Real unwired bonuses: Synthetic Muscle Grafts +1 Strength Bonus (QA-027, needs a +SB schema/derivation addend — `strength.modifier` adds to raw, not bonus); Blackbone Bracing +2 unarmed damage dropped — only its two talent grants fire (QA-028); Augmented Senses never grants the Heightened Senses (choose-one) talent, no `grants` block (QA-029); whole craftsmanship-conditional cybernetic layer (Good/Poor on Bionic Arm/Locomotion/Respiratory/Cybernetic Senses/MIU/Synthetic-Best) unwired — engine reads cybernetic craftsmanship ONLY for armour Best +1 AP (QA-030). Not-a-bug rollup (QA-031): 13 cybernetics correctly AE/conditionalBonus/armour/grant-wired, ~14 correctly narrative/immunity/weapon-only. (QA-AUDIT)
- [x] QA-TALENTS-TRAITS — Audited. 5 findings (QA-032..036). NARRATIVE-classified but automatable: Hyperactive Nymune Organ +1 Reaction/round + Full-Move/Run movement (reaction-budget + movement machinery exist) (QA-032); Warp Conduit +1 Psy Rating on Push / −10 phenomena (push/phenomena engine exists) (QA-033); Sharpshooter/Takedown negate the engine-applied Called-Shot/Stun −20 but conditionalBonuses can't express action-subtype — rollup, same shortfall as BUG-003 (QA-034); Polyglot treat-languages-as-Basic (−10) via the existing `treatAsBasic` path (QA-035). Not-a-bug rollup (QA-036): re-roll / immunity / PF / positional clusters correctly left narrative (no engine reroll-prompt or Fear/Pinning/Stun/Malignancy state to gate on). (QA-AUDIT)
- [x] QA-PSYCHIC — Audited. 5 findings (QA-037..041). **P0 (QA-037): Focus Power Test missing the +5×Psy Rating bonus** — RT Core p.157 (`:8043` "+5 to Focus Power score per level of Psy Rating", example WP45+5×5=70) is a genuine RT mechanic the engine never adds (only a homebrew `hasFocus`+10); 0.7.12 removed the DH2 PR-difference term but never added RT's +5×PR, so every psychic test rolls 15-25 too low. Push phenomena logic wrong (QA-038): Push+doubles wrongly diverted to Perils (no such RT rule — Push ALWAYS rolls Phenomena; Perils only via a 75+ Phenomena result, which `drawFromTable` doesn't auto-escalate). Phenomena-roll modifiers (Push per-rating/Sustaining +10/Renegade/warding) unapplied — `drawFromTable` takes no modifier (QA-039). Per-DoS/per-rating damage & effect scaling description-only + dead `Psychic Storm` attackType path + Force Shards mis-tagged Barrage (QA-040). Navigator Novice/Adept/Master tier progression + +0/+10/+20 + "Navigators never trigger Phenomena" unmodelled; `tier`/`level` schema fields unused (QA-041). NOTE: BUG-006 range normaliser + Fettered/Unfettered/Push PR resolution + opposed-prompt (BUG-007) confirmed correct. (QA-AUDIT)
- [x] QA-SHIP — Audited. 6 findings (QA-042..047). The voidship combat model is homebrew end-to-end: hull-damage is a fixed 1–4 by an invented Nonpen/Pen/Overpen tier (`damage>=armour`/`+3`) instead of RT's `Damage − Armour → Hull Integrity` (QA-042, `action-data.mjs:515-558` + `assign-damage-data.mjs:81-116`); critical RESULTS come from a 3-level×component×d10 matrix while the canonical RT Table 8-12 RollTable sits unused in the `tables` pack (QA-043, `voidship-critical-damage.mjs:1-346` vs `tables.yml:832`); critical TRIGGER is a fixed `roll≤target/10` (~10 DoS) with no `critRating` field, ignoring each weapon's Crit Rating (QA-044, `action-data.mjs:352`); void shields are permanently decremented, never restored per Strategic Round (QA-045, `calculateVoidShields` writeback + no restore hook); Crippled-ship state (0 Hull → −10 Man/Det, ½ Speed, ½ weapon Strength, auto-crit) unautomated (QA-046); Strategic Round/Turn economy + VU positioning/arcs absent rollup, component-bonus wiring broadly present NOTE (QA-047). (QA-AUDIT)
- [x] QA-SHEET-UI — Audited. 4 findings (QA-048..051). Real display bug FIXED (trivial, gate green): vehicle Rear armour input showed `{{actor.side}}` not `{{actor.rear}}` (QA-048, `vehicle-armour-panel.hbs:13`). Computed-but-not-displayed: XP-spent breakdown (`spentCharacteristics/Skills/Talents/PsychicPowers` + `calculatedTotal`) is computed every prepareData but the Experience panel only shows a hand-entered `used` (QA-049, `acolyte.mjs:517-544`). Dead computed values: `insanityBonus`/`corruptionBonus` derived but zero consumers (QA-050). Dead schema+getters: `faction`/`subfaction` (npc/vehicle/voidship) no input + no display — companion to QA-007 (QA-051). NOTE: all actor-sheet `data-action` controls have registered handlers (no no-op controls found). (QA-AUDIT)
- [x] QA-ACQUISITION — Audited. 5 findings (QA-052..056). **Two P0 data-table bugs** vs RT Core Table 9-35 (`CoreBook-201-401.pdf/markdown.md:3489-3514`): AVAILABILITY_MODS is wrong at every tier and missing Abundant +50 — Ubiquitous +30 vs canon **+70**, Common +10 vs **+20**, Scarce −10 vs **+0**, etc. (QA-052); SCALE_MODS has Negligible/Trivial swapped+mis-valued — Negligible +20 vs **+30** (QA-053). CLAUDE.md §G's "verbatim from p.270" claim is FALSE. P1: Commerce DoS applied as ×10 instead of RT's **×2 PF/DoS** (`:3534`) (QA-054). P2: ship-component/starship acquisition uses generic Scale, ignoring Table 9-36 + the "never give Scale bonus for Components" rule (`:509,:3558`) (QA-055). Not-a-bug rollup (QA-056): PF flows (group-wide world setting, Endeavour/chargen PF rewards), CRAFTSMANSHIP_MODS, ±30 fudge, and PF≥100/≤0 auto-pass/fail are all correct. (QA-AUDIT)
- [x] QA-DATA-QUALITY — Audited. 5 findings (QA-057..061). 27 NPCs with `wounds.max:0` incl. real combatants (Wych WS61, Howling Banshee) — extraction lost the Wounds line, NPCs have no wound track (QA-057, P1); 3 NPCs (Representative/The Luminary/Plaguebearer) ship with an all-zero characteristic block → every test resolves vs 0 (QA-058, P1); 289 NPC-embedded weapons with empty `damage` (natural weapons claws/fangs lost their profile, e.g. `Oversized claws` `:164` all-blank) + 76 OCR blobs where a weapon spec + next NPC's prose became one item *name* (QA-059, P1) — NOTE the 9 empty-damage weapons-pack items (launchers + effect grenades) are correctly empty; orphan pre-0.7.17 skill keys (athletics/stealth/linguistics/operate) linger on the 4 PoC `npcs.yml` xenos but are inert behind the `acolyte.mjs:446` guard (QA-060, P3); 16 OCR footnote daggers (†) in `rawGear`/`name` fields (QA-061, P2 cosmetic). CLAUDE.md 0.7.25's "27 wounds=0 / 261 empty-damage" upstream-bug note is now filed + re-counted (289). (QA-AUDIT)
- [x] QA-DH-BONUSES-PANEL — Audited. 1 finding (QA-062): `bonuses-panel.hbs` confirmed dead — preloaded at `handlebars-manager.mjs:20` (path is under `handlebars/`, not root) but invoked by NO template and no AppV2 `PARTS`; it renders the DH2 `backgroundEffects.abilities` (QA-001 machinery). Its sole `data-action="bonusVocalize"` emitter orphans the live-registered `AcolyteSheetV2._onBonusVocalize` handler (`acolyte-sheet-v2.mjs:17,103`). Catalogue: of 53 registered partials it is the ONLY uninvoked one (other 52 all invoked). Also noted v1 `AcolyteSheet` is imported but never `registerSheet`'d (only V2 registered). Bundle removal with QA-001. (QA-AUDIT)
- [x] QA-SKILL-CHAR-ARRAYS — Audited + FIXED (trivial, gate green). 1 finding (QA-067): normalised all 21 multi-entry skill `characteristics` arrays in `template.json` to their single RT-canon governing characteristic. Confirmed the only reader is the `acolyte.mjs:449` fallback (never fires — every skill has a non-empty primary `characteristic`); `base-actor.mjs:97` reads a talent's `applies.characteristics`, not the skill array; no template iterates `skill.characteristics`. Each array[0] already equalled `characteristic`, so loss-free. Closes QA-013. (QA-AUDIT)
- [x] QA-AMMO-SPECIALS — Audited. 4 findings (QA-063..066). All 6 ammo bonus functions ARE wired into the pipeline (`roll-data.mjs:337`, `damage-data.mjs:303/351/388`, `attack-specials.mjs:44`), so the gap is purely missing switch `case`s: 7 of 18 ammo types have no case in `rules/ammo.mjs`. Damage-class/quality changers unwired (QA-063, P1: Tempest Bolt Shells→Energy+Shock, Acid Shells→2d10 E/0 Pen — clear omissions, the Inferno→Flame/Explosive-Arrows pattern is already wired); flat dmg/pen modifiers unwired (QA-064, P2: Microburst −2/+2, Nephium +2 dmg, Void −1 dmg); action/target-conditional unwired (QA-065: Tracer +5 BS Full-Auto P2, Organgrinder Toughness-cascade P3). Weapon-mods (QA-066, P2): `weapon-modifiers.mjs` wires only 6 mods; Overcharge Pack +1 dmg is a clear flat-modifier omission, rest situational/narrative. (QA-AUDIT)
- [x] QA-COMBAT-ACTIVE-EFFECTS — Completeness-critic re-scan (post QA-094): audited the `active-effects.mjs` + `combat-action-manager.mjs` recurring-damage subsystem, never covered end-to-end by any dimension. 2 findings (QA-095..096). **QA-095 (P1):** the On-Fire/Blood-Loss per-turn handler IS built + canon-faithful (1d10 no-armour Energy + 1 Fatigue + WP-to-act for Burning; 1 Fatigue + Difficult −10 Medicae for Bleeding) and correctly gated by the `processActiveEffectsDuringCombat` setting — but it's an **orphaned consumer**: nothing in the engine ever CREATES the `Burning`/`Bleeding` ActiveEffect it keys on (`name === 'Burning'`/`'Bleeding'` at `combat-action-manager.mjs:38,40`), no `CONFIG.statusEffects` registers them, and no Flame hit / catch-fire crit / blood-loss crit toggles them, so the recurring damage only fires if a GM hand-makes an effect named exactly "Burning"/"Bleeding". This refines QA-080 (which called on-fire/Blood-Loss "GM hand-tracked render-only chat" and missed the existing per-turn consumer). **QA-096 (P3, FIXED — trivial, gate green):** removed two leftover `console.log(combat)/console.log(data)` debug lines firing on every combat turn/round. (QA-AUDIT)
- [x] QA-RANGE-BANDS — Completeness-critic re-scan (post QA-096): audited the personal ranged-attack range-band engine `rules/range.mjs:calculateRangeNameAndBonus` (`:100-122`), consumed live by `action-data.mjs` and fed real measured grid distance (`targeted-action-manager.mjs:tokenDistance`), never covered by any dimension (the only prior "range band" mention, QA-047, was ship-combat). 1 finding (QA-097, P2). The +0/−10/−30 bands are canon-correct (Normal +0 ≤2× range, Long −10 2×–3×, Extreme −30 >3× — RT Core `:2425/2458`), but the **Point Blank band edges are wrong**: upper bound is ≤3m vs canon ≤2m (`:2482` "two metres away or closer") → a 20-point over-grant (+30 vs Short +10) for targets at 2.1–3.0m; and the hard ≤1m→"Melee +0" cutoff drops the +30 a non-engaged adjacent ranged shot should get (canon: Point Blank applies unless engaged in melee, which the engine can't read). Weapon short-range<3m point-blank exception also unmodelled. Short Range ≤maxRange/2 +10 is canon-correct. (QA-AUDIT)
- [x] QA-OVERHEATS — Completeness-critic re-scan (post QA-097): audited the Overheats weapon-quality subsystem (`action-data.mjs:207-219` trigger + `:590-592` effect output) end-to-end vs RT Core p.116 — never covered by QA-WEAPONS (whose NOTE asserted "jam-overheat thresholds ARE correctly wired", a surface claim this disproves). 3 findings (QA-098..100). **QA-098 (P2):** overheat trigger is `rollTotal > 91` (fires 92+) but canon overheats on "91 or higher" — off-by-one, the same `>`→`>=` fix 0.7.7 applied to jam but never reached overheat. **QA-099 (P1):** Overheats weapons still Jam — no guard excludes them from the jam branch, so the Plasma Cannon (Overheat+Unreliable, jamThreshold 91) JAMS at roll 91 instead of overheating (inverse of canon "does not Jam; any would-be jam instead causes overheat") and double-applies overheat+jam at 92+. **QA-100 (P1):** the overheat result deals NO self-damage to the wielder (canon: energy damage = weapon damage, pen 0, to an arm), frames the drop as forced not the wielder's choice, and skips the one-round cooldown — the entire mechanical cost that balances plasma/melta is missing, only a flavour string shows. Best craftsmanship never-overheats guard (`!isBestRanged`) IS correct. (QA-AUDIT)
- [x] QA-FORCE-FIELD — Completeness-critic re-scan (post QA-100): audited the force-field roll subsystem (`rolls/force-field-data.mjs` + `prompts/force-field-prompt.mjs`, triggered from `acolyte.mjs:253-260`) end-to-end — QA-021/026 touched only the overload *values* and per-field coverage, never the roll *flow*. 3 findings (QA-101..103). **QA-101 (P1):** the field roll is a standalone manual sheet action that posts a pass/overload chat card but never integrates with damage resolution — `assign-damage-data.mjs` has zero `forceField` reference, so a successful field roll (canon ItS `:267` "the attack is nullified") does NOT auto-stop the damage, and an overloaded field (canon ItS `:269` recharge via Luminen Charge / Very Hard −30 Tech-Use) has no recovery path, staying overloaded until a GM hand-edits the flag. **QA-102 (P2):** chat speaker re-fetches via `game.actors.get(this.actor._id)` (`:64`) — the same unlinked-token anti-pattern BUG-005 cause B fixed in the damage pipeline, recurring here. **QA-103 (P3):** overload writeback `this.forceField = this.forceField.update(...)` (`:54-60`) is un-awaited and clobbers the item ref with a Promise. NOTE: overload values (Poor 20/Common 10/Good 5/Best 1) confirmed canon-correct per QA-021. (QA-AUDIT)
- [x] QA-RELOAD-JAM — Completeness-critic re-scan (post QA-103): audited the reload/jam weapon-state subsystem (`rules/ammo.mjs` clip drain + `acolyte.mjs:647 _computeWeaponReload` + `prompts/weapon-prompt.mjs:64` empty-clip block + `action-data.mjs:218 jam`) end-to-end — never covered by any dimension (QA-016 touched Recharge, QA-098..100 Overheats, but not reload/jam state). 2 findings (QA-104..105). **QA-104 (P1):** there is NO Reload action — `_computeWeaponReload` computes/displays `effectiveReload` time (with Rapid Reload halving) but its own comment admits "There is no live reload action in the engine"; firing only ever decrements `clip.value` (`ammo.mjs:17`), nothing resets it to `clip.max`, and the empty-clip warning literally says "Do you need to reload?" while offering no affordance — the player must hand-edit the clip. Canon RT Core p.249 RELOAD (`:2126/:2131`) + Table-9-1 action (`:1859`). **QA-105 (P2):** a Jam sets only the current attack to miss + emits chat text (`action-data.mjs:218-221,593`); no `system.jammed` state exists anywhere (grep-confirmed), so a jammed weapon fires again next attack with no Full-Action BS Test to clear and no ammo-loss-on-clear — vs canon RT Core p.249 WEAPON JAMS (`:2519-2521` "cannot be fired until it is cleared… Full Action… Ballistic Skill Test… ammo in it is lost"). NOTE: jam correctly forces the current attack to miss (`success=false`); empty-clip firing IS correctly blocked. (QA-AUDIT)
- [x] QA-TARGET-SIZE — Completeness-critic re-scan (post QA-108): audited the target-size to-hit modifier subsystem (`roll-data.mjs:276-283` `WeaponRollData.initialize()` → `modifiers['target-size'] = (size-4)*10`) end-to-end vs RT Core Table 9-9 — never covered by any dimension (only ship-combat "range bands" QA-047 + personal range bands QA-097 touched range, not target size). 2 findings (QA-109..110). **QA-109 (P2):** the size modifier is applied to MELEE (WS) attacks too — `initialize()` sets `target-size` with no ranged guard, and both melee/ranged route through the same `updateBaseTarget` — but RT 1e size is **ranged-only** ("Size is an important factor when **shooting** ranged weapons… **Ballistic Skill** Tests", Table 9-9 `:2495-2509`; Table 9-7 `:2369-2394` phrases every size row as "**Shooting** a X target"; melee modifiers are outnumbering/Prone/higher-ground only). So melee vs a Hulking Ork wrongly gets +10 (vs Scrawny −10). One-line `isRanged` guard. Not-a-bug rollup (QA-110): the `(size-4)*10` VALUES + the 10-tier dropdown (Minuscule −30 … Titanic +60) reproduce Table 9-9 verbatim, modifier is correctly target-side once-per-attack. (QA-AUDIT)
- [x] QA-COVER-AREA — Completeness-critic re-scan (post QA-110): audited the area/cover/scatter combat layer — Blast/Scatter/Deviation (`damage-data.mjs:371-453` `_calculateSpecials` + `:469` `scatterDirection` + `action-data.mjs:290` thrown-miss deviation) and the (absent) Cover subsystem — never covered end-to-end (QA-019 only listed Blast in a descriptive-effects rollup). 2 findings (QA-111..112). **QA-111 (P1):** Cover is entirely unmodelled — no `cover` schema field, no cover step in `assign-damage-data.mjs:38-51` (damage always resolves against the target's own location armour), grep-confirmed zero `.cover` references — vs RT Core p.245 (`:2352-2405`) where a shot to a concealed location resolves against the cover's Armour Points (excess to target) and erodes the cover AP by 1 per excess hit (worked Titus/sandbags example). **QA-112 (P2):** the Haywire Grenade ships `special: {}` (missing its canon **Blast (2)** + Haywire flags, ItS p.135 `:4744`), so no area/effect ever fires; and the `case 'haywire'` handler (`damage-data.mjs:417-419`) is dead code that is both DH2-phrased ("Haywire Field at strength 1d10") and has a negative-radius bug (`level * -10` m). NOTE: thrown-weapon miss deviation (1d5m, 1d10 direction) at `action-data.mjs:290` is canon-correct (RT Core `:2462`); thrown weapons correctly still roll damage on a miss (grenade scatter-and-detonate, `roll-data.mjs:91 canDamage = success || isThrown`). (QA-AUDIT)
- [x] QA-EVASION-REACTION — Completeness-critic re-scan (post QA-112): audited the Dodge/Parry Reaction subsystem end-to-end — the Reaction *budget* (`roll-helpers.mjs:220-260` + reset hook `hooks-manager.mjs:170-180`) was touched by QA-032/QA-071/QA-045, but whether a Reaction actually *negates a hit* in attack resolution was never audited. 2 findings (QA-113..114). **QA-113 (P1):** Dodge/Parry are absent from attack resolution — `action-data.mjs`→`damage-data.mjs`→`assign-damage-data.mjs` has NO defender-evasion step (grep-confirmed zero dodge/parry/negate refs in the pipeline AND in `templates/chat/`), so a hit always deals damage; the only way to roll a Reaction is the standalone sheet buttons `acolyte-sheet-v2.mjs:124-125 _onDodge/_onParry → rollSkill`, whose pass/fail result is never fed back to suppress the damage. Canon RT Core p.246 (`:2252` "but see Dodge and Parry Reactions") + p.238/242 (`:1945-1953`,`:2117`,`:2258-2260`) make it an integral "before Damage is rolled" branch. Same standalone-vs-integrated shape as the force field (QA-101). **QA-114 (P2):** even the manual Dodge can't apply the per-DoS burst-negation rule (`:1961` "each DoS negates one additional hit", Confessor-Varn example `:1965`) nor gate on awareness (`:1949`)/Stunned (`:2934`) — it's a context-free pass/fail with no link to the incoming hit count. NOTE: the separate dodge/parry pool model (`canSpendReaction` totalMax = dMax+pMax−base) is actually canon-correct for base+Step-Aside/Wall-of-Steel; the reset-on-own-turn timing is correct. Appended fix task below. (QA-AUDIT)
- [x] QA-VEHICLE-COMBAT — Completeness-critic re-scan (post QA-114): audited the vehicle actor + combat path (`documents/vehicle.mjs`, vehicle schema in `template.json`, and how `assign-damage-data.mjs` handles a vehicle target) end-to-end vs Into the Storm Ch.V VEHICLES (`roguetrader_intothestorm-126-257.pdf:2134+`) — never covered (QA-048 touched only a vehicle Rear-armour DISPLAY typo). 3 findings (QA-115..117). **QA-115 (P1):** the vehicle damage model is entirely unwired — `AssignDamageData` has only voidship + personal-creature branches, so a vehicle target routes through the personal path which reads `Object.entries(this.actor.system.armour)` (vehicles have NO `armour` map — only facing `front/side/rear` strings — so `undefined` likely THROWS once a creature hit location is set) and writes `system.wounds` (vehicles use `integrity`, with `wounds` defaulting 0/0 from `base`); facing Armour + Structural Integrity (the token bar1) are never consulted/written. Canon ItS `:2175/:2181` (facing AP reduces damage like personal armour; Integrity = the Wounds-like damage track). **QA-116 (P2):** Manoeuvrability has ZERO code consumers (getter+sheet only) though canon `:2197` applies it to "every Drive or Pilot Test made using the vehicle". **QA-117 (P2):** no Vehicle Critical Hit chart (`integrity.critical` has no producer), no vehicle Combat Actions (Ram/Evasive/Floor it!/Tactical Manoeuvring/vehicle Dodge), and the move-then-shoot −10/−20 penalty + tactical/cruising speed are display-only — canon ItS `:2237/:2256/:2270/:2300`. The vehicle actor is a stat-sheet stub. Appended fix task below. (QA-AUDIT)
- [x] QA-VEHICLE-DAMAGE-FIX — (deferred — discovery-only; tracked for human fix) (from QA-115/116/117) Build the RT vehicle combat layer: add a vehicle branch to `AssignDamageData` (guard it OUT of the `Object.entries(system.armour)` personal loop) that selects facing AP from `front/side/rear` by attack arc (or a facing dropdown), parses the string AP, reduces damage by `facingAP + Pen` with no TB, and writes `system.integrity.value`/`.critical`; apply `manoeuverability` to the operator's Drive/Pilot tests (needs a driver↔vehicle link); add a Vehicle Critical Hit RollTable + the ItS vehicle Combat Actions (Ram = facing AP + 1d10/2d10) + the move-then-shoot −10/−20 penalty. Large subsystem — Foundry-coupled, verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-EVASION-INTEGRATION-FIX — (deferred — discovery-only; tracked for human fix) (from QA-113/114) Thread Dodge/Parry into attack resolution: when an attack hits a target token, surface a "Roll Dodge/Parry" affordance on the attacker's result card (or auto-resolve via the defender's `targetActor`), and on a successful Reaction suppress the damage/assign-damage for the negated hit(s) — computing hits-negated = `1 + dodgeDoS` (capped at incoming count) against Semi/Full-Auto bursts. Reuse the existing `rollSkill('dodge'|'parry')` + Reaction-budget spend. Gate on awareness/Stunned once the QA-080 condition layer exists. Coordinates with QA-101 (force field), QA-071 (All Out Attack lockout / Defensive Stance grant). Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-COVER-FIX — (deferred — discovery-only; tracked for human fix) (from QA-111) Build the RT cover model: add a per-actor/per-token cover state `{ ap, concealed-locations }`; in `AssignDamageData.update/finalize` route a hit to a concealed location against the cover AP first (apply only excess to the target, decrement cover AP by 1 per excess-damage hit per RT Core `:2401`); optionally seed Table 9-7 Cover Examples as preset AP values. New subsystem — Foundry-coupled, verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-HAYWIRE-FIX — (deferred — discovery-only; tracked for human fix) Set `special: { blast: 2, haywire: true }` on the Haywire Grenade (`weapons.yml`), and rewrite the dead `case 'haywire'` handler (`damage-data.mjs:417-419`) to RT/ItS text ("electrical devices within the Blast radius offline [[1d5]] rounds; direct hit on a vehicle → automatic critical as Righteous Fury, +1 result"), dropping the `level * -10` negative radius. Bundle the handler text with QA-WEAPONS-DESCRIPTIVE-FIX. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-TARGET-SIZE-MELEE-FIX — (deferred — discovery-only; tracked for human fix) (from QA-109) Gate the `target-size` modifier assignment in `WeaponRollData.initialize()` (`roll-data.mjs:279`) on `this.weapon.isRanged` so it applies only to Ballistic Skill attacks (ship weapons already branch earlier). Foundry-coupled — verify on rt-smoke (melee vs a Hulking token shows no `target-size` row; a ranged attack still shows +10). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-OPPOSED-TEST — Completeness-critic re-scan (post QA-105): audited the opposed-test resolution subsystem (`action-data.mjs:checkForOpposed` + `roll-helpers.mjs:getOpposedDegrees`, driving Feint/Grapple/Knock-Down) end-to-end vs RT Core p.232 — never covered by any dimension (QA-094 only touched DoS counting). 3 findings (QA-106..108). **QA-106 (P2):** the degrees-of-success **tie-break is wrong** — both the generic path (`action-data.mjs:95` `opposedDos >= dos`) and the Knock-Down `getOpposedDegrees` net-0 ladder award EVERY tie to the defender, skipping RT's "highest Characteristic Bonus wins, then lowest dice roll" (`CoreBook-201-401.pdf/markdown.md:1600`); both-fail→stalemate IS canon-valid (not-a-bug). **QA-107 (P3, FIXED — trivial, gate green):** removed two stray debug `console.log`s firing every opposed action (`action-data.mjs:86`) and every non-zero modifier on every roll (`roll-data.mjs:145`) — same class as QA-096. **QA-108 (P3):** Knock-Down 2+DoS bonus-damage effect text omits the canon "armour counting as double" clause (RT Core p.241 `:2076`); value `1d5-3+SB`+Fatigue otherwise correct. NOTE: initiative (`1d10 + AgB`, `template.json:18` + `base-actor.mjs:129`) confirmed RT-correct — not a finding. (QA-AUDIT)
- [x] QA-OPPOSED-TIEBREAK-FIX — (deferred — discovery-only; tracked for human fix) (from QA-106) On a degrees-of-success tie in opposed resolution, break by highest governing-Characteristic Bonus (defender's `opposedTarget` char vs attacker's action char), then by lowest raw d100 roll — instead of auto-awarding ties to the defender. Apply to both `checkForOpposed` (`action-data.mjs:95`) and the Knock-Down `getOpposedDegrees` net-0 branch (treat 0 as "go to tiebreak", not "attacker fails"). Optionally append "armour counts as double" to the Knock-Down 2+DoS text (QA-108). Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-RELOAD-ACTION-FIX — (deferred — discovery-only; tracked for human fix) (from QA-104/105) Build the reload/jam weapon-state layer: add a Reload action/control that sets `clip.value = clip.max` (spending `effectiveReload` worth of Full/Half actions, decrementing a spare-clip resource if modelled); add a `system.jammed` flag set on a jam, block the attack prompt while jammed (mirror the empty-clip block at `weapon-prompt.mjs:64`), and a "Clear Jam" Full-Action BS-Test control that on success clears the flag + zeroes `clip.value` (ammo lost → forces a reload) and on failure leaves it jammed. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-FORCE-FIELD-INTEGRATION-FIX — (deferred — discovery-only; tracked for human fix) (from QA-101/102/103) Integrate force fields into hit/damage resolution: roll an equipped+activated, non-overloaded field before applying damage (success → nullify the hit; overload → set `overloaded`); add a recharge/repair action (Luminen Charge talent or Very Hard −30 Tech-Use) clearing `overloaded`; fix the chat speaker to use `this.actor` directly (not `game.actors.get(_id)` — unlinked-token safe); and `await` the overload `update()` without reassigning `this.forceField`. Foundry-coupled — verify on rt-smoke (held field stops damage; overloaded field refuses + recharges). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-OVERHEATS-FIX — (deferred — discovery-only; tracked for human fix) Fix the Overheats quality: change the trigger to `rollTotal >= 91` (`action-data.mjs:207`); when `hasAttackSpecial('Overheats')`, skip the jam branch entirely and route any would-be jam into the overheat path (Overheats weapons never Jam); on overheat, prompt take-energy-hit (weapon damage @ pen 0 to an arm via assign-damage) vs drop-weapon (Free Action), set a per-weapon cooling-down flag blocking re-fire until the round after next, and rephrase the effect text to reflect the choice. Foundry-coupled — verify the Plasma Cannon Unreliable case on rt-smoke (roll 91 → overheat not jam; overheat applies self-damage). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-RANGE-BANDS-FIX — (deferred — discovery-only; tracked for human fix) (from QA-097) Correct the Point Blank band in `rules/range.mjs:107`: change `<= 3` to `<= 2` (unambiguous per RT Core `:2482`); fold ≤1m into Point Blank (+30) and suppress only when the attacker is actually engaged in melee with that target (needs an engagement check the engine lacks — document the approximation if not built); optionally honour the short-range<3m → point-blank = short-range−1m exception. Foundry-coupled — verify a 3m shot now reads Short Range +10 (not +30) on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-CONDITION-PRODUCER-FIX — (deferred — discovery-only; tracked for human fix) (from QA-095) Wire the producer half of the On-Fire/Blood-Loss recurring-damage subsystem: register `Burning`/`Bleeding` (+ other RT conditions) in `CONFIG.statusEffects`, then `toggleStatusEffect` them onto the target when a Flame-quality hit's Agility test fails, an Energy/Explosive critical sets the target alight, or a blood-loss critical lands — so the existing `active-effects.mjs` handler (already canon-faithful) actually fires. Interim: relax the exact-name `===` match to also accept Foundry-default lower-case status ids. Bundle with QA-CONDITIONS-LAYER-FIX (QA-080) + QA-WEAPONS-DESCRIPTIVE-FIX (QA-019). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-AMMO-WEAPONMOD-WIRE-FIX — (deferred — discovery-only; tracked for human fix) (from QA-063/064/066) Add the missing automatable switch cases: ammo damage-class changers (Tempest→Energy+Shock; Acid→2d10 E/0 Pen override), flat ammo modifiers (Microburst −2 dmg/+2 pen + no-Maximal/no-Overheat, Nephium +2 dmg, Void −1 dmg, Tracer +5 BS on Full Auto), and Overcharge Pack +1 damage in `weapon-modifiers.mjs`. Leave target-side cascades (Organgrinder) for QA-WEAPONS-DESCRIPTIVE-FIX. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-WEAPONS-DESCRIPTIVE-FIX — (deferred — discovery-only; tracked for human fix) (from QA-019) Triage the descriptive-only target-effect qualities and wire the automatable subset (Concussive/Toxic/Shocking → Toughness test; Snare → Agility; Flame → Agility/on-fire) through the existing target-resist-roll path; leave narrative ones as text. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-ARMOUR-CRAFTSMANSHIP-FIX — (deferred — discovery-only; tracked for human fix) (from QA-023/024/025) Wire the automatable armour-craftsmanship/type rules: Poor armour −10 Agility (roll modifier), Good armour +1 AP first-attack/round (per-round flag via the Reaction-budget machinery), Primitive armour half-AP vs non-Primitive weapons (thread winning-piece `type` into assign-damage). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-CYBERNETICS-CRAFTSMANSHIP-FIX — (deferred — discovery-only; tracked for human fix) (from QA-030) Build a cybernetic-craftsmanship resolution layer: when a cybernetic's `craftsmanship` is Good/Poor, apply its listed AE/conditionalBonus/grant/penalty (Good Bionic Locomotion → Sprint; Poor Bionic Arm → −10 WS/BS; Good Cybernetic Senses → Heightened Senses + +20; Best Synthetic Muscle Grafts → Unnatural Strength ×2 + −10 Ag). Currently only armour Best +1 AP reads cybernetic craftsmanship. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-STRENGTH-BONUS-ADDEND — (deferred — discovery-only; tracked for human fix) (from QA-027) Add an additive Strength-Bonus path (`system.characteristics.<c>.bonusModifier` summed into `bonus = floor(total/10)+unnatural+bonusModifier`, or route via `.unnatural`) so flat "+N to Strength/Char Bonus" effects (Synthetic Muscle Grafts +1 SB, Bionic Arm Good +10 Str-using-arm) are expressible without inflating the raw characteristic by 10×. Schema/derivation change. (QA-AUDIT)
- [x] QA-UNARMED-DAMAGE-ADDEND — (deferred — discovery-only; tracked for human fix) (from QA-028) Add an additive flat-damage modifier to the unarmed/natural-weapon damage path (`damage-data.mjs` unarmed branch + `unarmedDamageProfile`) so cybernetics/talents granting "+N damage to unarmed attacks" (Blackbone Bracing +2) reach the roll. Currently only the dice *formula* varies by talent/trait. Engine/derivation change requiring live rt-smoke verification → human fix. (QA-AUDIT)
- [x] QA-REACTION-POOL — Audited. 2 findings (QA-159..160). Not-a-bug (QA-159): the separate dodge/parry pools do NOT double-count — `canSpendReaction` subtracts the shared base (`totalMax = dMax+pMax−base`, `roll-helpers.mjs:255`), so a base character correctly gets ONE shared Reaction (Dodge XOR Parry); Step Aside/Wall of Steel raise the correct per-type cap. Refutes the QA-032 "two separate base-1 pools" concern. Real gap (QA-160, P1): "gain an additional Reaction" effects are unexpressible — `reactionBudget()` (`roll-helpers.mjs:220`) hardcodes ONLY Step Aside/Wall of Steel by name; no additive channel exists (no schema addend; an AE on `reactions.*.max` is clobbered by the derived-data overwrite at `acolyte.mjs:435-437` after AEs apply), so Defensive Stance (`combat-actions.mjs:140`, a printed Combat Action) and Hyperactive Nymune Organ grant no extra budget, and Defensive Stance's −20-WS-to-attackers is also unwired. (QA-AUDIT)
- [x] QA-REACTION-ADDEND-FIX — (deferred — discovery-only; tracked for human fix) (from QA-160) Add an additive `system.combat.reactions.base.modifier` summed into the budget (read in `reactionBudget()`/`canSpendReaction`), wire Defensive Stance to set a per-round +1 flag (cleared on turn change) plus a −20-WS conditional on attackers targeting the actor, and route Hyperactive Nymune Organ through the same addend; reconcile with the `acolyte.mjs:435-437` overwrite so it isn't clobbered. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-ACTION-CONDITIONAL-FIX — (deferred — discovery-only; tracked for human fix) (from QA-034) Extend the conditional/attack-specials machinery with an action-subtype + weapon-class condition (shared with BUG-003 Weapon Master), then wire the penalty-negating talents (Sharpshooter → cancel Called Shot −20; Takedown → cancel Stun −20) and weapon-class bonuses through the attack-modifier sum. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-FOCUS-POWER-PR-FIX — (deferred — discovery-only; tracked for human fix) (from QA-037, P0) Add `modifiers['psy rating'] = 5 * this.pr` to `PsychicRollData.update()` (`roll-data.mjs:415`) so the Focus Power Test gains the RT +5-per-Psy-Rating bonus; decide the fate of the non-canon flat `hasFocus`+10. Must verify live on rt-smoke (PR-5 Unfettered WP-45 → modifiedTarget 70). Already promoted to BUG-011 (P0). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-PHENOMENA-LOGIC-FIX — (deferred — discovery-only; tracked for human fix) (from QA-038/039) Rework `checkForPerils()`: Push ALWAYS rolls Psychic Phenomena (drop the doubles→Perils branch); chain to the Perils table on a 75+ Phenomena result; give `drawFromTable` a modifier param and pass the Push per-rating / Sustaining-+10 / warding-±10 bonuses. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-PSYCHIC-PER-DOS — (deferred — discovery-only; tracked for human fix) (from QA-040) Add a per-DoS scaling hook to `PsychicDamageData` (per-DoS damage dice / tiered-success effects), re-tag genuinely per-DoS powers (Force Shards) as `Psychic Storm` or fix their classification, and decide whether to wire Terrify (+Fear/3PR) / Mind Scan (tiered successes). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-NAVIGATOR-TIERS — (deferred — discovery-only; tracked for human fix) (from QA-041) Model Navigator Novice/Adept/Master tiers: usable `tier` field with the +0/+10/+20 test addend + tier-keyed effects, and short-circuit `checkForPerils` for Navigator-discipline powers (Navigators never trigger Phenomena/Perils). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-SHIP-DAMAGE-MODEL-FIX — (deferred — discovery-only; tracked for human fix) (from QA-042/043/044) Rebuild the voidship damage pipeline to RT RAW: `hullLoss = max(0, rolledDamage − facingArmour)` (lances ignore armour; void shields absorb whole hits first); add a `critRating` field to the `shipWeapon` template + ship-weapons data and trigger criticals when DoS ≥ Crit Rating; point `executeCritical` at the canonical `Critical Hits to Starships` RollTable (`tables.yml:832`) indexed by damage-past-Armour / 1d5, chaining to `Catastrophic Damage` on 11+, and retire the homebrew Nonpen/Pen/Crit matrix. Large engine rework — fix loop, not discovery. (QA-AUDIT)
- [x] QA-SHIP-SHIELDS-RESTORE-FIX — (deferred — discovery-only; tracked for human fix) (from QA-045) Restore `system.shields` to the ship's full Void-Shield rating at the start of each Strategic Round (and re-apply per attacker within a Round). Needs a Strategic-Round tracker or a per-attacker restore in `calculateVoidShields`. (QA-AUDIT)
- [x] QA-SHIP-CRIPPLED-FIX — (deferred — discovery-only; tracked for human fix) (from QA-046) Derive `isCrippled = hullIntegrity.value <= 0` in `voidship.prepareData`; apply −10 Manoeuvrability/Detection, ½ Speed, ½ weapon Strength (round up); when a Crippled target takes damage > Armour, route straight to the Critical Hits chart. (QA-AUDIT)
- [x] QA-XP-DISPLAY-FIX — (deferred — discovery-only; tracked for human fix) Surface the engine-computed XP spend in the Experience panel: render `experience.calculatedTotal` (+ optionally the per-category `spentCharacteristics/Skills/Talents/PsychicPowers` breakdown), and decide whether to drive `used`/`available` from `calculatedTotal` or show both with a mismatch indicator. Currently the sheet relies on a hand-entered `system.experience.used`. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-ACQUISITION-TABLES-FIX — (deferred — discovery-only; tracked for human fix; already promoted to BUG-012 P0) (from QA-052/053/054) Correct `rules/acquisition.mjs` to RT Core Table 9-35: replace AVAILABILITY_MODS with Ubiquitous +70 / Abundant +50 / Plentiful +30 / Common +20 / Average +10 / Scarce +0 / Rare −10 / Very Rare −20 / Extremely Rare −30 / Near Unique −50 / Unique −70 (add the `abundant` key); fix SCALE_MODS to Negligible +30 / Trivial +20; change the Commerce scalar from `commerce * 10` to `commerce * 2`. Add a node test reproducing the Herodor worked example (Rare −10 + Good −10 + Negligible +30 = +10, PF 40 → target 50). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-CRITIC — Completeness pass done. 1 finding (QA-068): re-scanned `rules/` + `acolyte.mjs` and found SIX substantial subsystems with real code/data that no seed dimension audited end-to-end — combat actions vs Table 9-1, personal Critical Hits (Table 8-12 `critical-damage.mjs`, wired but values unaudited), movement/encumbrance math, conditions/status-effects (NONE modeled yet `critical-damage` text imposes Stunned/Prone/Useless/on-fire), Fear/Pinning/Insanity/Corruption/Malignancy/Mutation (chargen-data only, no engine), and Endeavours/Renown. Appended all six as new discovery dimensions below. (QA-AUDIT)
- [x] QA-COMBAT-ACTIONS — Audited vs RT Core combat-actions table (`CoreBook-201-401.pdf/markdown.md:1841-1871`). 4 findings (QA-069..072). All to-hit modifier VALUES are correct (AOA +20, Charge +10, Called Shot −20, Full/Semi +20/+10, Guarded −10, Overwatch/Stun/Suppressing −20, Standard 0) and Aim Half/Full Concentration is right. Real gaps: Manoeuvre missing `Attack` subtype → hidden from melee dropdown (QA-069, P2, autofixable); Tactical Advance wrongly removed as "DH2-only" — it's RT canon — plus Move/Run/Stand/Focus-Power/Use-a-Skill absent (QA-070, P3); combat-action SIDE-EFFECTS unautomated rollup — only the actor's own to-hit applies, Defensive Stance/All Out Attack reaction riders + Guarded +10 Dodge/Parry + Charge/Run easier-to-hit all unmodeled (QA-071, P1, ties to QA-CONDITIONS + reaction budget); Knock Down help text mis-states +10 trigger & omits armour-double (QA-072, P3 cosmetic). No DH2-leftover actions beyond the documented legacy Swift/Lightning talents. (QA-AUDIT)
- [x] QA-CRIT-DAMAGE-TABLE — Audited `rules/critical-damage.mjs` (personal critical effects) vs RT Core Tables 9-11..9-26 (`CoreBook-201-401.pdf/markdown.md:2620-2729+`). 3 findings (QA-073..075). **DH2-leftover (QA-073, P2):** the whole Energy/Explosive/Impact/Rending × Arm/Body/Head/Leg × 1-10 effect-text map is the **Dark Heresy 2e** version — rows diverge from RT (e.g. Energy/Arm/1 "1d5 rounds" vs RT "1 Round"; Energy/Arm/3 is a different effect), uses DH2's named "Lost Hand/Arm/Foot condition (see page 242/243)" framework + DH2 page refs (Scatter "page 230" vs RT 248) — RT uses plain "counts as having one arm". **Lookup bug (QA-074, P2):** `getCriticalDamage` passes the raw `hit.location` (`Right Arm`/`Left Arm`/`Right Leg`/`Left Leg`) to `getFuzzy`, which exact-matches table keys `Arm`/`Leg` → undefined → null effect for 4 of 6 locations (only Head/Body resolve); the armour path at `:44` DOES strip whitespace, the crit path doesn't. **Not-a-bug (QA-075):** the index math (`wounds.critical + criticalDamageTaken` running total, 10+ cap, armour−pen+TB reduction, True-Grit-only TB on crit, damageType selection) is RT-correct. Appended follow-up fix task below. (QA-AUDIT)
- [x] QA-CRIT-DAMAGE-TABLE-FIX — (deferred — discovery-only; tracked for human fix) Re-author `rules/critical-damage.mjs` from RT Core Tables 9-11..9-26 (Energy 9-11..14, Explosive 9-15..18, Impact 9-19..22, Rending 9-23..26 — each location 1..10+), retiring the DH2 text + "Lost X condition (page 242)" refs; AND fix the limb lookup so `Right/Left Arm/Leg` resolves to the `Arm`/`Leg` table key (strip the side prefix in `getCriticalDamage`/`getFuzzy`, mirroring the `:44` armour normalisation). Verify a limb critical now renders an effect on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-MOVEMENT-ENCUMBRANCE — Audited `_computeMovement` (`base-actor.mjs:134-150`) + `_computeEncumbrance` (`acolyte.mjs:662-771`) vs RT Core Table 9-30 (movement), Table 9-16 (Size), Table 9-33 (carry/lift/push). 4 findings (QA-076..079). Carrying-Weight table is RT-correct (verbatim, SB+TB 0→20) and the `size−4` modifier maps the Size "Base Movement" column correctly. Real gaps: NO min-1 floor → negative movement for small low-AgB creatures + 0 (not ½/1/2/3) for AgB-0 actors, violating the explicit "Base movement can never be reduced below 1" + Table 9-30 AB-0 row (QA-076, P2); **Unnatural Speed unwired** — quadruped multiplier IS wired but the sibling ×2-AgB trait is dropped despite being on NPCs across 8 packs (QA-077, P1); Encumbered flag set but its −10 movement-test / −1 AgB penalty has no consumer (QA-078, P1); Lifting/Pushing weights not derived (QA-079, P3 minor). Appended fix tasks below. (QA-AUDIT)
- [x] QA-MOVEMENT-FIX — (deferred — discovery-only; tracked for human fix) (from QA-076/077/078/079) Wire the movement/encumbrance gaps: clamp `_computeMovement` base to `Math.max(1, …)` (+ optionally the Table 9-30 AB-0 ½-row); add a `hasUnnaturalSpeed(traits)` helper and double the post-size base when present (RT: doubles AgB after size); apply the Encumbered penalty (−1 AgB to movement/Initiative + −10 to movement-related tests) when `encumbrance.encumbered`; optionally derive Lifting/Pushing weights. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-CONDITIONS — Audited the (absent) condition/status-effect layer. 1 rollup finding (QA-080, P1). Confirmed NO `CONFIG.statusEffects`/`toggleStatusEffect`/per-actor condition track exists (`fatigue` is the only stored condition-state; `isOnFire` at `template.json:1518` is a ship-component GM checkbox, not a character condition). The engine PRODUCES condition outcomes purely as render-only chat text via `addEffect`→`effectOutput` (consumed only by chat .hbs): `critical-damage.mjs` imposes Stunned ×46 / Prone ×27 / Useless ×14 / Blood Loss ×33 / Blinded ×7 / Deafened ×5 / catch-fire ×3 / Lost-limb conditions; Knock Down/Stun Attack/Suppressing(Pinned)/All-Out-Attack/Concussive/Flame/Crippling all text-only. Nothing writes to the target, so "Stunned→+20 to-hit/no-Reactions", "Prone→Stand", "Pinned→−20 BS", on-fire/Blood-Loss recurring damage are all GM hand-tracked — and existing automations that key off this state (All Out Attack lockout QA-071, Stand action QA-070) have nothing to gate on. Canon: RT Core `:2070,2470,2912,2932-2934`. Ties QA-019/036/070/071/038/040. (QA-AUDIT)
- [x] QA-CONDITIONS-LAYER-FIX — (deferred — discovery-only; tracked for human fix) (from QA-080) Build an RT condition/status-effect subsystem: register RT conditions in `CONFIG.statusEffects` (Stunned/Prone/Pinned/Blinded/Deafened/On Fire/Blood Loss/Unconscious/Helpless/Unaware) at init; have combat resolution `toggleStatusEffect` the rolled outcome (with duration flag) onto the target instead of only emitting chat text; then gate downstream rules on it (Stunned → +20 to-hit + no Reactions; Prone → Stand action + to-hit mods; Pinned → −20 BS; On Fire/Blood Loss → recurring-damage hook). Coordinates with QA-019 (weapon target-effects), QA-070 (Stand action), QA-071 (All Out Attack lockout). Large engine rework — fix loop, not discovery. (QA-AUDIT)
- [x] QA-PSYCHOLOGY — Audited the RT psychology subsystem. 3 findings (QA-081..083), all P1 automation-gaps — the whole loop is unimplemented. Fear/Shock (QA-081): no Fear-test path (`drawFromTable` only ever draws Perils/Phenomena, never `'Fear'`), Fear (N) creature trait inert, the Shock RollTable (`tables.yml:666`, mis-named `Fear`) ships but is never drawn — its desc even carries a DH2 "after the first" DoF-ism. Pinning (QA-082): Pinning Tests are description-only text in `action-data.mjs:200-202` / `combat-actions.mjs:242,285,295`; no Pinned state, no −20 BS / Half-Action enforcement. Insanity/Corruption (QA-083): flat acolyte-only ints, NO Trauma/Malignancy/Mutation threshold tests, `malignancy`/`mentalDisorder`/`mutation` item types are empty dead schema with no backing pack, and Mental-Traumas/Malignancies/Mutation outcome tables are absent from the `tables` pack (only Fear/Shock exists); the lone derived value (insanityBonus/corruptionBonus) is QA-050's dead getter. Canon RT Core p.248/293-301 (`:2472,4466,4506,4696,4702`). Appended fix tasks below. (QA-AUDIT)
- [ ] QA-PSYCHOLOGY-FEAR-FIX — (from QA-081/082) Build the Fear+Pinning resolution path: a `rollFear(rating)` actor method (WP Test vs Fear Rating, Insanity-double-digit auto-pass `:4522`, combat-fail → `drawFromTable('Fear')` at +10×DoF using the QA-039 modifier param, non-combat fail-by-30 → 1d5 Insanity); a Pinning Test on Suppressing/Overwatch (WP at action difficulty → Pinned). Both depend on the QA-080 condition layer (Pinned/Shock states) to enforce −20 BS / Half-Action / Shock outcomes. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-PSYCHOLOGY-TRACKS-FIX — (from QA-083) Implement the Insanity/Corruption degradation loop: author Mental Traumas (Table 10-6), Malignancies (10-8), and Mutation outcome tables into the `tables` pack (+ optional compendium packs backing the `malignancy`/`mentalDisorder`/`mutation` item types); add threshold hooks (each 10 Insanity → Trauma Test → Table 10-6; each 10 Corruption → Malignancy Test → Table 10-8; each 30 → two-Characteristic Mutation test); consume `insanityBonus`/`corruptionBonus` (closes QA-050) for Fear-immunity + test difficulty. Large content+engine subsystem. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-ENDEAVOURS — Audited `rules/endeavours.mjs` + `rogue-trader-settings.mjs:60-84` vs RT Core Ch.XII, plus the light-touch on `aim.mjs` + `hit-locations.mjs`. 5 findings (QA-084..088). The Endeavour tracker works as a manual GM ledger but diverges from canon: flat single-bar AP model with no per-Objective sub-structure, no Lesser/Greater/Grand size→PF mapping, and **no excess-AP→PF auto-conversion** (+1 PF/100 over target, `:3791`) — all P3 (QA-084); the **Misfortune** complication path (Table 9-41, per-session d100 PF-loss tied to active Endeavours, `:4054,4070`) is entirely absent — no table, no engine path (QA-085, P3). Renown confirmed **not-a-bug** — RT Core 1e has no Renown tracking mechanic; "Renown" is only an Origin-Path/talent name (QA-086). **Real correctness bug (QA-087, P1):** `getHitLocationForRoll` reverses digits WITHOUT zero-padding, so single-digit to-hit rolls (1–9) mis-locate — roll 7 → `Head` instead of canon `07→70→Body` (`:2268` "a roll of 20 becomes 02"); ~9% of hits (high-DoS) hit the wrong location → wrong armour/crit table; one-line `padStart(2,'0')` fix but verify live. Dead code (QA-088, P3): `aim.mjs calculateAimBonus` is an uncalled no-op stub (aim works via `modifiers.aim`). Appended the hit-location fix task below. (QA-AUDIT)
- [ ] QA-HIT-LOCATION-PAD-FIX — (from QA-087) Zero-pad the to-hit roll to two digits before reversing in `rules/hit-locations.mjs:getHitLocationForRoll` (`roll.toString().padStart(2,'0')`, or `String(roll % 100).padStart(2,'0')` to also fold 100→`00`) so single-digit rolls (1–9) map to the correct reversed location. Verify a single-digit to-hit on rt-smoke (roll 07 → Body, not Head). While there, optionally delete the dead `aim.mjs calculateAimBonus` stub (QA-088). Fix loop, not discovery. (QA-AUDIT)
- [x] QA-DIFFICULTIES — Audited `rules/difficulties.mjs` (the GM difficulty-modifier dropdown on every roll prompt) vs RT Core. 1 finding (QA-089, P2). The ladder is a DH2/RT hybrid: `'40':'Simple (+40)'` is a **DH2 tier** (RT 1e tops at Easy +30, no +40/+50/+60); `'-30'` is mislabelled "Very Difficult" vs canon **"Very Hard"** (`CoreBook-1-200.pdf/markdown.md:5277,5761,6574`); and the ladder omits canon Punishing (−50) + Hellish (−60) (`:5763-5765`). The map KEY is the actual modifier applied, so "Simple (+40)" injects a non-RT +40 into play. Values/labels for +30/+20/+10/+0/−10/−20/−40 (Easy/Routine/Ordinary/Challenging/Difficult/Hard/Arduous) are all RT-correct. Flagged not auto-fixed since it removes a tier + adds two (GM-option change). (QA-AUDIT)
- [ ] QA-DIFFICULTIES-LADDER-FIX — (from QA-089) Correct `rules/difficulties.mjs` to the RT 1e ladder: drop `'40':'Simple (+40)'`, rename `'-30'`→`'Very Hard (-30)'`, add `'-50':'Punishing (-50)'` and `'-60':'Hellish (-60)'`. One-function data edit; verify the dropdown on a roll prompt on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-FATE-POINTS — Audited the Fate-point subsystem. 2 findings (QA-090..091), both P1/P2 automation-gaps. Of the six standard RT Fate uses (RT Core p.279 `:1662-1672`) only **re-roll** is wired (QA-090) — and only via the `roll-control__fate-reroll` button on attack + psychic chat cards (`action-roll-chat.hbs:282`/`psychic-action-chat.hbs:72` → `basic-action-manager.mjs:74 _fateReroll` → `acolyte.mjs:786 spendFate()` decrements `fate.value`), NOT on the skill/characteristic card (`simple-roll-chat.hbs` has no fate control), and it's offered even on a *successful* roll (canon restricts to a failed test); the other five (+10 pre-roll, +1 DoS, Initiative=10, remove 1d5 wounds, recover-from-Stunned) have no engine path. **Burning Fate** (QA-091) is unmodeled — `spendFate()` only touches `fate.value`, never `fate.max`; no `burnFate()`, no burn prompt, no auto-survive-lethal-damage hook in `assign-damage`; a permanent burn is possible only by hand-editing the Max field on `fate-panel.hbs`. Both depend on the absent condition (QA-080) / wound-recovery (QA-WOUNDS-HEALING) layers for the Stunned/wound spends. Talent-specific Eye-of-Vengeance + Blademaster fate/reroll confirmed separate + working. Appended fix tasks below. (QA-AUDIT)
- [ ] QA-FATE-USES-FIX — (from QA-090/091) Wire the standard Fate-point uses: add a generic Fate-spend affordance to the skill/characteristic chat card (re-roll gated on `!rollData.success`, +1 DoS post-roll) and pre-roll prompt options (+10 to test, count Initiative as 10), each routed through `spendFate()`; add a `burnFate()` method decrementing BOTH `fate.max` and `fate.value` surfaced as a "Burn Fate" control; and an auto-survive-lethal-damage burn prompt in `assign-damage`. The remove-1d5-wounds and recover-from-Stunned spends land with QA-WOUNDS-HEALING / QA-CONDITIONS-LAYER-FIX. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-WOUNDS-HEALING — Audited wound/damage recovery + wound-state thresholds. 2 findings (QA-092..093), both P3. Confirmed the wound track is write-once-down: the ONLY writeback is `assign-damage-data.mjs:226-236` (monotonic `wounds.value -= damageTaken`, `wounds.critical += criticalDamageTaken`); `wounds.max` is the only derived value (`acolyte.mjs:418`); grep finds ZERO heal/recover/bed-rest/wounds-per-day logic and ZERO `lightlyDamaged|heavilyDamaged|criticallyDamaged|woundState` derivation. So all of RT Core p.262's recovery model (natural 1/day Lightly, 1/week Heavily, Critical-via-medical, bed-rest TB lumps, Medicae attention) is unmodeled (QA-092), and the three RT Damage states (Lightly ≤2×TB / Heavily >2×TB / Critically Damaged = `wounds.critical>0`, canon `:2977-2997`) aren't derived (QA-093). NOTE the fix_plan premise here ("≥ half wounds lost gating Critical Damage", RT p.247-248) was **DH2-confused** — RT uses 2×TB thresholds and the states govern healing RATE only, not crit-gating (corrected in QA-093). Engine DOES correctly apply Critical *effects* at 0 wounds (`:137-150`). Appended fix task below. (QA-AUDIT)
- [ ] QA-WOUNDS-HEALING-FIX — (from QA-092/093) Build the RT recovery model: derive `wounds.state` in `acolyte.prepareData` (`damage = wounds.max - wounds.value`; `criticallyDamaged = wounds.critical > 0`; else `heavilyDamaged = damage > 2×TB`; else `lightlyDamaged`), surface on the sheet beside the fatigue flags; add `restDay()`/`restWeek()` + Critical-Damage-per-week removal + a Medicae-Test recovery action that remove the state-appropriate Damage from `wounds.value`/`wounds.critical`. Downtime-flow, Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)

- [x] QA-ATTACK-RESOLUTION-DOS — Completeness-critic re-scan (after QA-068) surfaced the core attack-resolution file `action-data.mjs` (724 LOC) as never audited end-to-end; audited the DoS/additional-hits math. 1 finding (QA-094, P1). **The personal-test/combat DoS over-counts canon**: `dos = 1 + getDegree(target, roll)` (`action-data.mjs:228`) where `getDegree` = tens-digit difference (`roll-helpers.mjs:21`), but RT Core p.22's worked example (`CoreBook-201-401.pdf/markdown.md:1586`, roll 18 vs 42 = **2** DoS) proves canon DoS = `floor((target−roll)/10)` (full 10-point bands below target) — engine yields **4**. Over-counts by 1 always (the DH2 `1 +` BUG-001 removed from DoF but deferred on DoS) plus another 1 when the roll's units digit > target's. Propagates to: displayed DoS on every skill+combat card (`simple-roll-chat.hbs:122`/`action-roll-chat.hbs:240`), opposed-test resolution (`checkForOpposed:95`/`getOpposedDegrees:115` — flat term cancels, units term doesn't), and Semi/Full-auto additional hits + Twin-Linked (`:240,248,263-274` — `dos-1` strips the `1+` so auto-fire is only one-hit-high in the units-cross case). NOT a BUG-001 dup (that fixed DoF only). Appended fix task below. (QA-AUDIT)
- [ ] QA-DOS-COUNT-FIX — (from QA-094) Recompute DoS the canon way in `action-data.mjs:228` — `dos = success ? Math.floor((modifiedTarget - roll.total)/10) : 0` (drop both the `1 +` and the tens-digit `getDegree`). ⚠ Re-derive every combat hit-count written against the old convention: Semi `floor((dos-1)/2)`, Full `dos-1`, Twin-Linked `dos/3`/`dos/2`, Accurate `floor(dos/2)` (`damage-data.mjs:184`), Lance `(dos-1)` (`damage-data.mjs:255`), Razor Sharp / Eye of Vengeance thresholds — canon "additional hit per [two] degree(s)" maps to plain `dos`/`floor(dos/2)` once DoS = band count. Add node tests (Yolanda 18 vs 42 → 2; canon auto-fire 2 DoS → 3 hits) + live rt-smoke check. Full combat-math pass — fix loop, not discovery. (QA-AUDIT)

- [x] QA-SURPRISE-TWOWEAPON — Completeness-critic re-scan (post QA-117): audited the to-hit modifier paths the attack engine is missing — Surprise/Unaware and Two-Weapon Fighting — plus the Aim subsystem (`rules/aim.mjs`, thin: only the already-filed QA-088 dead `calculateAimBonus` stub; live `aimModifiers()` Half +10/Full +20 is canon-correct). 3 findings (QA-118..120). **QA-118 (P1):** Surprise/Unaware +30 WS/BS is entirely unwired — no surprise/unaware state or modifier exists in `rolls/actions/documents` (grep-zero), yet the system's own dormant Origin-Path data (`homeworlds.mjs:140,184`, `divinations.mjs:50`) references "the normal +30 bonus" — RT Core p.235 `:1771` + p.246 Unaware `:2517`/`:2368` (Easy +30). The largest to-hit swing in the game must be hand-applied; the Surprise-Round lost-turn is also unmodelled. **QA-119 (P2):** the Multiple Attacks action ships `modifier:0` with a code comment "Mechanical handling of the second attack is on the GM for now" (`combat-actions.mjs:319-325`); no off-hand penalty, talent gate, or second roll exists — RT Core p.246 `:2340` mandates each of two attacks −20 (−10 with Ambidextrous), gated on Two-Weapon Wielder. Same shortfall class as BUG-003/QA-034. **QA-120 (P2 data-quality):** `Two-Weapon Wielder (Melee)` (`talents.yml:3235`) carries the **Unarmed Master** benefit + Unarmed-Warrior prereq by copy-paste ("unarmed combat attacks do 1d10+SB, no longer Primitive") instead of dual-melee-fighting text — the sibling (Ballistic) entry has correct dual-wield text. Appended fix tasks below. (QA-AUDIT)
- [ ] QA-SURPRISE-UNAWARE-FIX — (from QA-118) Wire the Surprise/Unaware +30: register an `Unaware`/`Surprised` condition in the QA-080 condition layer, and when an attack target carries it add `modifiers['surprise'] = 30` to the WS/BS test in `WeaponRollData` (or route via the difficulty step); interim = a manual "Target Surprised/Unaware" checkbox on the weapon prompt. The lost-Turn-on-first-Round economy needs the combat-tracker Surprise-Round structure (cf. QA-047/QA-071). Re-activates the dormant homeworld/divination "deny/re-roll the +30" benefits (QA-001). Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-STUN-GRAPPLE — Completeness-critic re-scan (post QA-120): audited the two melee special-actions that carry real engine code/data but were never covered end-to-end — Stun (`action-data.mjs:157-178`) and Grapple (`combat-actions.mjs:187-191`) — vs RT Core p.246/250. 3 findings (QA-121..123). **QA-121 (P1, dh-leftover):** the Stun action is the DH2 static-defence version — it (a) **ignores the WS-to-hit gate** (the stun block runs unconditionally and overwrites the `_calculateHit()` `success`, so a stun can land on a *missed* attack), (b) **omits the defender's 1d10 roll** (uses a static `defense = headArmour.total + headArmour.toughnessBonus`, ~5.5 easier than RAW's `1d10+TB+AP`), and (c) never doubles head AP for unarmed/Primitive — vs RT Core p.250 (`:2179`) Hard(−20) WS Test *if it succeeds* → `1d10+SB` vs target's `1d10+TB+AP(head, ×2 if unarmed/Primitive)`. **QA-122 (P2):** Grapple is description-only — no opposed-Strength subsystem, no grappled state, no Controller/Target options (Damage/Throw Down/Push/Break Free/Slip Free), vs the full RT Grappling flow (`:2007-2041`). **QA-123 (P3, cosmetic):** `ignoreModifiers` (`roll-data.mjs:21,222`) is set but never read (its siblings ignoreDegrees/Success/Controls/Damage ARE consumed by the chat template) — dead, currently harmless only because Stun discards the WS result + Spray force-sets success. NOTE: the Stun stun-roll `1d10+SB`, the −20 action modifier definition, and `ignoreDegrees` (correctly hides the DoS row) are present/correct. Appended fix task below. (QA-AUDIT)
- [ ] QA-STUN-GRAPPLE-FIX — (from QA-121/122) Fix the Stun action to RT RAW: gate stun resolution on the `_calculateHit()` WS success (with the −20 actually applied), roll the defender's `1d10` (`1d10+SB` vs `1d10+TB+headAP`, doubling `headAP` for unarmed/Primitive), and emit the Stunned condition (once QA-080 exists). Build the Grapple subsystem (grappled state + opposed-Strength Controller/Target options). While here, resolve the dead `ignoreModifiers` flag (QA-123 — delete or wire into `modifiedTarget`). Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-WEAPON-TRAINING — Completeness-critic re-scan (post QA-123): audited Weapon-Training / proficiency enforcement — the −20 untrained penalty path — never covered by any dimension (QA-WEAPONS audited weapon *qualities*, not training gating; the only weapon-class talent the engine reads is Weapon Master/BUG-003). 1 finding (QA-124, P1). The attack pipeline (`roll-data.mjs:256-300` + `action-data.mjs`, grep-zero for training/proficien) never checks whether the actor holds the weapon's `Weapon Training (group)` talent, so every attack resolves at full WS/BS — the canon RT Core p.142 −20 penalty for using an untrained weapon (`CoreBook-1-200.pdf:4849`, restated for Heavy `:5157`/Pistol `:5415`/Exotic; corroborated by the pack's own Exotic Weapon Training "use it without penalty" benefit `talents.yml:857`) is purely GM-applied. The full set of Basic/Pistol/Heavy/Melee/Throwing/Flame/Exotic training talents ships in the pack and weapons carry the `class`+`type` fields, but nothing maps them to a proficiency check. NOTE: checked the Exotic Weapon Training entry for the QA-120-style copy-paste bug — its `data.benefit` is correct (the adjacent "Favoured by the Warp" psychic text is a separate doc); only `pickable.options: []` is empty (minor). Appended fix task below. (QA-AUDIT)
- [ ] QA-WEAPON-TRAINING-FIX — (from QA-124) Wire the untrained −20: in `WeaponRollData.update()` derive the required Weapon Training talent from the weapon's `class`+`type` (class→Basic/Pistol/Heavy/Melee/Throwing prefix; type→group, with Universal-group expansion Bolt/Las/Launcher/Melta/Plasma/SP; type `Exotic`→`Exotic Weapon Training (<name>)`), test via `hasTalent`/fuzzy, and add `modifiers['untrained'] = -20` when absent. Needs a class+type→talent map + Universal expansion. Shares the weapon-class condition family with BUG-003/QA-ACTION-CONDITIONAL-FIX. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-TWO-WEAPON-FIX — (from QA-119/120) Wire Two-Weapon Fighting: on the Multiple Attacks action, check for a wielded secondary weapon + Two-Weapon Wielder, apply `modifiers['two-weapon'] = -20` (−10 with Ambidextrous) to the WS/BS test, and chain a second off-hand attack prompt — shares the action-subtype/weapon-pairing condition machinery with BUG-003/QA-ACTION-CONDITIONAL-FIX. Also (QA-120, data-only) re-author `Two-Weapon Wielder (Melee)`'s benefit+prerequisites in `talents.yml:3235` to the canon dual-melee text (mirror the Ballistic entry), replacing the mis-pasted Unarmed Master content. Foundry-coupled for the engine half — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-SPECIAL-EFFECT-TEXT — Completeness-critic re-scan (post QA-124): audited the CORRECTNESS of the per-quality chat-card effect text generated by `_calculateSpecials` (`damage-data.mjs:391-441`) — an angle QA-019 never checked (it was a rollup that only noted these are "descriptive-only, no forced test," not whether the numbers/wording are RT-correct). 3 findings (QA-125..127), all P2. **Key insight:** the attack-specials COMPENDIUM descriptions are canon-correct, but the engine emits its OWN hard-coded text that diverges (DH2-derived). **QA-125 Shocking (dh-leftover):** `:425` hard-codes "Challenging (+0)" (omits canon "+10 per Armour point on location hit"), adds a spurious "1 level of Fatigue" RT doesn't impose, and stuns for "half degrees of failure" vs canon "half the Damage suffered" (RT Core :5983). **QA-126 Toxic (data-quality):** `:437` uses `special.level * -10` → renders "with 0" (Toxic is unleveled, pack `level:0`), should be canon "−5 per point of Damage taken"; failure damage uses the weapon's `damageType` not canon **Impact** ignoring Armour/TB (RT Core :6005); plus a config/data contradiction — `attack-specials.mjs:254` declares Toxic `hasLevel:true` vs pack `hasLevel:false`. **QA-127 Snare (dh-leftover):** `:428-435` is the DH2/Black-Crusade Snare (level penalty + Unnatural ±5 / Size ±10 / "Snared" action-throttle block) vs RT 1e's plain "Agility Test or immobilised/helpless; escape via Strength or Agility Test" (RT Core :5991); same `hasLevel:true` config mismatch (`attack-specials.mjs:238`). NOTE: Haywire effect-text DH2-phrasing + negative-radius already filed (QA-112); Hallucinogenic (`:415`) also uses the suspect `level * -10` pattern but its RT canon wasn't fully verified this pass — flagged for the fix task. (QA-AUDIT)
- [x] QA-HEAVY-BRACING — Completeness-critic re-scan (post QA-127): audited the Heavy-weapon bracing subsystem end-to-end vs RT Core p.116 — never covered by QA-WEAPONS (which audited weapon *qualities*, not weapon-class firing rules) nor QA-COMBAT-ACTIONS (which read the action *list* but not whether "Brace Heavy Weapon" does anything). 3 findings (QA-128..130). **QA-128 (P1):** the unbraced-Heavy −30 BS penalty is never applied — the attack pipeline never reads weapon `class` for bracing (`roll-data.mjs:211-265` reads `system.class` only for Weapon Master `:262`; `Heavy` appears only in a docstring `roll-helpers.mjs:80`), and `brace` exists ONLY as the description-only `Brace Heavy Weapon` combat action (`combat-actions.mjs:113-117`, a Half action with no `attack` modifier and no state set); no `braced` flag is stored/read anywhere — RT Core `:5869` "Firing a heavy weapon without bracing incurs a -30 penalty to hit." **QA-129 (P2):** unbraced Heavy weapons should be prohibited from Semi-/Full-Auto fire (`:5869`) but fire-mode selection is never gated by bracing, so they still get the auto-fire +10/+20 and extra hits. **QA-130 (P2):** the canon counter-effects that grant "counts as braced" — Auto-Stabilised trait (`traits.yml:5-6`), Suspensors weapon-mod (`weapon-mods.yml:380-381`), Bulging Biceps talent (`talents.yml:351`, RT `:4905`) — are all unwired (grep-zero for `auto.stab|stabili|suspensor` in `src/module/`). NOTE: the Brace action correctly carries no positive to-hit bonus (bracing removes a penalty, it isn't a buff). Appended fix task below. (QA-AUDIT)
- [x] QA-FLAME-SPRAY — Completeness-critic re-scan (post QA-130): audited the Flame/cone weapon subsystem end-to-end vs RT Core p.142 — never covered by QA-WEAPONS (whose NOTE claimed Spray "ARE correctly wired", a surface claim this disproves) nor QA-COVER-AREA. 3 findings (QA-131..133). **QA-131 (P1, dh-leftover+automation-gap):** the no-BS-test cone path keys on the **Spray** quality (`action-data.mjs:180-185` force success/dos=1; `roll-data.mjs:217,222-224` ignoreModifiers/Degrees/Success) — DH2/Black-Crusade terminology — but RT 1e flamers carry `flame:true` (9 weapons; ZERO `spray:` flags in any pack) which migrates to an embedded **Flame** attack-special (`item.mjs:242-260`), so `hasAttackSpecial('Spray')` is always false; the pack has a **Flame** entry (`attack-specials.yml:58`, desc "wielder does not need to Test Ballistic Skill … always considered to hit … body") but NO Spray entry, and "Spray" is a dead catalog name (`attack-specials.mjs:242`). Net: the force-hit path is unreachable and flamers wrongly roll BS-to-hit + a random hit location, contradicting their own attack-special text — canon RT Core `:5921-5937`. **QA-132 (P2):** the Flame "jams on a 9 on the Damage die (before bonuses)" rule (`:5937`) is unmodeled — flamers use the normal d100 to-hit jam threshold (`action-data.mjs:209-218`) they should not even roll. **QA-133 (P3):** the catch-fire text (`damage-data.mjs:408-410`) collapses RT's TWO Agility tests (avoid-hit, then second test or catch fire `:5923`) into one and never the avoid-hit test; the untrained/heavy-unbraced +20/+30 Agility-to-targets relief (`:5935`) is unwired (ties QA-124/QA-128). Appended fix task below. (QA-AUDIT)
- [x] QA-FIRING-MODES — Completeness-critic re-scan (post QA-133): audited the variable weapon-firing-mode subsystem (Maximal plasma mode + the Las Overcharge/Overload settings) end-to-end vs RT Core p.116 — never covered by QA-WEAPONS (which audited weapon *qualities*, not mode toggles; its NOTE listed "Maximal … ARE correctly wired" but never checked reachability) nor QA-AMMO-SPECIALS. 2 findings (QA-134..135). **QA-134 (P2, automation-gap):** Maximal is fully coded (`damage-data.mjs:283-287` +1d10, `:341-342` +2 pen, `ammo.mjs:221-222` ×3 ammo) but UNREACHABLE — no weapon carries a `maximal` flag (`grep -rni maximal src/packs/` → only one ammo-prose mention; plasma weapons store `special:{overheats:true}` with no maximal), and there is NO Maximal mode toggle (the prompt's firing-mode dropdown is `{{#if isLasWeapon}}` only, `type==='Las'`; plasma `type: Plasma` gets none), so `hasAttackSpecial('Maximal')` is always false → dead code. Canon RT Core `:6318-6320` makes Maximal a selectable per-shot mode on every Imperial plasma weapon (+10m/+1d10/+2 Pen/×3 ammo/gains Recharge/+2 Blast); the +10m range, dynamic Recharge, and +2 Blast aren't even coded. **QA-135 (P2, dh-leftover):** the `las_fire_modes: ['Standard','Overcharge','Overload']` toggle (`config.mjs:68` → `roll-data.mjs:18,196` → `attack-specials.mjs:18-41` synthesises the special → +1/+2 dmg `damage-data.mjs:277-280`, +2 pen `:346-347`, ×2/×4 ammo `ammo.mjs:209-213`) has NO RT 1e Core basis — RT's only las power-boost is the **Overcharge Pack** *modification* (+1 dmg / clip halved, `:6891`), there is no "Overload" las setting; it's an Only War/Black-Crusade import. Contrast: the canon Overcharge Pack mod is itself unwired (QA-066) while this non-canon setting is fully wired. Appended fix task below. (QA-AUDIT)
- [x] QA-PRIMITIVE-DICE — Completeness-critic re-scan (post QA-135): audited the damage-dice modifier qualities Tearing/Primitive/Proven (`damage-data.mjs:126-171`) end-to-end vs RT canon — QA-WEAPONS' NOTE asserted "Tearing/Proven/Primitive ARE correctly wired" without verifying the dice math (the same surface-claim pattern QA-FLAME-SPRAY/QA-OVERHEATS/QA-FIRING-MODES disproved). 2 findings (QA-136..137). **QA-136 (P0):** Primitive is implemented as a DH2/Black-Crusade per-die **cap** (`:155-162`) instead of RT 1e's defender-side **armour-doubling** (RT Core p.142 `CoreBook-1-200.pdf:5961-5963`); worse, the cap `level` resolves to **0** (weapons store boolean `primitive:true`; pack Primitive is `hasLevel:false,level:0` so `_getAttackSpecials` never lifts the level) → `result.result > 0` always true → `modifiers['primitive'] = 0 − dieValue` **zeroes every primitive-weapon die** across all 29 Primitive weapons (Flintlock `1d10+2` → flat 2). The RT armour-doubling-vs-Primitive-weapons rule is also absent from `assign-damage-data.mjs` (distinct from QA-025's inverse Primitive-*armour* case). **QA-137 (P2):** the per-die Primitive/Proven adjustment is an **overwritten scalar** (`:160,:167` assignment not `+=`) → wrong total for any multi-die weapon (only the last qualifying die counts); Proven is additionally **dead** (no pack entry, no weapon carries it → `hasAttackSpecial('Proven')` never true); config (`attack-specials.mjs:206-212` hasLevel:true) contradicts the pack (false) for both. NOTE: Tearing (`:128-135`, `2d10kh1` keep-highest, discarded dice skipped at `:150`) IS canon-correct. Appended fix task below. (QA-AUDIT)
- [x] QA-WEAPON-QUALITY-CATALOG — Completeness-critic re-scan (post QA-137): audited the REMAINING `_calculateSpecials` quality handlers QA-125/126/127 deferred (Concussive/Corrosive/Crippling/Felling/Graviton/Hallucinogenic/Indirect) for BOTH RT-validity and reachability — the QA-SPECIAL-EFFECT-TEXT-FIX note explicitly punted "re-verify wording vs RT/ItS" on these. 3 findings (QA-138..140). **QA-138 (P2, dh-leftover):** all 7 are Dark Heresy 2e / Black-Crusade / Only-War qualities ABSENT from RT 1e's definitive weapon-quality list (RT Core p.142-145, `CoreBook-1-200.pdf:5899-5974` = the 26 in the pack); canon-grep confirms Felling/Hallucinogenic=0 anywhere, Concussive/Indirect/Corrosive only descriptive prose, Crippling=the "Crippling Strike" talent, Graviton=a weapon (Graviton Gun) whose engine text doesn't even match its real Blast(5)/incapacitate rule. **QA-139 (P2, automation-gap, SYSTEMIC):** `_getAttackSpecials` (`item.mjs:249`) silently drops any weapon special-flag with no attack-specials PACK entry (no `else`, no warn) — so Geode Grenade (`hallucinogenic`), Javelin + Stun Grenade (`concussive`) lose their signature quality at migration; generalises the one-off dead-catalog findings (Spray QA-131, Proven QA-137, Unstable QA-015) — config catalog ⊋ pack. **QA-140 (P3, dh-leftover):** the Hammer Blow→Concussive injection (`damage-data.mjs:378-385`) gates on `hasTalent('Hammer Blow')`, a DH2 talent ABSENT from talents.yml (RT uses Crushing Blow `:503`) → dead, and would add the non-RT Concussive quality. Appended fix task below. (QA-AUDIT)
- [x] QA-UNNATURAL-DAEMONIC — Completeness-critic re-scan (post QA-140): audited the Unnatural-Characteristic / Daemonic soak-and-bonus application engine (`roll-helpers.mjs:364 unnaturalCharacteristicMultipliers` + `:397 daemonicToughnessMultiplier`, applied in `acolyte.mjs:365-379`, `base-actor.mjs:124`, `assign-damage-data.mjs:57-58`) end-to-end vs RT Core p.368/p.364 — never covered (QA-077 touched only Unnatural *Speed*; cybernetics/movement passes touched the multiplier in passing). 2 findings (QA-141..142). **QA-141 (P2, dh-leftover):** `_computeMovement` (`base-actor.mjs:143`) derives movement from `agility.bonus`, which folds in `.unnatural` (`:124`), so Unnatural Agility wrongly DOUBLES movement — RT Core p.368 (`CoreBook-201-401.pdf/markdown.md:6857`) explicitly says movement uses the *unmodified* AgB; the engine's own helper docstring (`roll-helpers.mjs:353-354`) flags the rule it violates. Inverse error to QA-077 (Unnatural Speed unwired); the two cancel only for creatures with BOTH traits. **QA-142 (P1, automation-gap):** `daemonicToughnessMultiplier` exact-matches `name === 'daemonic'` (`:399`), but 15 NPC trait entries carry the canonical "Daemonic (TB X)" name (vs 6 bare) — confirmed (TB X) is the doubled value (Ebon Geist `npcs.yml:1515` "Daemonic (TB 8)", toughness.base 40 ⇒ TB 4×2=8), so all 15 silently soak with single TB (half RAW). Sibling helpers (quadruped/unnatural) tolerate the parenthetical; this one doesn't. NOTE: TB/SB damage-soak, melee-SB, initiative (incl. Lightning-Reflexes ×Unnatural), and the unnatural multiplier parse itself are all RT-correct. Appended fix task below. (QA-AUDIT)
- [ ] QA-UNNATURAL-DAEMONIC-FIX — (from QA-141/142) (a) In `_computeMovement` (`base-actor.mjs:143`) base movement on the *unmodified* tens-digit Agility Bonus (`Math.floor(agility.total/10)`), not `agility.bonus`, and layer the Unnatural Speed doubling (QA-077) on that raw base — coordinate with QA-MOVEMENT-FIX so the two don't re-introduce the cancellation. (b) In `daemonicToughnessMultiplier` (`roll-helpers.mjs:399`) match Daemonic by prefix excluding "Daemonic Presence" (`/^\s*daemonic\s*(\(|$)/i`), with a node test (`Daemonic (TB 8)`→2, `Daemonic Presence`→1); optionally honour an explicit "(TB X)" as a direct soak-TB override. Foundry-coupled for (a) — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [x] QA-CONSUMABLE-DRUGS — Completeness-critic re-scan (post QA-142): audited the ENGINE-CONSUMABLE-ACTIVATE combat-drug subsystem (`roll-helpers.mjs:605-661` `CONSUMABLE_ACTIVATIONS`/`consumableActivation` + `:581-590` `parseConsumableDuration`, applied by `item-sheet.mjs:72-116 _useConsumable`) end-to-end vs RT Core p.142-143 / ItS Ch.III — real engine code built by the effects-audit loop, never covered by any QA dimension. 4 findings (QA-143..146). **QA-143 (P2, automation-gap):** the two talent-granting drugs (Frenzon→Frenzy, Cold Fire→Battle Rage) are mechanically inert — `_useConsumable` only posts a chat reminder (`item-sheet.mjs:106-107`) and their specs carry `changes: []`, so using Frenzon never confers Frenzy nor fires its +10 WS/S/T/WP / −20 BS/Int conditional bonuses, even though both talents exist (`talents.yml:1023`/`:214`) and the `flags.rt.grants` machinery exists (BUG-009/010) — a deliberate-but-real limitation (timed grants need auto-removal). **QA-144 (P3):** comedown penalties (Stimm/Slaught/Spur/Wideawake −20 / +Fatigue) and Attention Spanner's success/failure branch are note-only — no expiry follow-up effect, no per-use focus Test rolled, so even Attention Spanner's clean-AE +30 Int is dropped. **QA-145 (P3, Foundry-coupled):** seconds-based drug AEs (minutes/hours drugs) never auto-expire outside an active Combat (worldTime doesn't tick), so a downtime drug persists until hand-deleted. **QA-146 (not-a-bug):** all 8 drug profiles (Frenzon/Stimm/Slaught/Spur/Cold Fire/White Void/Wideawake/Attention Spanner) verified canon-correct + reachable (name-match strips "(drug)"); De-Tox/Obscura/Tranq correctly excluded as non-buffs. Appended fix task below. (QA-AUDIT)
- [x] QA-SHIP-CREW-ACTIONS — Completeness-critic re-scan (post QA-146): audited the voidship CREW-ACTION resolution subsystem (`voidship.mjs:104-153` `rollCrew`/`rollTurrets`/`rollBoarding` + `action-data.mjs:301-396` `_calculateVoidshipHits`/`_calculateVoidshipHit`/`calculateResultVoidship`, rendered live in `simple-roll-chat.hbs:85-113`) end-to-end vs RT Core p.218-220 — QA-SHIP (QA-042..047) audited weapon damage/crit/shields/crippled but never the crew-check roll mechanics. 4 findings (QA-147..150). **QA-147 (P1):** turrets are modeled as an OFFENSIVE weapon — `rollTurrets` rolls each turret-rating point as a d100 vs crewRating scoring 1-or-2 "hits" (with the homebrew ÷10 double, cf. QA-044) — but RT Core p.220 (`:1198`) makes turret rating purely DEFENSIVE (−10 to enemy Hit-and-Run pilot tests, +10 to the ship's boarding Command Test, shoots down torpedoes/assault craft the engine doesn't model); the offensive roll has no RT basis and the real bonuses are unwired. **QA-148 (P1):** boarding is a homebrew per-`boarding.actions` d100 loop vs crewRating (`action-data.mjs:302-318`), but RT Core p.219 (`:990`) boarding is a SINGLE opposed Command Test (+10/10 Crew-Population diff, +10/10 Hull diff, +10/turret-rating), degrees→1d5 Crew+1d5 Morale OR 1 Hull, then a Morale rout check, initiated by a Hard(−20) Pilot+Manoeuvrability Test — none of which exists; the `boarding.{actions,damage}` schema has no canon counterpart. **QA-149 (P2, dh-leftover):** `_calculateVoidshipHit` grades every Maneuver/Detection check on an invented 5-tier ladder (Flawless ≤target/10, Success ≤target, Flawed Success ≤target+10, Failure, Fumble) — RT tests are pass/fail + DoS tens (p.22 `:1586`); a roll up to 10 OVER target wrongly reads "Flawed Success" instead of a 1-degree failure, and RT defines no critical-success band on skill checks. **QA-150 (P3):** `rollCrew` uses a flat `crewRating + maneuverability/detection` target, dropping each Manoeuvre's canon Pilot(Space Craft)+Manoeuvrability skill and named difficulty (Come-to-New-Heading Difficult −10 `:965`, Disengage Challenging +0 `:984`, Ram/Board Hard −20 `:990`). Appended fix task below. (QA-AUDIT)
- [x] QA-PSYCHIC-SUSTAIN — Completeness-critic re-scan (post QA-150): audited the psychic Sustaining-Multiple-Powers subsystem (`psy.currentRating = rating − sustained` at `acolyte.mjs:397`, fed into the standalone damage button `rollPsychicPowerDamage:139-148`) end-to-end vs RT Core p.157 — never covered by QA-PSYCHIC (which audited Focus Power +5×PR, phenomena logic, per-DoS scaling, Navigator tiers, NOT sustaining). 2 findings (QA-151..152). **QA-151 (P2):** the sustained-reduced `currentRating` reaches ONLY the orphaned standalone damage button; the real cast path `PsychicRollData.initialize()` (`roll-data.mjs:391`) reads raw `psy.rating`, so the Focus Power Test / range / in-cast damage / phenomena all ignore the Sustaining-Multiple-Powers reduction (`:8064` "reduces the effective Psy Rating of both powers by 2/3/…"), and the two damage paths can roll the same power at different PR. **QA-152 (P3):** `currentRating` has no `Math.max(0,…)` floor (negative PR feeds `1d10+@pr`/range) and no "power ends at PR 0" handling (`:8064`); the linear `rating−sustained` also over-counts the single-power case (canon: 1 power = no reduction) and the "Sustained Rating" field label is ambiguous (reduction vs power count). NOTE: the Sustaining +10/extra-power phenomena modifier was already filed (QA-039); not re-filed. Appended fix task below. (QA-AUDIT)
- [x] QA-SHIP-WEAPON-ATTACK — Completeness-critic re-scan (post QA-152): audited the voidship WEAPON-ATTACK roll path (`voidship.rollItem`→`performShipWeaponAttack` `targeted-action-manager.mjs:141` → `WeaponRollData` ship branch `roll-data.mjs:252-326` → `action-data.mjs:142-145` `calculateSuccessOrFailure`→`_calculateVoidshipHits("Weapon", strength)` `:336-367`) end-to-end vs RT Core p.217 — QA-SHIP (042..047) audited hull-damage tier / crit results / crit trigger / shields / crippled and QA-147..150 the crew/turret/boarding/maneuver checks, but never the weapon **to-hit / number-of-hits** model. 2 findings (QA-153..154). **QA-153 (P1):** the engine rolls `weapon.system.strength` **independent** d100 BS tests and counts successes (`voidshipShotsAmount = strength`, `roll-data.mjs:326`; per-roll classify `:336-367`), and **never computes DoS** for ship weapons — but RT Core `:1208` makes a macrobattery a **single** BS test scoring `1 hit + 1 per DoS, capped at Strength` (Strength = the *maximum* hits `:1172`; worked example `:1250` BS58/roll29 → 2 DoS → 3 hits). The accuracy→volume mechanic central to void combat is absent; hits are a binomial spread unrelated to the roll's quality. **QA-154 (P2):** the distinct lance rule (`1 hit + 1 per 3 DoS`, `:1212`) is unimplemented — lances route through the same per-Strength independent-roll path with no class branch (masked only because pack lances are Strength 1-2). NOTE: the per-roll crit trigger `≤target/10` ignoring Crit Rating is QA-044 (not re-filed); the homebrew Nonpen/Pen damage *tier* is QA-042 (not re-filed); ship range-band ±2 damage (`action-data.mjs:504-512`) is the 0.7.11 fix. Appended fix task below. (QA-AUDIT)
- [x] QA-COMBAT-CIRCUMSTANCE-MODS — Completeness-critic re-scan (post QA-154): audited the personal attack-modifier pipeline against RT Core's Standard Combat Modifiers table (p.244, `CoreBook-201-401.pdf/markdown.md:2369-2468`) — which situational to-hit modifiers the engine auto-applies vs leaves to the manual Difficulty/Modifier prompt fields. 3 findings (QA-155..157). The pipeline auto-derives ONLY the combat-action modifier (`combat-actions.mjs:20-25`), target-size (QA-109/110), range band (QA-097), Aim, and Called Shot −20; everything else routes through the prompt's generic Difficulty dropdown (`weapon-roll-prompt.hbs:144`) + free Modifier field. **QA-155 (P2, rollup):** the whole Table 9-7 circumstance set — Ganging-Up/Outnumbering +10/+20, Higher Ground +10, Prone-target +10 WS/−10 BS, Shooting-into-Melee −20, Darkness −20/−30, Difficult/Arduous terrain, Fog/Mist/Shadow/Smoke −20, unarmed-vs-armed −20 — is neither auto-applied nor surfaced as toggles (grep-confirmed zero attack-logic hits); the target-state ones are token-adjacency-computable (engine already measures `tokenDistance`). **QA-156 (P2):** Shooting-into-Melee −20 + its Stunned/Helpless/Unaware waiver + the optional Stray-Shots reallocation are absent though adjacency data exists. **QA-157 (P2):** Helpless-target coup-de-grace (damage rolled twice + summed, auto-hit, two natural-10s = automatic Righteous Fury) is unimplemented — `damage-data.mjs:_calculateDamage` only does the standard confirm-to-hit RF, no `helpless` anywhere in `src/module`; gated on the absent condition layer (QA-080). NOTE: excludes already-filed rows — Surprised/Unaware +30 (QA-118), Stunned +20 (QA-121/122), Fatigued −10 (BUG-004), weapon-without-Talent −20 (QA-124), cover/concealed (QA-111). Appended fix task below. (QA-AUDIT)
- [x] QA-THROWN-WEAPON-SB — Completeness-critic re-scan (post QA-157): audited the personal damage Strength-Bonus application path (`damage-data.mjs:_calculateDamage` SB add `:224-225` + RF-extra SB `:180-181`, gated on `item.mjs` isMelee/isThrown/isRanged getters) end-to-end vs RT Core p.117 — QA-WEAPONS / QA-PRIMITIVE-DICE / QA-UNNATURAL-DAEMONIC touched melee-SB and the damage formula but never the **thrown**-weapon SB rule. 1 finding (QA-158, P1). **Thrown muscle-powered weapons get no Strength Bonus to damage**: SB is added only when `actionItem.isMelee` (class==='melee'), but a thrown weapon is `isThrown`/`isRanged` (class==='thrown') — mutually exclusive — so knives/axes/spears/Warhammer(`2d10`)/Javelin/Hunting Quoit roll bare dice, violating RT Core p.117 (`CoreBook-1-200.pdf/markdown.md:5866` "When using a thrown weapon you add your SB to the damage you inflict (with the exception of explosives such as grenades)"). Grenades correctly get no SB but only by accident (the branch excludes ALL thrown weapons, not just grenades). Discriminator for the fix: grenades carry `system.type: 'Grenade'`, muscle-thrown weapons carry Primitive/Exotic/etc. Appended fix task below. (QA-AUDIT)
- [ ] QA-THROWN-WEAPON-SB-FIX — (from QA-158) In `damage-data.mjs:_calculateDamage`, add `this.modifiers['strength bonus'] = SB` when `actionItem.isThrown && actionItem.system.type !== 'Grenade'` (exclude grenades/explosives per RT Core p.117), and apply the same gate to the Righteous-Fury melee-extra SB at `:180-181` so a thrown RF extra roll also gets SB. Foundry-coupled — verify on rt-smoke (a thrown knife/Warhammer shows a `strength bonus` damage row; a frag grenade does not). Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-CIRCUMSTANCE-MODS-FIX — (from QA-155/156/157) Add a "Combat Modifiers" section to the attack prompt (`weapon-roll-prompt.hbs`) with checkboxes for the common Table 9-7 rows (Higher Ground, Darkness, Difficult/Arduous terrain, Fog, unarmed-vs-armed), each writing its canon value into the modifier sum; auto-derive the token-adjacency ones (Prone-target, Shooting-into-Melee −20 with the Stunned/Helpless/Unaware waiver, Outnumbering +10/+20) from token positions (reuse `targeted-action-manager.tokenDistance`); add a Helpless toggle that doubles the damage roll, auto-succeeds the to-hit, and auto-confirms RF on two natural-10s. Coordinates with the condition layer (QA-080) for Prone/Helpless/Unaware state. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-SHIP-WEAPON-ATTACK-FIX — (from QA-153/154) Rebuild the voidship weapon to-hit/hits model to RT RAW: replace the `strength`-independent-rolls loop (`_calculateVoidshipHits("Weapon", …)`) with a **single** Ballistic Skill Test (reuse `_calculateHit` + the canon DoS computation), then derive hits = `Math.min(strength, 1 + dos)` for macrobatteries and `Math.min(strength, 1 + Math.floor(dos/3))` for lances (branch on `weapon.system.class`/type), building that many `voidshipResults` from the one roll; fire at most one crit, triggered when DoS ≥ the weapon's Crit Rating (coordinates with QA-044 + the QA-SHIP-DAMAGE-MODEL-FIX `critRating` field). Bundle with QA-SHIP-DAMAGE-MODEL-FIX (damage rolled once per hit + void shields cancel hits first). Foundry-coupled — verify on rt-smoke (Str-4 macrobattery, gunner 58, roll 29 → 3 hits). Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-PSYCHIC-SUSTAIN-FIX — (from QA-151/152) Make sustaining consistent + RT-correct: source the base rating in `PsychicRollData.initialize()` from `psy.currentRating` (not raw `psy.rating`) so the Focus Power Test / range / damage / phenomena all honour the reduction like `rollPsychicPowerDamage` does; clamp `currentRating = Math.max(0, rating − sustained)` in `acolyte.mjs:397` and surface a "power ends" notice at 0; clarify the `psy.sustained` field semantics (additional powers maintained, reduce only at ≥2 — or relabel as the direct reduction). Foundry-coupled — verify on rt-smoke (Sustained 2, PR 5 → effective PR 3 in both the cast test and the damage button). Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-SHIP-CREW-ACTIONS-FIX — (from QA-147/148/149/150) Rebuild voidship crew actions to RT RAW: (a) make turret rating defensive — retire the offensive `rollTurrets`/`_calculateVoidshipHits("Turrets")` path, apply −10×rating to enemy Hit-and-Run pilot tests and +10×rating to the boarding Command Test (needs torpedo/squadron + Hit-and-Run subsystems for the shoot-down role); (b) replace the boarding d100 loop with a single opposed Command Test between ship leaders (population/hull/turret bonuses → winning-degree Crew+Morale or Hull damage → loser Morale rout check), initiated by the Hard(−20) Pilot+Manoeuvrability Test; (c) resolve Maneuver/Detection crew checks as plain pass/fail + DoS (drop the Flawless/Flawed-Success/Fumble ladder in `_calculateVoidshipHit` + `simple-roll-chat.hbs`); (d) thread a per-Manoeuvre difficulty (−10/+0/−20) into `rollCrew` and prefer an assigned bridge officer's Pilot/Detection skill over the flat Crew-Rating fallback. Large ship-combat rework — Foundry-coupled, verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-CONSUMABLE-DRUGS-FIX — (from QA-143/144) Make activated drugs apply their full mechanical payload: on a `grantsTalent` drug, embed the named talent (resolve from the talents pack like `applyItemGrants`, tag `flags.rt.consumable`) and delete it when the timed AE expires; add an AE-expiry hook that auto-creates each drug's comedown effect (−20 to the listed Tests + Fatigue addend) and clears seconds-based effects out of combat (world-time/`updateWorldTime` check or a manual "effect ended" control on the chat card); roll Attention Spanner's focus Test at activation to pick the +30-Int vs −20-all branch. Frenzon's Fear-immunity + Stimm's no-Stun land with the QA-080 condition layer. Foundry-coupled — verify on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-WEAPON-QUALITY-CATALOG-FIX — (from QA-138/139/140) Purge the non-RT weapon-quality handlers from `_calculateSpecials` (`damage-data.mjs`): delete the `concussive`/`corrosive`/`crippling`/`felling`/`graviton`/`hallucinogenic` cases and the dead Hammer Blow→Concussive block (`:378-385`); decide if `indirect` maps to a real ItS mortar rule and if so re-author it to ItS canon WITH a pack entry. Strip the now-orphaned `concussive`/`hallucinogenic` flags from the affected weapons (Geode Grenade, Javelin, Stun Grenade) or replace with an RT-valid quality (e.g. Stun Grenade → Shocking/Blast, Geode Grenade → Toxic). Add a `game.rt.warn` in `_getAttackSpecials` (`item.mjs`) when a flag has no pack match so future config↔pack drift surfaces instead of failing silently. Bundle with QA-SPECIAL-EFFECT-TEXT-FIX. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-PRIMITIVE-WEAPON-FIX — (from QA-136/137) Retire the DH2 Primitive die-cap in `damage-data.mjs:155-162` and implement RT Primitive in damage assignment instead — when the attacking weapon has the Primitive quality and the struck location's winning armour piece is non-Primitive, **double** that location's AP before subtracting penetration (defender-side companion to QA-025; both need the winning piece's `type` threaded into `assign-damage-data.mjs`). Separately make the surviving per-die quality (Proven, if kept) **accumulate** (`+=`) across dice instead of overwriting, add a `Proven` attack-special pack entry (`hasLevel:true`) if Proven support is wanted, and reconcile the Primitive/Proven `hasLevel` config↔pack mismatch. Foundry-coupled — verify a Flintlock rolls full `1d10+2` and a target's armour doubles vs it on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-FIRING-MODES-FIX — (from QA-134/135) Wire the Maximal plasma mode and retire the non-RT Las settings: add a `maximalMode` toggle gated on `weapon.system.type==='Plasma'` (mirror the `lasMode` selector) that pushes a Maximal attack-special so the existing dmg/pen/ammo paths fire, AND apply the +10m range / dynamic Recharge (ties QA-016) / +2 Blast the Maximal block currently omits; separately drop the `las_fire_modes` Overcharge/Overload settings (reduce to `['Standard']` + remove the `attack-specials.mjs:18-41` synthesis) and instead wire the canon Overcharge Pack modification (+1 dmg, halve `clip.max`) per QA-066. Foundry-coupled — verify a plasma gun on Maximal shows +1d10/+2 Pen/3 rounds on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-FLAME-SPRAY-FIX — (from QA-131/132/133) Rewire flamers to RT Flame RAW: key the cone/no-BS-test path on the **Flame** quality instead of the dead "Spray" (`action-data.mjs:180`, `roll-data.mjs:217`), force the hit location to **Body** for Flame hits, and retire/redirect the "Spray" catalog name (`attack-specials.mjs:242`); evaluate Jam from the damage roll (any raw die === 9 pre-bonus) and skip the d100 to-hit jam check for Flame weapons; emit both Agility tests (avoid-hit, then catch-fire on damage) and grant targets +20 Agility when the wielder lacks the weapon's Weapon Training talent (+30 for a heavy flamer fired unbraced — coordinates with QA-124 / QA-128). Foundry-coupled — verify a flamer auto-hits Body with no to-hit roll on rt-smoke. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-HEAVY-BRACING-FIX — (from QA-128/129/130) Build the Heavy-weapon bracing layer: add a per-weapon/per-attack `braced` state (weapon-prompt checkbox or set by taking the Brace Heavy Weapon Half action that Round); in `WeaponRollData.update()` add `modifiers['unbraced'] = -30` when `weapon.system.class === 'Heavy'` and not braced; exclude Semi-/Full-Auto Burst from the available modes for an unbraced Heavy weapon (force single shot); and treat the actor/weapon as auto-braced when it has the Auto-Stabilised trait, Bulging Biceps talent, or a Suspensors mod (suppress both the −30 and the auto-fire prohibition). Foundry-coupled — verify on rt-smoke (Heavy weapon unbraced → −30 + single-shot only; braced or Auto-Stabilised → no penalty + auto-fire allowed). Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-SPECIAL-EFFECT-TEXT-FIX — (from QA-125/126/127) Re-author the DH2-derived effect strings in `_calculateSpecials` (`damage-data.mjs`) to RT 1e canon — Shocking (`:425`: +10/Armour-point Toughness Test, Stun ½ Damage, no Fatigue), Toxic (`:437`: −5/damage-point Toughness Test, failure = 1d10 **Impact** ignoring Armour/TB), Snare (`:428`: Agility Test or immobilised/helpless, escape via Strength/Agility) — ideally sourcing the text from each attack-special item's already-canon `description` rather than hard-coded strings. Fix the `hasLevel` config so Toxic + Snare are `hasLevel:false` in `attack-specials.mjs:238,254` (RT has no leveled Toxic/Snare). Re-verify Hallucinogenic/Crippling/Corrosive/Concussive/Felling/Graviton wording vs RT/ItS while there (several may be DH2 qualities absent from RT's quality list). Bundle the condition-toggling half with QA-CONDITIONS-LAYER-FIX (QA-080). Fix loop, not discovery. (QA-AUDIT)

## Notes
- Findings → `QA_FINDINGS.md` (numbered QA-NNN, format in `specs/07`). Confirmed P0/P1
  engine bugs get promoted to `BUGS.md` by a human afterward.
- Verify before filing (file:line read + canon cite). No speculation. Discovery only.

## Out of scope
- Fixing (beyond trivially-safe gate-green); the chargen wizard (shelved); re-filing
  BUGS.md items; inventing non-RT rules.
