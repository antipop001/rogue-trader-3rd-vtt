# fix_plan — QA / correctness audit

A **discovery** loop. One task per iteration = audit ONE dimension per
`specs/07-qa-audit.md`: grep + read the relevant code/data, VERIFY each issue against
the source (and RT canon for rules claims), append numbered findings to
`QA_FINDINGS.md`, append any newly-found sub-areas as tasks here, gate green
(`build:check` + `npm test`), commit. **File findings; don't fix** (except a
trivially-safe, gate-green change). Don't re-file `BUGS.md` (fixed) items. Re-verify
CLAUDE.md's old audit list against current code — many are DONE.

## Active list

- [x] QA-DH-RULES — Audited. 3 findings (QA-001..003): DH2 background/homeworld/role/divination pipeline still runs in live `prepareData()` (dormant but latent-leak), `divinations.mjs` = Emperor's Tarot (no RT analog), `elite-advances` dead-store. All DH2 modules confirmed live-imported by `acolyte.mjs` + `config.mjs` but no UI/pack data sets the legacy bio fields. (QA-AUDIT)
- [x] QA-DH-SCHEMA — Audited. 5 findings (QA-004..008): aptitude system still live (acolyte field + `aptitude` Item type + Aptitudes panel on the active V2 sheet) (QA-004); talent schema DH2 `aptitudes`/`tier` rendered on item sheet, `tier` consumed by nothing (QA-005); fixed dangling `"Inf"` (removed Influence char) on `charm.characteristics` (QA-006, trivial fix, gate green); `threatLevel` getters with no consumer (QA-007); not-a-bug rollup — `subtlety` correctly absent, `influence` characteristic removed, parry-as-skill a deliberate documented deviation (QA-008). (QA-AUDIT)
- [x] QA-DH-NAMING — Audited. 1 rollup finding (QA-009): 21 `DarkHeresy*` sheet classes + the `DarkHeresy` config-namespace const (backs `CONFIG.rt`), ~91 identifier occurrences; 6 "Dark Heresy" strings (4 console in `hooks-manager.mjs`, 1 **in-app tour UI** `main-tour.mjs:7`, 1 data comment); stale `DH_CONTAINER_ID` + leftover `console.log('DarkHeresyItemContainer:…')` in `item-container.mjs`. `en.json` clean, no `.dh` global, no lowercase variants. Cosmetic — strings autofixable, bulk class rename deferred per CLAUDE.md §J. (QA-AUDIT)
- [x] QA-DH-SKILLS — Audited. 4 findings (QA-010..013). Two real governing-characteristic errors FIXED (trivial, gate green): `commerce` Int→Fel (QA-010), `survival` Per→Int (QA-011) — RT canon CoreBook-1-200 :3793/:3826; both were tested vs the wrong characteristic. Not-a-bug rollup (QA-012): skill list 48/48 canon-complete (+parry deviation) and ALL specialist specialty lists match RT canon exactly — CLAUDE.md deviation #5 (specialty drift) is RESOLVED. Filed QA-013: vestigial multi-entry `characteristics` arrays diverge from RT's single governing char (no roll impact; primary drives rolls). (QA-AUDIT)
- [x] QA-WEAPONS — Audited. 7 findings (QA-014..020). Real automation gaps: Force-weapon psyker bonuses unwired (QA-014, +PR dmg/pen + Focus bonus all missing); `Unstable` unwired AND absent from the `attackSpecials()` catalog (QA-015); `Recharge` not enforced — fires every Round (QA-016); Power-Field parry-destroy unautomated (QA-017); `Customised` ½-reload ignored (QA-018); target-side qualities (Concussive/Toxic/Shocking/Snare/Flame/Blast/…) are descriptive-only `addEffect` text, no forced test/condition (QA-019 rollup); Defensive help text says +10, code/canon = +15 (QA-020 cosmetic). NOTE: Inaccurate/Defensive/Scatter/Storm/Spray/Tearing/Lance/Maximal/Razor Sharp/Proven/Primitive/jam-overheat thresholds/Best-never-jams/Poor↔Good craftsmanship-reliability ARE correctly wired (`rules/attack-specials.mjs` + `damage-data.mjs` + `action-data.mjs`). (QA-AUDIT)
- [x] QA-ARMOUR-FIELDS — Audited. 6 findings (QA-021..026). Real bug FIXED (trivial, gate green): force-field Poor overload 15→**20** per canon Table 3-10 (QA-021, ItS :318 / Faith&Coin Table 3-5). `maxAgility` confirmed DH2-leftover — RT armour table has no Max-Ag column, zero code consumers (QA-022). Automation gaps: Poor armour −10 Ag unwired (QA-023), Good armour +1 AP first-hit/round unwired (QA-024), Primitive armour half-AP vs non-Primitive weapons unwired — `assign-damage` never reads armour `type` (QA-025), per-field force-field conditions/coverage (Mirror Shield arm/body-only, Power Field melee/3m exception, Conversion photon-flash) all unwired (QA-026). NOTE: Best craftsmanship (+1 AP, half-weight), mixing-armour highest-AP-per-location, and force-field overload Common/Good/Best ARE correctly wired. (QA-AUDIT)
- [x] QA-CYBERNETICS — Audited. 5 findings (QA-027..031). Real unwired bonuses: Synthetic Muscle Grafts +1 Strength Bonus (QA-027, needs a +SB schema/derivation addend — `strength.modifier` adds to raw, not bonus); Blackbone Bracing +2 unarmed damage dropped — only its two talent grants fire (QA-028); Augmented Senses never grants the Heightened Senses (choose-one) talent, no `grants` block (QA-029); whole craftsmanship-conditional cybernetic layer (Good/Poor on Bionic Arm/Locomotion/Respiratory/Cybernetic Senses/MIU/Synthetic-Best) unwired — engine reads cybernetic craftsmanship ONLY for armour Best +1 AP (QA-030). Not-a-bug rollup (QA-031): 13 cybernetics correctly AE/conditionalBonus/armour/grant-wired, ~14 correctly narrative/immunity/weapon-only. (QA-AUDIT)
- [ ] QA-TALENTS-TRAITS — Talents/traits the effects-audit loop recorded as "narrative" that are actually automatable; per-DoS/per-rank scaling; conditional gaps. (QA-AUDIT)
- [ ] QA-PSYCHIC — Phenomena/Perils auto-roll path correctness, per-DoS damage scaling, Navigator tier progression, range edges. (QA-AUDIT)
- [ ] QA-SHIP — Homebrew critical-damage tables vs RT Table 8-12, VU positioning, Strategic Round, component-bonus coverage. (QA-AUDIT)
- [ ] QA-SHEET-UI — Sheet fields shown but non-functional; computed-but-not-displayed; no-op controls. (QA-AUDIT)
- [ ] QA-ACQUISITION — Profit Factor flows + acquisition modifier coverage vs RT Core p.270. (QA-AUDIT)
- [ ] QA-DATA-QUALITY — NPC math (wounds.max=0, empty weapon `damage`, orphan skills), OCR artifacts in pack descriptions. (QA-AUDIT)
- [ ] QA-DH-BONUSES-PANEL — `bonuses-panel.hbs` (renders `backgroundEffects.abilities`) is registered as a preload partial (`handlebars-manager.mjs:20`) but is NOT invoked by any sheet template (`{{> ...bonuses}}` appears nowhere). Verify it's truly dead and catalogue other registered-but-uninvoked partials. (QA-AUDIT)
- [ ] QA-SKILL-CHAR-ARRAYS — (from QA-013) Normalise every skill's multi-entry `characteristics` array to the single RT-canon governing characteristic; confirm no sheet/prompt reads `characteristics[1+]` first. Data-only cleanup pass. (QA-AUDIT)
- [ ] QA-AMMO-SPECIALS — (from QA-WEAPONS) Audit `calculateAmmoAttackSpecials` / `calculateAmmoSpecials` (`rules/ammo.mjs`) + `weapon-modifiers.mjs`: special ammunition (Hellfire, Tox/Toxic, Snare Shells, Void Rounds, Manstopper, Tracer…) and weapon-mod qualities described in the `ammo`/`weapon-mods` packs but not applied to the roll. QA-WEAPONS only covered the base-weapon `attack-specials` path. (QA-AUDIT)
- [ ] QA-WEAPONS-DESCRIPTIVE-FIX — (from QA-019) Triage the descriptive-only target-effect qualities and wire the automatable subset (Concussive/Toxic/Shocking → Toughness test; Snare → Agility; Flame → Agility/on-fire) through the existing target-resist-roll path; leave narrative ones as text. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-ARMOUR-CRAFTSMANSHIP-FIX — (from QA-023/024/025) Wire the automatable armour-craftsmanship/type rules: Poor armour −10 Agility (roll modifier), Good armour +1 AP first-attack/round (per-round flag via the Reaction-budget machinery), Primitive armour half-AP vs non-Primitive weapons (thread winning-piece `type` into assign-damage). Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-CYBERNETICS-CRAFTSMANSHIP-FIX — (from QA-030) Build a cybernetic-craftsmanship resolution layer: when a cybernetic's `craftsmanship` is Good/Poor, apply its listed AE/conditionalBonus/grant/penalty (Good Bionic Locomotion → Sprint; Poor Bionic Arm → −10 WS/BS; Good Cybernetic Senses → Heightened Senses + +20; Best Synthetic Muscle Grafts → Unnatural Strength ×2 + −10 Ag). Currently only armour Best +1 AP reads cybernetic craftsmanship. Fix loop, not discovery. (QA-AUDIT)
- [ ] QA-STRENGTH-BONUS-ADDEND — (from QA-027) Add an additive Strength-Bonus path (`system.characteristics.<c>.bonusModifier` summed into `bonus = floor(total/10)+unnatural+bonusModifier`, or route via `.unnatural`) so flat "+N to Strength/Char Bonus" effects (Synthetic Muscle Grafts +1 SB, Bionic Arm Good +10 Str-using-arm) are expressible without inflating the raw characteristic by 10×. Schema/derivation change. (QA-AUDIT)
- [ ] QA-UNARMED-DAMAGE-ADDEND — (from QA-028) Add an additive flat-damage modifier to the unarmed/natural-weapon damage path (`damage-data.mjs` unarmed branch + `unarmedDamageProfile`) so cybernetics/talents granting "+N damage to unarmed attacks" (Blackbone Bracing +2) reach the roll. Currently only the dice *formula* varies by talent/trait. (QA-AUDIT)
- [ ] QA-CRITIC — Completeness pass: which dimensions were shallow / which modality wasn't run? Append the gaps as new tasks. (QA-AUDIT)

## Notes
- Findings → `QA_FINDINGS.md` (numbered QA-NNN, format in `specs/07`). Confirmed P0/P1
  engine bugs get promoted to `BUGS.md` by a human afterward.
- Verify before filing (file:line read + canon cite). No speculation. Discovery only.

## Out of scope
- Fixing (beyond trivially-safe gate-green); the chargen wizard (shelved); re-filing
  BUGS.md items; inventing non-RT rules.
