<div class="rt-panel rt-grid-col-2 {{isError actor.encumbrance.encumbered}}">
    <h1 class="rt-panel__header">
        {{#if actor.encumbrance.encumbered}}
            Encumbrance (overweight)
        {{else}}
            Encumbrance
        {{/if}}
    </h1>
    <div class="rt-field__wrapper">
        <span class="rt-field__header">Current Enc.</span>
        <span class="rt-field__span">{{actor.encumbrance.value}}</span>
    </div>
    <div class="rt-field__wrapper">
        <span class="rt-field__header">Max Enc.</span>
        <span class="rt-field__span">{{actor.encumbrance.max}}</span>
    </div>
    <div class="rt-field__wrapper">
        <span class="rt-field__header">Lift (kg)</span>
        <span class="rt-field__span">{{actor.encumbrance.lifting}}</span>
    </div>
    <div class="rt-field__wrapper">
        <span class="rt-field__header">Push (kg)</span>
        <span class="rt-field__span">{{actor.encumbrance.pushing}}</span>
    </div>
    {{#if actor.backpack.hasBackpack}}
        {{#unless actor.backpack.isCombatVest}}
            <div class="rt-field__wrapper">
                <span class="rt-field__header">Backpack Current Enc.</span>
                <span class="rt-field__span">{{actor.encumbrance.backpack_value}}</span>
            </div>
            <div class="rt-field__wrapper">
                <span class="rt-field__header">Backpack Max Enc.</span>
                <span class="rt-field__span">{{actor.encumbrance.backpack_max}}</span>
            </div>
        {{/unless}}
    {{/if}}
</div>
