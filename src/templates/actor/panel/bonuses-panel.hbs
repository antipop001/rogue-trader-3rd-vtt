<div class="rt-panel rt-grid-col-2 rt-grid-row-1">
    <h1 class="rt-panel__header">Bonuses</h1>
    <div class="rt-table--border rt-bonusestable--border">
        <div class="table-row--head">
            <div class="table-cell">Name</div>
            <div class='table-cell'>Source</div>
            <div class="table-cell--last">Source</div>
        </div>
        {{#each actor.backgroundEffects.abilities as | ability |}}
            <div class="table-row">
                <div class="table-cell">
                    {{> systems/rogue-trader-3rd/templates/actor/partial/trait-toggle.hbs toggle=(concat 'description_' ability.source) text=ability.name}}
                </div>
                <div class="table-cell">
                    <span>{{ability.source}}</span>
                </div>
                <div class="table-cell--last">
                    <label class="rt-control-button bonus-vocalize" data-action="bonusVocalize" data-bonus-name="{{ability.name}}">
                        <span class="rt-control-button__span material-icons">{{defaultVal icon 'record_voice_over'}}</span>
                    </label>
                </div>
                <div class="table-cell--description description_{{ability.source}}" {{hideIfNot
                    (isExpanded (concat 'description_' ability.source ))}}>
                    <span>{{ability.benefit}}</span>
                </div>
            </div>
        {{/each}}
    </div>
</div>
