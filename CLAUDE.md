# Rogue Trader 3rd Edition (Foundry VTT)

Unofficial Foundry VTT system for **FFG's Rogue Trader 1e** content using **Dark Heresy 2nd Edition mechanics** as its rules base, progressively migrated to RT 1e canon mechanics. The "3rd Edition" in the name refers to the system iteration, not a game edition — there is only one printed RT edition.

Forked from mrkeathley's Dark Heresy 2 system. Authored by MortarionUA. Compatible with Foundry VTT v13–v14.

**[LANDED in 0.8.0 — merged to `main`] (was branch `ralph/effects-audit`, off `ralph/backgrounds`): RT combat/psychic engine bug-fixes + an autonomous effect-wiring loop.** Two streams, all live-verified on rt-smoke (deploy + Playwright page-context). **(1) Nine engine bugs** the user hit in play, tracked in the new repo-root **`BUGS.md`** (symptom / root cause + file:line / fix / canon cite / verify per entry), all fixed: BUG-001 DoF was `1+getDegree` (DH2) → tens-diff RT 1e (DoF only; DoS keeps +1, combat hit-count depends on it); BUG-004 Fatigue threshold TB not TB+WB + remove characteristic-halving + −10 to all tests when fatigued; BUG-005 Assign Damage reads `armour.total` (worn+NA+cyber) not worn-only `.value`, fixed a `this.actor=await update()` clobber + `game.actors.get(id)` (wrong for unlinked tokens) crash before wounds wrote; BUG-006 psychic ranges stored as prose ("5m x Psy Rating") → `normalizePsychicRange()`; BUG-007 opposed powers post a "Roll Opposed" card (`_promptOpposed`+`opposed-prompt-chat.hbs`, suppress silent auto-roll for psychic); BUG-008 Righteous Fury → RT 1e RAW (confirm attack → extra damage roll, chains; melee +SB; dropped DH2 1d5 crit-table); plus BUG-002/003 (Paranoia +2 Init, Weapon Master +10/+2/+2) done by the loop. **Key Foundry gotcha (BUG-009/010, `flags.rt.grants` item-grants):** defer the grant off the `createItem` hook turn (`setTimeout`, single pass) AND embed on a **freshly-fetched** actor (`game.actors.get(actor.id)`), NOT the `item.parent` ref captured in the hook — that ref goes stale when the granting item's ActiveEffect re-prepares the actor, and `createEmbeddedDocuments` on a stale actor doc HANGS (this also caused the apparent intermittent clobber). **(2) The effect-wiring loop** (`loops/effects-audit/` kit, `specs/06`, run `./run_ralph.sh N` in tmux): 52 iterations, 0 rejections, 0 errors, node tests 51→154, AUDIT-CRITIC = 0 unresolved. Wired every actionable talent/trait/cybernetic/ammo/consumable effect (AEs / `conditionalBonuses` / `pickable` / `flags.rt.grants`), recorded narrative ones in the ratchet (`tests/chargen/effect_wiring_audit.test.mjs`), and built NEW subsystems: per-Round **Reaction budget** (`system.combat.reactions`, `canSpendReaction`, `combatTurnChange` reset), **natural/unarmed weapon** damage profiles, **item-grant** machinery, consumable timed-use, Rapid Reload `effectiveReload`, unnatural-characteristic multipliers, additive wounds/initiative modifiers. Self-halted with `.ralph/STOP` leaving ~27 `E2E-*` verification tasks. **Verification sweep (2026-06-24): all pass — incl. BUG-010 (choice-grants), fixed via the fresh-actor-ref root cause above.** Two deploy gotchas codified: (a) a `stop+rm-caches+start` can drop Foundry to the SETUP screen — relaunch via page `fetch('/setup',{method:'POST',body:JSON.stringify({action:'launchWorld',world:'rt-smoke'})})` (needs admin session; a plain `systemctl restart` auto-relaunches); (b) **deploy the root `template.json`** too (not just `module/templates/packs/`) — schema additions like `system.combat.reactions` need it (additive `?? 0` fields like initiative/wounds modifiers survive without it via AE-created paths, masking the gap). (Memory: `project_combat_engine_fixes`.)

**[LANDED in 0.8.0 — merged to `main`] (was branch `ralph/backgrounds`, off `main`): Origin-Path "background" automation — the chargen WIZARD is SHELVED.** Redirected the autonomous Ralph loop (2026-06-23) off the chargen wizard onto turning the **~95 Origin-Path background traits (+7 stray talents)** the origin data grants — e.g. `Mutant` (Tainted lure), `Void Accustomed`, `Ill-Starred`, `Acquisition:…`, `Klan:…`, `Kindred:…` — into **REAL compendium items with full ActiveEffects** so they "work like everything else" (drag-drop onto a sheet, AE auto-applies, resolve at `commit.mjs` by exact name). Today they commit as name-only **stubs** because the `traits` pack holds only 45 *creature* traits and nothing the origin path actually grants. **Wizard shelved reversibly (code intact):** the `renderActorDirectory` "Character Creation" button hook is commented out in `hooks-manager.mjs`, and `AcolyteSheetV2.CHARGEN_UI_ENABLED=false` gates the `openChargen` header control — the wizard + advanced builder live on parked branch `ralph/chargen` (its prior run reached 35/43 tasks; the rest need a live rt-smoke deploy). **Loop seed (commit `9740941`):** `specs/04-backgrounds-compendium.md` (authoring schema — trait YAML uses top-level `data:` description/source, AE block copied from talents like Hotshot Pilot with `mode 2` on `system.{characteristics|skills}.<k>.modifier`, `conditionalBonuses` for situational bonuses, `pickable` for "(choose one)", `name:` EXACT-match to the data grant), `fix_plan.md` (TAL-FIX-001 + BG-001…015 grouped by origin source with exact names + per-task ratchet targets), retuned `PROMPT.md`/`specs/README.md`, and the backpressure `tests/chargen/background_coverage.test.mjs` — a **ratchet** asserting resolved-grant counts never drop below a floor the loop raises per group (**traits 8→103, talents 72→79**). Items land in the existing `traits`/`talents` packs (one resolution source for `commit.mjs`); each tagged `flags.rt.category: 'Background'`. Dynasty/warrant *label* traits (`Acquisition:…`/`Contacts:…`/`Warrant Age:…`) stay text-faithful — their PF/Ship-Point effects are already applied by sibling `profit_factor`/`ship_points` effects, so no double-grant. Gate = `npm run build:check` (lean gulp `compile`: clean+scss+copy+packs, no release-zip) + `npm test`; green at seed (53/53). Canon source `/mnt/project_data/RT/RT-DOCS/` (cite the page in each `source:`); authored content can't be gate-verified for RT-rules correctness, so it is logged to `.ralph/data-vendor-queue.md` for separate review. (Memory: `project_chargen_parity_ralph` PIVOT note + loop setup `reference_ralph_loop_setup`.)

**Unreleased (local commits on main, not pushed: `c6d270f` Stage A, `f4eb7c7` Stage B): Chargen wizard Stage B — Origin Path with mechanical effects.** Full port of the RTT_MAKER origin-path engine. `module/chargen/origin.mjs` (pure JS, registry-passed, dice injected) interprets every declarative effect kind (`char_mod`, `char_mod_choice`, `grant_skill`, `grant_talent`+condition, `trait`, `profit_factor`, `ship_points`, `fate`, `wounds`, `insanity`/`corruption` fixed-or-rolled, `origin_xp_cost`, nested `choice`, cross-step `substitute`) plus the ItS machinery: option `xp_cost` hard-debited against the 500-xp pool (`OriginError` on overspend — the one warn-don't-block exception), `requires`/`excludes` clause groups (`{any_of|all_of}` over `{step,option}`/`{step,source}`/`{species}`/`{career}`), species-replaces-Home-World gating, `distinct_group` "choose N distinct", and the Warrant & Ship 2-D chart adjacency (printed-column rows + the three explicit width-change maps). Query API (`legalOptions`/`illegalityReasons`/`availableChoiceOptions`/`warrantLegalNext`) filters wizard dropdowns to legal picks. Wizard rebuilt on the **replay model**: all decisions live in `this.inputs`, the `ChargenState` is rebuilt from scratch on every change (editing an earlier step auto-invalidates downstream picks, dropped picks surface as warnings). Pages: Characteristics → Species (Kroot/Ork, species dice formulas feed the Stage-1 roller) → 5 origin steps → optional Warrant & Ship → Career (selection only) → Wounds/Fate + summary + commit. Commit mapping (`commit.mjs`): skills via `ADVANCE` + specialist specialities (unmapped → bio notes, never dropped), talents from the compendium by exact name → alias (`TALENT_ALIAS`: Resistance Psychic Powers→Techniques) → pickable base with pre-set `system.choice` ("(choose one)" left unset so the `onCreateItem` hook prompts) → stub+gap, traits from compendium else stub with rules text, `bio.originPath` labels (career via `CAREER_LABEL` casing table; **legacy `bio.homeWorld` deliberately never written** — Forge/Hive World collide with DH2 `homeworlds()` and would inject DH2 backgroundEffects), wounds/fate `{max,value,rolled:true}` (wizard stores final totals incl. Endurance/fate adjustments), `experience {total:500, used:originXpSpent}`, GM-only "apply PF to world setting" button. 51 node tests; e2e on rt-smoke (`tests/e2e/verify_chargen_b.py`, run with the RTT_MAKER venv python — system python lacks playwright) builds Void Born→Stubjack(WS, ins 3)→Tainted→Dark Voyage(ins 4)→Endurance→Rogue Trader, wounds TB4×2+3+1=12, insanity 7, 5 embedded trait items, bio + flags consistent.

**Earlier unreleased (same branch): Chargen wizard Stage A — Characteristics.** First slice of the in-Foundry character builder ported from the RTT_MAKER Python app (plan: `docs/chargen-plan.md`). New `module/chargen/` package (pure JS, no Foundry globals, dice injected): `characteristics.mjs` (2d10+25 roll / pool-assign / manual / fixed-36, bonus math, warn-only range checks, ItS species profiles), `state.mjs` (`ChargenState`, serializable, stored in `flags.rt.chargen` for resume/audit), `mapping.mjs` (the validated engine→schema tables ported from RTT_MAKER `export/foundry.py` — char keys, 49-skill table, specialities incl. the navigate/forbiddenLore "Warp" disambiguation, `ADVANCE`, origin-step + career keys), `commit.mjs` (state→actor: rolled base in `.base`, origin mods summed into `.modifier`), `data.mjs` + `data/*.json` (7 origin-path files vendored from RTT_MAKER via `tools/sync_chargen_data.py`, `--dry-run`/`--execute`). Wizard UI: `module/applications/chargen-wizard.mjs` (ApplicationV2 + Handlebars; **gotcha: AppV2 has a read-only `state` getter — the builder state property must be named something else**, here `chargenState`), template `templates/chargen/chargen-wizard.hbs`, SCSS `scss/components/_chargen.scss` imported at top level outside `.rt-wrapper`; content styled via `.rt-chargen-app .window-content.rt-chargen` to out-specify the V2 dark theme (gotcha #2). Entry points: "Character Creation" button in the Actors directory (`renderActorDirectory`, `ACTOR_CREATE`-gated) and an `openChargen` header control on `AcolyteSheetV2` (`_getHeaderControls()` append — don't redeclare `window.controls`, arrays replace). Tests: `npm test` → `node --test tests/chargen/` (28 cases mirroring the Python suite). Verified live on rt-smoke via Playwright (`tests/e2e/verify_chargen.py`): Roll All path (values 27–45, chat message, actor matches wizard, flags stored), manual-entry path (unclamped values, warn-only range warning), header control present. Not yet released — cut as **0.8.0** when ready.

**Current version: 0.8.32** (released 2026-07-07). **V1→DialogV2 dialog cleanup (follow-up to 0.8.31).** Migrated the remaining legacy V1 `Dialog` uses that could sit over a V2 sheet: `acquisition.mjs` (the Acquisition Test opened from item Acquire buttons) and the two simple `endeavours.mjs` sub-prompts (Add Objective / number entry) → **DialogV2** with `button.form`-scoped reads + V1 fallback (`fear.mjs` was already DialogV2). **Gotcha discovered + documented:** the endeavours **main tracker** was deliberately KEPT on V1 — DialogV2 form-wraps its content, and the browser strips nested `<form>` elements (invalid HTML), so the tracker's embedded `<form class="rt-endeavour-new">` (read via jQuery `attachListeners`) vanished under DialogV2 and add-endeavour broke (caught live: `form_exists:false`). Rule of thumb for future migrations: dialogs that read via **`button.form`** migrate cleanly (their inputs survive in DialogV2's wrapper form); dialogs that read a **specific nested form** must be restructured or stay V1. The tracker is a GM tool (not a sheet roll) so V1 is fine. All paths live-verified on rt-smoke (acquisition rolls; endeavour-add works 0→1; no JS errors). The remaining V1 `Dialog` in `rogue-trader-migrations.mjs` is a one-time migration confirm — left as-is. Gate green (300 node tests). (Memory: `project_combat_engine_fixes`.)

**0.8.31** (released 2026-07-07). **Roll-dialog fix — skill/damage/crew rolls silently not firing when the prompt sits over the sheet.** Root cause was two compounding bugs: (1) the roll prompts used the legacy V1 `Dialog` while the actor/voidship sheets are ApplicationV2 — mixing V1 dialogs with the V2 window z-index stack mis-layers them, so the "Roll" click could land on the sheet behind instead of the dialog (worst "when the dialog is on top"); (2) the button callbacks read fields via jQuery global-ID lookup (`html.find('#modifier')[0].value`) and several prompt templates reuse `id="modifier"`/`id="difficulty"`, so with a second dialog open (or a stale one) `document.getElementById` returns the FIRST match — cross-reading another dialog's field, or throwing on the unguarded `[0].value` and silently aborting the roll. Migrated the three sheet-roll prompts — **skills (`simple-prompt`), damage (`damage-prompt`), crew (`crew-prompt`)** — to **DialogV2** (the pattern already in `grapple.mjs`/`ship-combat.mjs`; shares the V2 window stack so it layers correctly) with **form-scoped `button.form.querySelector` reads** (never document-global, so duplicate IDs can't cross-read) and full guards, keeping a V1 fallback for older cores. Live-verified on rt-smoke: a single skill roll fires its chat card over the sheet; with **two stacked roll dialogs** each `form.querySelector('#modifier')` reads its OWN value (10/20) where `document.getElementById` returns only the first (10) — proving the cross-read (the "sometimes" trigger) is gone. The `rollSkill` logic itself was already correct (untrained-Advanced gate, Dodge/Parry reaction budget, Fatigue/Encumbered/Prone/Guarded modifiers) — only the dialog was broken. (Orphaned `boarding`/`turrets-roll-prompt` templates share the ID pattern but no code references them — dead, ignored. The remaining V1 `Dialog` uses in `fear.mjs`/`endeavours.mjs`/`acquisition.mjs` are GM tools, not sheet rolls — lower priority.) Gate green (300 node tests). (Memory: `project_combat_engine_fixes`.)

**0.8.30** (released 2026-07-01). **Compendium data-QA batch 2 — full pack sweep with the programmatic diff tools: 108 availability fixes + ship typo.** Went through every remaining pack one by one with `tools/compendium_qa/`. **Big systematic find:** ammo/weapon-mods/tools/consumables still carried the schema-default `availability: common` on nearly every item — the 0.7.8 audit corrected this for *cybernetics* but never these four packs. Built a **generalized `availability_diff.py`** (matches any pack's `name`+`availability` to any RT-DOCS source table row where a cell is an Availability tier, across all books) and corrected **108 values** to canonical (ammo 8, weapon-mods 18, tools 59, consumables 23) — tool extraction spot-verified against the actual source rows (Suspensors Extremely Rare, Recaf Abundant, Ration Packs Ubiquitous), all four re-diff to 0 mismatches. Also: **cybernetics** availability confirmed clean (30/30, `cybernetics_diff.py`); **ship-weapons** stats clean (17/17, strength/crit/damage-bonus); **ship-components** `Auger`→`Augur` sensor-name typo fixed (source: Battlefleet Koronus; not name-referenced, unlike the cybernetic "Auger Arrays" which is load-bearing in chargen grants + left, logged to data-vendor-queue); **attack-specials** DH2-leftover-free (26 curated qualities). **Prose packs** (traits 140, ship-traits 34) aren't value-diffable and stay as-is (traits has its own 0.8.0 coverage ratchet). NPC packs were data-cleaned separately in 0.8.3 via `tools/npc_pipeline/`. Gate green (300 tests). Compendium data QA is now swept end-to-end; the `tools/compendium_qa/` diffs (weapons/armour/psychic/cybernetics/availability) are reusable for any future re-check. (Memory: `project_combat_engine_fixes`.)

**0.8.29** (released 2026-07-01). **Compendium DATA-QA pass — programmatic pack-vs-source diff tooling + 5 verified weapon fixes.** After trying compendium data QA three ways and finding the LLM approaches unreliable for it, built the RIGHT instrument: deterministic **`tools/compendium_qa/{weapons,armour,psychic}_diff.py`** that parse the pack YAML + the RT-DOCS source tables and compare field-by-field (no LLM, no hallucination). **The three-approach lesson:** (1) the Antigravity **loop can't do data** — pointed at `src/packs/` it dry-stopped empty in 2 iters (correctly — its discovery-skim can't systematically diff values); (2) **diff agents hallucinate values** — a weapons agent reported plausible-but-fabricated "current pack values", and when I "independently verified" I **misread the pack's unusual `data:`→`img:`→`name:`→`---` layout** (the data block sits ABOVE its name), wrongly concluded the agent hallucinated, and **corrupted 4 weapons — caught while debugging the tool, reverted**; the agent had been right; (3) the **programmatic tool is the only checkable answer — once validated against known-good entries** (does it get Autogun right?), which is what separated signal from noise. Gotchas the tools encode: data-above-name YAML, generic-pack-name vs patterned-source-row matching, 8-col melee vs 11-col ranged tables, the OCR `S`→`$` RoF artifact, errata overrides, 9 source books. **Results:** weapons **158 matched, 5 real bugs fixed** — Void Speargun held a full boltgun profile (→1d10+2 Rending, pen 2, range 10, S/-/-, clip 1, Storm/Tearing stripped), Kroot Hunting Rifle pen 1→3 + clip 1→8 (the weapon the agent *couldn't* verify — RT-DOCS OCR-blanked it), Sonic Rifle damageType, Assault Gauntlet + Pulse Carbine RoF (remaining 7 diffs all non-bugs: 2 errata-correct + 5 whip melee-reach); **armour 25 matched, 0 mismatches**; **psychic 34 matched, 0 cost/prereq mismatches** (confirming the 0.7.8 / 0.7.23 audits programmatically, not just by agent assertion). Gate green (300 tests). The tools are reusable — future data passes are `python3 …_diff.py`, not an LLM sweep. (Memory: `project_combat_engine_fixes`.)

**0.8.28** (released 2026-07-01). **Fourth Antigravity run (`ralph/agy-qa-4`) aimed at the un-swept ACTOR COMPUTATION PIPELINE — 18 fixes, 18/18 independently reviewed CONFIRM.** First run using the new **`FOCUS` env** (aims discovery at one fresh subsystem — the agy-qa-3 lesson) — pointed at `acolyte.mjs`/`npc.mjs` `prepareData` + the characteristic/unnatural/encumbrance/movement helpers, explicitly OFF the picked-over combat core. Validated the whole tuned kit: it **dry-stopped at iter 34** (2 empty discovery passes, before the 40 cap — the `DRY_LIMIT` working), statuses stayed clean (the `normalize_status` fix held — zero stranding), and the fresh seam yielded **far more than the combat re-sweeps** (17 fresh fixes + the deferred BUG-Q-231). Node tests **252 → 300**. The fixes (BUG-Q-231, 235-253): **recompute-after-AE cluster** (the highest blast-radius — derived stats now re-derive AFTER Active Effects: `_refreshArmourToughnessBonus` so AE-modified Toughness reaches damage soak (245), encumbrance/initiative refresh on AE Str/Tou (246), and an **idempotent source-fold** reading immutable `_source` so repeated `_computeCharacteristics` no longer corrupts `wounds.max`/`unnatural` (251) — locked by `idempotent_source_fold.test.mjs`); **Unnatural-characteristic** (live NPC scaling not pre-baked static (237), and the multiplier feeds Opposed-Test DoS per verbatim RT Core p.368 (238), Lightning Reflexes reads baked Unnatural Agility on NPCs (249)); **movement/encumbrance** (Lift/Push use canon Table 9-33 not ×2/×4 — all 63 values verified (235), Quadruped × size ordering (241), backpack contents count toward encumbrance (242), Flyer/Hoverer/Crawler movement-replacing traits (253), size-missing defensive default (244)); **reactions** (Prone −20 Dodge/−10 Parry + Fatigue/Encumbered now reach `rollReaction`'s fast path (236/247) — verified single-application, no double-count with `rollSkill`); plus Best-craft armour won on an equal-base-AP tie (239), a real async-`prepareData` stale-data race (243), and wound-recovery per p.262 (250). **Review model:** 5 parallel canon reviewers (one per cluster) → **18/18 CONFIRM** against verbatim RT-DOCS (incl. the flagged-high-risk 238, which turned out to be literal RAW, not a fabrication like 0.8.26's Daemonic); the AE-ordering cluster specifically cleared for double-count/source-mutation risk (`_source` read-only, assignment-only writes); AE-ordering + idempotency + Prone-reactions **live-verified on rt-smoke**. This run's fix+verify held up completely. **Lesson: aim each run at fresh surface via `FOCUS` — the un-swept pipeline out-yielded the combat core several-fold, and the tuned kit (normalize/dry-stop/canon-discipline) worked end-to-end.** (Memory: `project_combat_engine_fixes`.)

**0.8.27** (released 2026-06-30). **Third Antigravity run (`ralph/agy-qa-3`) on fresh surface + a stranded-backlog recovery — 22 fixes + 10 loop-verified confirmed.** The tuned loop (now `MIN_OPEN=1`, `DRY_LIMIT`, canon-discipline + fresh-surface briefs) was aimed off the picked-over combat core and found ~20 real psychic/vehicle/ship/chargen bugs — but a **status-word drift** surfaced: the discovery model wrote `status: new`/`discovery` instead of `open` on **26 findings** (across this run AND, retroactively, 0.8.26), stranding them invisibly (the fixer only matches `open`) AND masking the dry-stop (so it ran the full 40). **Hardened** with `normalize_status()` (any non-standard status → `open`) + a brief requiring exactly `- status: open`. Then **triaged all 26 stranded findings** with 5 parallel canon reviewers → **22 REAL, 2 dup, 2 wrong-premise**; the reviewers confirmed the bug-hunter's *code reads* are reliable but its *canon citations* are often DH2/wrong-page, so every fix was **scoped to the RT-1e-correct slice**. Fixes (BUG-Q-183…226): **3 P0s** — Reliable jam→hit (96-99 wrongly stayed a HIT; now auto-miss + jam only on a 1d10 of 10 via a new `reliable-jam`; the Best-craft path had the same latent bug), Overheats no longer leaves an overheating shot a hit, voidship salvo flags only ONE Critical (was one per hit → crit chart rolled N times); **P1** ship range no longer double-dips (canvas ±20 dropped, Strategic-Round ±10 is sole source); and P2-P3 Flame jam-on-natural-9, Flexible can't-Parry, Prone −10 WS (melee-only, no BS) + −20 Dodge (dropped the DH2 Sturdy clause), Shocking quality added (was non-RT Concussive) + Tempest ammo `Shock`→`Shocking`, per-DoS dice feed Righteous Fury, cover doubled vs Primitive + penetration layered (was double-counted), Tearing `kh`-guard prefix-match, Gunslinger two-pistol penalty, Marksman + Deadeye Shot (inert talents wired), grapple push uses attacker DoS, ship half-Speed rounds up, vehicle-ram facing parameterised, per-DoS floored at 0 DoS, removed DH2 Deathdealer dead code + Vengeful quality + bogus `hasLevel` flags. Separately, **independently canon-reviewed the loop's own 10 verified findings** (211/212/213/214/215/218/220/221/227/229 — phenomena-while-sustaining, doubles-on-100, single-sustain −1 PR, Renegade Unfettered +5/Psy-Rating, Astropath Perils mitigation, vehicle crit Crack/Crippling, Destructive voidship, opposed tie-break + Knock-Down, Helpless melee auto-hit, chargen 5000 XP): **10/10 CONFIRM** against verbatim RT-DOCS — this run's fix+verify held up (unlike 0.8.26's Daemonic). 252 node tests; Prone/cover live-verified on rt-smoke. One review-found follow-up filed (BUG-Q-231, sustaining +10 should scale per additional power). **Lesson: the loop's *findings* are gold but its *bookkeeping* (status words) and *citations* need a normalization + independent-triage gate.** (Memory: `project_combat_engine_fixes`.)

**0.8.26** (released 2026-06-30). **Cross-model bug pass, part 3 — a second Antigravity run + a thorough independent canon review (6 parallel reviewers).** A fresh `ralph/agy-qa-2` run of the Antigravity loop (Gemini finds + verifies, Claude fixes) landed **7 engine fixes** (BUG-Q-179…196), then a deliberate **pre-merge review** fanned out one canon reviewer per verified fix against RT-DOCS. **5/6 confirmed correct:** BUG-Q-179 Flame weapons no longer roll a ghost BS test that could jam/clobber DoS (p.142 "does not need to Test Ballistic Skill"); BUG-Q-181 auto-fire jam threshold **94+** (Semi/Full/Suppressing) vs single-shot 96+ (canon :1988/:2147); BUG-Q-188 Lightning Reflexes scales its Initiative multiplier with Unnatural Agility (p.110 "+1 to the multiplier"); BUG-Q-194 Accurate/Maximal bonus **dice** now feed Righteous Fury + Tearing/Proven (rolled before the RF scan, kept out of `rollFormula` to avoid double-count — RF fires on "any damage die"); BUG-Q-195 Felling ignores **levels of Unnatural Toughness** at soak (not base TB), composing with Daemonic. **The review's key catch — BUG-Q-196 Daemonic — was reverted to RAW:** the loop made Daemonic stack *additively* with Unnatural Toughness (UT×2+Daemonic=×3), but the verbatim trait (p.364 "double their Toughness Bonus against all damage") is a **distinct ×2**, not an Unnatural step, so UT×2+Daemonic = **×4** (the additive reading is a Black Crusade house rule the commit admitted citing) — both Claude's fix AND Gemini's verify had agreed on the wrong reading; only an independent verbatim pass caught it. Also resolved the **disputed BUG-Q-182** (the verify correctly flagged it incomplete): grenade/missile **launchers** (isRanged) now use the barrel-detonate rule (p.126 — 1d10, on 10 detonates in the barrel + weapon destroyed), not the standard jam. And cleared the **3 capped-out open P0s**: BUG-Q-208 vehicle facing Armour **doubled vs Primitive** weapons (p.142, matching personal combat); BUG-Q-209 melee-Lance penetration guarded with `max(0, dos−1)` so a bare success (0 DoS) no longer applies `pen×−1` and zeroes Penetration; BUG-Q-210 an **engine-level** `calculateHits()` guard so a ranged ammo weapon with `fireRate≤0` (insufficient ammo) produces NO hits (p.115), closing a free-shot path that bypassed the dialog guard. 225 node tests; all five behavioural fixes **live-verified on rt-smoke** (Daemonic ×4 incl. Felling composition, vehicle Primitive-doubling, ammo no-hits, launcher routing). The review validated the cross-model loop's safety model: independent verification catches fixes that *both* the fixer and same-pass verifier rubber-stamp. (Memory: `project_combat_engine_fixes`.)

**0.8.25** (released 2026-06-30). **Cross-model bug pass, part 2 — the 4 open findings triaged against authoritative canon (NotebookLM).** The Antigravity loop left 4 unfixed findings (BUG-Q-175…178) whose cited rules were partly hallucinated; resolved them against the **full RT corebook via NotebookLM** (the local RT-DOCS markdown has an incomplete weapon-qualities glossary). **BUG-Q-176 Melta:** removed the short-range Penetration-DOUBLING — "Melta" is not a Weapon Special Quality in RT 1e (RT Core p.122), just a weapon group with high base Pen; the doubling was a DH2/Only War import. **BUG-Q-177 Maximal:** removed the +2 Penetration — RT Maximal (plasma firing mode, p.123) grants +10m range, +1d10 Damage, Recharge, +2 Blast, but NO Pen bonus (the +1d10 is kept). **BUG-Q-178 Scatter:** rewrote to the real rule (p.116) — removed the DH2 flat +3/−3 damage + flat +10 to-hit, added +1 HIT per two DoS at Point Blank and a doubled-Armour note at Long/Extreme (the finding's *own* cited rule was also wrong). **BUG-Q-175** (Toxic/Snare/Shocking resist) = wontfix (re-files QA-019, by-design `[Apply]` buttons). Lesson: the loop's discovery flags the right *quality* even when its *cited rule* is wrong — human + authoritative-canon triage is the safety gate. Antigravity queue now **0 open · 17 verified · 1 wontfix**. 216 node tests; live-verified. (Memory: `project_combat_engine_fixes`.)

**0.8.24** (released 2026-06-30). **Cross-model bug pass — 14 correctness fixes found by Antigravity (Gemini), fixed by Claude, cross-verified.** First run of the new `loops/antigravity-qa/` kit: `agy` (Google Antigravity / Gemini 3.1 Pro) hunts bugs + independently verifies fixes; `claude` (Opus) fixes them; findings flow through `.ralph/bug-queue.md`. 18 findings filed (BUG-Q-161…178), **14 fixed + cross-verified, 0 disputed, 0 red gates** (4 open left for triage). A different model surfaced a cluster of DH2-leftovers + real bugs the single-model QA audit missed, all live-verified + node-tested (216 tests, +10): **True Grit** halves Critical Damage (was −TB, DH2); **Accurate** extra damage is Basic-class only (RT p.143); **Mighty Shot/Crushing Blow** flat +2 (was DH2 half-bonus); **Twin-Linked** +20 to-hit + a flat +1 hit on 2+ DoS (was reducing hits); **Razor Sharp** never fired — the code matched the literal typo `Razer Sharp`; **Compact** mod docks Damage not Penetration; **Unbalanced** parry is −10 not a block (only **Unwieldy** blocks); **Righteous Fury** extra die keeps Tearing + Proven; **Suppressing Fire** fireRate = full RoF; **Maximal**/burst-fire additional-hit caps; voidship hit uses a standard RT test (dropped the homebrew Flawless/Flawed scale), boarding/turret dead-code removed, and **Lances no longer bypass Void Shields** (they ignore Armour but not shields — RT p.216; a 0.7.11 regression). Five independent canon spot-checks all confirmed correct. The kit (`run_agy_qa.sh` driver + the three model briefs + `specs/08`) is committed for re-runs. (Memory: `project_combat_engine_fixes`.)

**0.8.23** (released 2026-06-29). **QA-audit backlog — Endeavours full-canon model + Misfortune (the LAST open QA item).** The Endeavour tracker was a flat record with a GM-typed PF reward and no downside. Reworked to RT Core Ch.XII: new `src/module/rules/endeavours-helpers.mjs` (pure, node-tested — extracted because `endeavours.mjs`'s setting import pulls Foundry globals) with `endeavourSizeReward` (Lesser +1 / Greater +3 / Grand +5), `endeavourTotals`, `endeavourCompletionPF` (size reward + 1 per full 100 AP over target), and `misfortuneResult` (Table 9-41). The dialog now sizes each Endeavour (auto-setting its PF reward), nests **Objectives** (themed Exploration/Trade/Military/Criminal/Creed, each with its own AP/target that roll up), awards the excess-AP bonus on completion, and has a **Roll Misfortune (Table 9-41)** button (1d100 → Nuisance −1 / Grim −2 / Calamitous −1d5 PF, recoverable). **QA-086 (Renown)** confirmed not-a-bug (RT 1e has no Renown). With this the **160-finding QA audit is fully worked down** — the open remainder is documented interpretation notes in `.ralph/data-vendor-queue.md` for RT-rules review, not engine gaps. 206 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.22** (released 2026-06-29). **QA-audit backlog — ship/vehicle combat follow-ups (the last small ones).** **Range-band auto-inject:** the ship VU range-band BS modifier now reaches the to-hit (shipShootingCheck returns it → `performShipWeaponAttack` → `WeaponRollData.shipRangeModifier` → `modifiers['range band']`, re-applied each `update()`), instead of only a chat note. **Ship Ram** (RT Core p.219): hull-size dice (1d5 transport/raider, 1d10 frigate, 2d5 light cruiser, 2d10 cruiser) + prow Armour ignoring void shields, self damage = target Armour + 1d5, spends the Shooting Action, surfaces the Hard (−20) Pilot test. **Ship Come About** (Adjust Speed & Bearing) + **Disengage** resolve as Manoeuvre Actions enforcing the economy + the canon test (Disengage also forfeits firing). **Vehicle Dodge Reaction** (ItS): gated on the vehicle having moved ≥ tactical speed; rolls the driver's Drive (+ Manoeuvrability), avoids 1 + DoS shots, fail-by-5 crash + size penalty noted. `game.rt.shipRam/shipComeAbout/shipDisengage/vehicleDodge` + voidship movement-panel buttons. 202 node tests; live-verified on rt-smoke. With these, the QA audit's combat layer is complete except Endeavours. (Memory: `project_combat_engine_fixes`.)

**0.8.21** (released 2026-06-29). **QA-audit backlog — vehicle-combat follow-ups (move-then-shoot + Ram).** Two QA-117 leftovers unblocked by the movement-tracking pattern from the Strategic-Round work. **Move-then-shoot:** new `system.combat.movedThisTurn` on vehicles (set via the Operate Vehicle dialog's "Movement this turn" selector); a ranged weapon attack reads the operator's active vehicle (QA-116 `activeVehicleId`) and applies −10 (moved tactical) / −20 (twice tactical) in `WeaponRollData.update()`. **Ram:** `vehicleRam()` deals damage = the rammer's striking-facing AP + 1d10 (2d10 at twice tactical) + pushes the target 1m/point; ramming another vehicle damages the rammer by the target AP + 1d5; `game.rt.vehicleRam()` opens a dialog. 202 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.20** (released 2026-06-29). **QA-audit backlog — Strategic Round / VU void-ship combat (QA-047, abstract tracker).** Space combat previously had only isolated ship attack/crew rolls. New `src/module/rules/ship-combat.mjs` adds the RT Strategic-Round structure (RT Core pp.213-220): node-tested pure helpers (`shipInitiativeBonus` = tens digit of Detection, `shipManoeuvreDistance` full/half Speed, `shipMaxTurn` 90°/45° by hull, `shipRangeBand` Short +10 / Long −10 / out, `firingArcAllows` prow=fore / beam own arc / dorsal+keel any-but-aft, `shipShootingResolution`); `rollShipInitiative()` (1d10 + Detection bonus per ship in the Combat); a per-ship action economy (`system.combat.strategicTurn` — one Manoeuvre + one Shooting per Strategic Turn, reset by the combat-turn hook); `shipManoeuvre()` (move full/half Speed VU, turn to hull max, record VU moved); and `shipShootingCheck()` (on firing, prompts VU distance + target arc, aborts an out-of-arc/range shot, spends the Shooting Action, surfaces the range-band BS modifier). `game.rt.shipInitiative()`/`shipManoeuvre()` + Strategic Initiative / Manoeuvre buttons on the voidship movement panel. Positioning stays on the GM's canvas (1 grid square = 1 VU per canon) — the system reads VU distance + arc as inputs, keeping it fully testable. Authored range-band thresholds + firing arcs + the follow-ups (Ram/Disengage, auto-applying the modifier to the to-hit) logged to the data-vendor-queue. The chosen approach (abstract tracker over canvas geometry) was a user scoping decision. 202 node tests (+6 ship-combat); live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.19** (released 2026-06-29). **QA-audit backlog — Drug timed-effect subsystem (QA-143/144/145).** Drugs were note-only; now `src/module/rules/drugs.mjs` makes them mechanical: `useDrug()` applies the benefit (AE + optional granted Talent embedded from the pack for the duration + Attention Spanner's focus-Test branch), rolls the duration, and records an active-drug entry. **QA-143** Frenzon now grants Frenzy + Fear immunity, Cold Fire grants Battle Rage (so Frenzy's conditional bonuses actually fire while active). **QA-144** comedowns fire on wear-off — Stimm −20 STR/T/AG (1h), Spur −20 T/AG (1h), Wideawake +1 Fatigue, Slaught a GM note (Test-gated); Attention Spanner rolls 1d100 vs WP → +30 Int / −20 all. **QA-145** self-expiry — because `duration.seconds` AEs don't tick out of combat, `expireDrug` runs from the combat-round hook, the `updateWorldTime` hook, or a "Wears off now" chat button (removing benefit + granted talent, applying the comedown). Foundry gotcha: `getFlag/setFlag` validates the scope, so the active-drug list uses the system scope (`rogue-trader-3rd`), not the raw `flags.rt` object key the rest of the code embeds directly. 196 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.18** (released 2026-06-29). **QA-audit backlog — Vehicle combat cluster (QA-115/116/117).** **QA-115 (P1 crash)** attacking a vehicle routed through the personal-creature damage path — it read `system.armour` (undefined → `Object.entries(undefined)` THREW on any creature hit location), never read the front/side/rear facing armour, and wrote `wounds` not `integrity`. New vehicle branch in `AssignDamageData`: `update()` guards vehicles out of the creature armour loop (crash fix) and reads the selected facing's AP; `finalize()` reduces the hit by facing AP − Penetration (no Toughness Bonus, per ItS "Armour works in the same manner as an individual's") → Structural Integrity, overflow → `integrity.critical`; writeback hits integrity. The assign-damage prompt gains a Front/Side/Rear facing dropdown; the chat card shows the facing + integrity. **QA-117** new `src/module/rules/vehicle-critical-damage.mjs` — the ItS Table 5-2 Vehicle Critical Hit Chart (cumulative: Jarring Blow → Staggered → Weapon Destroyed → Drive Damaged → Penetrating Hit → Fire → Destroyed → Explodes), drawn on integrity overflow (the producer `integrity.critical` lacked); plus the five ItS vehicle Combat Actions (Tactical Manoeuvring / Evasive / Floor it! / Ram / Manoeuvre) in the catalog (tagged `vehicle:true`). **QA-116** a vehicle's Manoeuvrability now modifies the operator's Drive/Pilot Tests — new `system.combat.activeVehicleId` + an "Operate Vehicle" control. Deeper movement automation (move-then-shoot penalty, Ram auto-resolution, vehicle Dodge, crash-on-fail) stays GM-adjudicated (needs the absent Strategic-Round movement layer). 196 node tests; live-verified on rt-smoke (no crash; front AP 20/rear 10; overflow → cumulative crit; manoeuvrability ±applies). (Memory: `project_combat_engine_fixes`.)

**0.8.17** (released 2026-06-29). **QA-audit backlog — Cover subsystem (RT Core p.245).** **QA-111** cover was entirely unmodelled; every shot at a target behind cover hit at full damage. Now: new `system.combat.cover.ap` field; `AssignDamageData.finalize()` interposes the cover's AP for hits to a concealed location (Body / Left Leg / Right Leg — the canon "firing around/over cover conceals body + legs" general rule), Penetration reduces the cover's effective AP (canon: "it does affect cover"), only the excess reaches the target (resolved against its own armour + TB as normal), and the cover's AP erodes by 1 per hit that gets excess through (persisted). "Take Cover" sheet header control + `game.rt.takeCover()` opens a Table 9-7 preset dialog (4/8/12/16/32 AP or custom); the assign-damage chat card shows what cover absorbed + its new AP. Reproduces the printed Titus/sandbags worked example exactly (AP 8 → 7 → 6 across hits 8/11/8). The Penetration-on-second-layer interpretation is logged to the data-vendor-queue for RT-rules review. 196 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.16** (released 2026-06-29). **QA-audit backlog — straggler sweep (Power Field parry, phenomena Push modifier, Navigator immunity, Force Shards per-DoS).** **QA-017** a successful Parry with a Power-Field weapon resolves the 75% chance to destroy the attacker's weapon (`rollReaction` posts a chat note; parry result unchanged). **QA-039** the Psychic Phenomena roll is now modified by Push (RT Core p.157 Table 6-1: +5/rating Sanctioned, +10/rating Renegade, +10 Sustaining) — `drawFromTable` takes a modifier and rolls `1d100+mod`. **QA-041** Navigators no longer roll Phenomena/Perils (RT Core p.180; `checkForPerils` skips Navigator-discipline powers) — their Novice/Adept/Master tier scaling stays a data follow-up. **Force Shards** re-tagged Psychic Bolt→Psychic Storm so it scores one hit per Degree of Success (reviving the dead `isPsychicStorm` per-DoS branch). **QA-019** (target-side resist tests) confirmed BY DESIGN — the resist roll stays an `[Apply: X]` button per the apply-from-outcomes rule. Remaining per-power scalers (Navigator tiers, Terrify per-rating, Mind Scan tiered) logged to the data-vendor-queue. The open backlog is now down to the design-heavy subsystems (Strategic Round/VU, Endeavours, vehicle/cover combat, drug timers). 196 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.15** (released 2026-06-29). **QA-audit backlog — Force weapons.** **QA-014** a psyker (psy.rating > 0) wielding a weapon with the Force quality now adds their Psy Rating to both Damage and Penetration, and the damage type becomes Energy (ItS). Wired across the three damage-pipeline stages with correct ordering — damage modifier in `_calculateDamage` and penetration modifier in `_calculatePenetration` (both BEFORE their `_total*` sums), Energy type in `_calculateSpecials`; non-psyker / non-Force unaffected. The optional Focus-Power +1d10/DoS bonus (ignoring armour/TB) is left as a manual follow-up. 196 node tests; live-verified on rt-smoke. **Remaining open QA items (≈40) are the design-heavy subsystems + a few stragglers** — Strategic Round / VU map, Endeavours/Exploration/Renown, vehicle + cover combat (QA-111/115/116/117), drug/effect timers (QA-143/144/145), remaining ship-structure items, plus weapon/psychic stragglers QA-017 (Power Field destroys weapon on Parry), QA-039 (psychic phenomena roll modifiers), QA-041 (Navigator power tiers), QA-019 (auto-resist test — intentionally a button per the apply-from-outcomes design rule), and per-power per-rating/tiered psychic scaling (Terrify/Mind Scan/Force Shards). Tracked in `QA_FINDINGS.md`. (Memory: `project_combat_engine_fixes`.)

**0.8.14** (released 2026-06-28). **QA-audit backlog — psychic per-DoS damage scaling.** **QA-040** new `perDoSDamage` Roll-formula field (on the shared `damage` template) is rolled once per Degree of Success (min 1 on a success) and added to the damage — so "1d10 per Degree of Success" reaches the roll instead of the description; Banishment wired to `perDoSDamage: '1d10'` (ItS variant). Also fixed a latent crash: an empty-damage power produced `new Roll(0)` (Foundry v13 rejects a non-string formula) → `'0'`. Remaining per-DoS/per-rating strands (Terrify per-rating effect, Mind Scan tiered success, Force Shards tag) logged to the data-vendor-queue for per-power review. 196 node tests; live-verified on rt-smoke (damage scales 1d10/3d10/5d10 at DoS 1/3/5). (Memory: `project_combat_engine_fixes`.)

**0.8.13** (released 2026-06-28). **QA-audit backlog — grapple + two-weapon fighting.** **QA-122** new `src/module/rules/grapple.mjs` — an Opposed Strength Test resolver for the core grappling options (Grab/start, Damage Opponent 1d5-3+SB Primitive + Fatigue, Throw Down→Prone, Push, Break Free), a new **Grappled** condition (token HUD + `actor.statuses`), `game.rt.grapple()` + a "Grapple" sheet header control that targets the selected opponent and prompts for the option (maintain/assistance/Take-Control/Slip-Free remain GM-adjudicated). **QA-119** two-weapon fighting — a Multiple Attacks action by a Two-Weapon Wielder takes −20 per attack (one roll per hand), −10 with Ambidextrous, via `calculateCombatActionModifier`. 196 node tests; live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.12** (released 2026-06-28). **QA-audit backlog — talent/trait/cybernetic conditionals.** **QA-034** Sharpshooter cancels the −20 for Called Shots, Takedown the −20 for the Stun Action. **QA-130** the unbraced-Heavy −30 is cancelled by the Auto-Stabilised trait, the Bulging Biceps talent, or an equipped Suspensors mod. **QA-035** Polyglot lets Speak Language be attempted untrained at half-Characteristic −10. **QA-027** the Synthetic Muscle Grafts cybernetic adds +1 to the Strength Bonus (name-based, like Unnatural — avoids the AE-timing + "+1 SB without +10 raw" issues). **QA-028** the Blackbone Bracing cybernetic adds +2 Damage to unarmed attacks. 196 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.11** (released 2026-06-28). **QA-audit backlog — Recharge, aptitude removal, talent/trait conditionals.** **QA-016** Recharge weapons can only fire every other Round — new `system.recharging`/`rechargedRound` fields; firing a Recharge weapon stamps it, `prepareWeaponRoll` blocks the dialog while recharging (like the jam gate), and the combat-turn hook clears it once two Rounds have passed (fire N → recharge N+1 → fire N+2). **QA-004** removed the DH2 aptitude system entirely (the `aptitude` Item type, the acolyte `system.aptitudes` field + `get aptitudes()`, `get isAptitude()`, the rendered Aptitudes panel + its preload/template) — no aptitude items exist in any pack. **QA-034** penalty-negating talents now apply — Sharpshooter cancels the −20 for Called Shots, Takedown the −20 for the Stun Action. **QA-130** the unbraced-Heavy −30 (QA-128) is cancelled by the Auto-Stabilised trait, the Bulging Biceps talent, or an equipped Suspensors mod. **QA-035** Polyglot lets Speak Language be attempted untrained at half-Characteristic −10. 196 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.10** (released 2026-06-28). **QA-audit backlog — combat features (helpless coup-de-grace, Fate, shooting-into-melee, RT crit tables).** **QA-157** a Helpless target (RT Core p.244) now triggers a coup de grace — `_calculateDamage` rolls the weapon's damage TWICE and sums it, and two natural 10s across the rolls make the first Righteous Fury automatic (no confirm roll). **QA-090/091** Fate Points: `actor.spendFate(kind)` + a Spend-Fate row on the Fate panel (Re-roll/+10/+1 DoS/Act-First post a chat note; Heal 1d5 and Un-tire auto-apply), and `actor.burnFate()` + a Burn button (permanently −1 max Fate to cheat death); removed a pre-existing duplicate `spendFate()` that shadowed the new one. **QA-156** Shooting into Melee: a ranged shot at a target with an adjacent opposite-disposition token takes −20 BS (waived if any engaged character is Stunned/Helpless/Unaware), auto-detected from the canvas via the node-tested `shootingIntoMeleePenalty()`. **QA-073** the personal Critical Effect tables (RT Core Tables 9-11..9-26) were re-authored from Dark Heresy 2e to RT 1e RAW — all 160 entries (4 damage types × 4 locations × 10 severities) extracted by four parallel subagents from RT-DOCS and **cross-verified against the corebook PDF via NotebookLM** (5 spot-checks matched, incl. the book's own printed "noise" typo, kept faithful); logged to the data-vendor-queue for review. New node-tested helpers `woundDamageState`/`woundRecovery` (from 0.8.8) plus `shootingIntoMeleePenalty`; 192 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.9** (released 2026-06-28). **QA-audit backlog — Haywire/Unstable + weapon-mod completion.** **QA-112** the Haywire Grenade now carries `special: {blast: 2, haywire: true}` and its chat-effect handler was rewritten to RT/ItS text (electronics offline 1d5 rounds + auto-crit on vehicle/ship), replacing the DH2 "Haywire Field at strength" wording with its negative `level × −10` radius; **QA-015** the Unstable quality was added to the `attackSpecials()` authoring catalog (was pack-only) + a rule note (per-hit 1d10: 1=half, 10=double — the damage-pipeline variance itself deferred). **Weapon-mod mechanism completion:** discovered the mod-application engine (`weapon-modifiers.mjs`) already exists and is fully wired into the attack/damage/special pipeline (only ~6 of 30 mods were filled in), and completed the high-value ones — **QA-066** Overcharge Pack grants the Overcharge quality so the existing +1-Damage handler fires (RT Core p.135), Tox Dispenser grants Toxic, and **QA-018** the Customised quality halves reload time rounding UP (RT Core p.143, new `roundUp` arg on `rapidReloadTime`, composed with Rapid Reload). Live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.8** (released 2026-06-28). **QA-audit backlog — feature wave (wound recovery, weapon jams, unusual ammo).** Building the self-contained RT subsystems flagged by the audit: **QA-092/093** the character's RT Damage state (Healthy / Lightly / Heavily / Critically Damaged) is derived each `prepareData` (from Damage taken vs 2×TB, with any Critical Damage forcing Critically) and shown on the Wounds panel, plus a natural-healing path — `actor.applyRest('day'|'week')` and "Rest (Day)/(Week)" buttons that recover Wounds per RT Core p.262 (Lightly = TB/bed-rest-day; Heavily = 1/day or TB/complete-rest-week; Critically = 1 Critical/week); **QA-105** weapon jams now PERSIST — a new `system.jammed` field is set on a jam, `prepareWeaponRoll` blocks the attack dialog while jammed, and a "Clear Jam" weapon-sheet control rolls the owner's Ballistic Skill (Full Action) to clear it (RT Core p.238); **QA-064/065** wired the unusual-ammunition modifiers (Tracer Shells +5 BS Full Auto, Microburst Flask −2 Dmg/+2 Pen, Nephium Fuel Tank +2 Dmg, Organgrinder Rounds Toughness-test effect). New node-tested helpers `woundDamageState` / `woundRecovery`; 194 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.7** (released 2026-06-28). **QA-audit backlog — full re-triage + correctness wave (XP breakdown, force-field/crit/movement/psychic fixes).** A 5-agent parallel re-triage of all 160 QA findings against the *current* code established that ~50 were already fixed in 0.8.0–0.8.6 (the acquisition Table 9-35, the whole ship-combat cluster, NPC data, the psychology layer, most skill/characteristic fixes) but never marked. Of the genuinely-open remainder this release clears: **QA-049** the Experience panel now shows the engine's Calculated Spend + per-category breakdown (and fixed a latent NaN — talent/power cost read `item.cost`, no getter, now `item.system.cost` with `||0` guards); **QA-051** dropped the dead faction/subfaction fields (schema + 6 getters); **QA-102/103** force-field finalize no longer clobbers the item with an un-awaited update or re-fetches the actor via `game.actors.get(_id)` (BUG-005 class); **QA-154** lances score 1 hit + 1 per **3** DoS (capped at Strength), not the macro 1+DoS; **QA-151** psychic casting uses the sustained-reduced `currentRating`; **QA-137** Proven sums across dice (`+=`); **QA-140** removed a dead Hammer Blow→Concussive block (neither exists in RT packs); **QA-072** Knock-Down action text (dropped bogus +10-on-Charge, added "armour counts double"); **QA-074** crit lookup strips "Right/Left " so limb crits resolve to the Arm/Leg tables; **QA-141** movement uses the UNMODIFIED Agility Bonus (Unnatural Agility no longer speeds you up; Unnatural Speed still does); **QA-009** renamed the DH2 startup log strings + removed a stray per-update debug log; **QA-139** `_getAttackSpecials` warns on an unwired weapon-special flag; **QA-029** Augmented Senses cybernetic grants Heightened Senses. 192 node tests; all live-verified on rt-smoke. **Remaining open ≈48** — mostly unbuilt RT subsystems (Strategic Round/VU, Endeavours, Fate spend/burn, wound recovery, personal RT crit tables, drug timers, vehicle/cover combat, grapple, two-weapon, psychic per-DoS scaling) tracked in `QA_FINDINGS.md`. (Memory: `project_combat_engine_fixes`.)

**0.8.6** (released 2026-06-27). **QA-audit P2/P3 backlog — DH2-leftover cleanup + more correctness fixes (batches F–H + the background-effects pipeline removal).** Continues the 0.8.5 sweep. **(1) DH2 background-effects pipeline REMOVED (QA-001/002/004 partial):** the Origin Path (0.6.0) replaced DH2's Home World × Background × Role × Divination creation, but the old pipeline still ran in the live acolyte `prepareData()` and was a known landmine (writing `bio.homeWorld` would inject DH2 bonus abilities + characteristic flags). Deleted `src/module/rules/{homeworlds,backgrounds,roles,divinations,elite-advances}.mjs`, the `backgroundEffects` getter/init/`_computeBackgroundFields()` + the homeworld `has_bonus/has_negative` flag block in acolyte.mjs, the dead "roll Wounds/Fate from homeworld" prompt on both sheets, the `DarkHeresy.bio` DH2 name-list entries in config.mjs, and the unused `system.backgroundEffects` schema field (−143 lines + 5 files). Characteristics now compute purely from base/advance/modifier. **(2) DH2 schema/dead-code cleanup:** removed `threatLevel` (QA-007, schema + getters), armour `maxAgility` (QA-022, schema + sheet input), the DH2/Only War per-shot "Las Firing Mode" Standard/Overcharge/Overload chain (QA-135 — the Overcharge Pack weapon-mod's +1 Damage still works), the dead `bonuses-panel.hbs` + `bonusVocalize` machinery (QA-062), the DH2 Tier/Aptitudes inputs on the talent sheet (QA-005), the dead `insanityBonus/corruptionBonus` derived fields (QA-050), and the dead `calculateAimBonus`/`ignoreModifiers` (QA-088/123). **(3) Correctness:** Lift/Push weights derived per RT Table 9-33 (QA-079, shown on the encumbrance panel); OCR footnote daggers stripped from NPC packs (QA-061). Confirmed NOT-A-BUG: QA-129 (unbraced Heavy is just −30 BS, no auto-fire ban). 191 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.5** (released 2026-06-26). **QA-audit P2/P3 backlog — small correctness + text fixes (17 findings).** A sweep of low-severity personal-combat / rules-text corrections, each verified against RT-DOCS and live-checked where behavioural: **QA-109** Target Size to-hit modifier is RANGED-only (RT Core p.247; melee unaffected); **QA-076** movement has a minimum Half Move of 1m (no 0/negative for small/low-Ag actors); **QA-097** Point Blank Range is ≤2m (was ≤3m); **QA-089** the Test Difficulty dropdown matches the RT 1e ladder (Easy +30 … Very Hard −30; dropped the DH2/Only War Simple +40 / Arduous −40 tiers and relabelled −30 "Very Hard"); **QA-106** opposed-test ties break by the higher Characteristic, not auto-to-defender; **QA-152** effective Psy Rating floored at 0; **QA-069** the Manoeuvre action gained the `Attack` subtype so it appears in the melee dropdown; **QA-070** restored Tactical Advance (RT canon, was wrongly dropped as DH2-only); **QA-020** Parry help-text "Defensive +15" (was +10); **QA-125/126/127** Shocking / Toxic / Snare chat-card effect text rewritten to RT 1e (Snare now applies Helpless, Shocking Stunned via the Apply buttons); **QA-072/108** Knock-Down bonus damage notes "armour counting as double"; **QA-088** removed the dead `calculateAimBonus` no-op; **QA-123** removed the dead `ignoreModifiers` field; **QA-061** stripped 16 OCR footnote daggers (†) from NPC name/gear fields. Confirmed NOT-A-BUG: QA-098 (overheats already ≥91), QA-006 (charm clean), QA-096 (debug-gated logs), QA-120 (Two-Weapon Wielder Melee already correct — the finding misread the YAML's post-data name layout). 191 node tests. (Memory: `project_combat_engine_fixes`.)

**0.8.4** (released 2026-06-26). **Void-ship combat rewritten to RT RAW (QA ship-combat cluster, 7 findings).** The whole voidship attack→damage→defence→boarding pipeline, previously homebrew, now follows RT Core Ch.VIII: **QA-153** ship-weapon to-hit is a SINGLE BS test scoring `1 + DoS` hits capped at Strength (was N independent rolls); **QA-044** a Critical Hit triggers on DoS ≥ the weapon's **Crit Rating** (`critRating` added to the `shipAttack` schema + all 17 ship weapons populated from canon); **QA-043** Criticals draw the canonical `Critical Hits to Starships` RollTable (1d5; 11+ → Catastrophic) via `drawShipCriticalResult()`, retiring the homebrew matrix; **QA-042** Hull damage = the salvo's combined rolled `1d10+X` Damage minus the facing Armour ONCE for macrobatteries / ignoring Armour for lances (`voidshipHullDamage()` helper; crit-that-did-0 → 1 auto point), replacing the fixed 1/2/4 tier constants; **QA-045** void shields no longer drain permanently — full strength applies to every incoming salvo; **QA-147** turrets are a purely DEFENSIVE rating (−10 enemy Hit-and-Run Pilot / +10 own boarding Command), no longer firing bogus offensive percentile rolls; **QA-148** boarding is a single opposed Command Test (+10 per 10 Crew/Hull advantage, +10 per own turret; loser 1d5 Crew + 1d5 Morale or 1 Hull per degree, then a Morale rout check) via `boardingCommandBonus()`. New node-tested helpers `voidshipWeaponHits` / `voidshipHullDamage` / `boardingCommandBonus`; 191 node tests; all live-verified on rt-smoke. (Memory: `project_combat_engine_fixes`.)

**0.8.3** (released 2026-06-26). **NPC compendium data-quality pass (QA-057/058/059).** Across all 20 NPC packs (338 actors): **(1) QA-059b** — 76 OCR-blob weapon items whose name held the inline spec (`vicious beak (Melee; 1d10+4; …) # prose`) parsed into structured fields + names cleaned. **(2) QA-059a** — ~80 real weapon damage profiles source-extracted from the supplement stat blocks (per-book subagent extraction → `tools/npc_pipeline/apply_npc_fixes.py`, minimal-diff JSON patcher). **(3) QA-059 deletion** — 139 junk weapon items removed (OCR prose / cybernetics / gear / clothing / mis-tagged psychic-powers wrongly tagged `type: weapon`), keeping 21 correctly-empty real items (effect grenades, ammo launchers, Snare/web, placeholders) via `tools/npc_pipeline/classify_junk_weapons.py` (whitelist + proper-noun guard, preview-first). Net: empty-`damage` weapons **289 → 21**. **(4) QA-057** — 26/27 missing Wounds extracted (Apex Predator 15, Howling Banshee 15, Warboss Snokgritz 35, …; the 1 left = "Representative", a non-statblock entry). **(5) QA-058** — The Luminary + Plaguebearer characteristic profiles recovered (OCR had placed the unnatural-bonus row above the real values). All 338 actors intact; 533 filled weapons preserved; compilePacks round-trips; live-verified on rt-smoke. Authored/extracted content is logged for RT-rules review. (Memory: `project_combat_engine_fixes`.)

**0.8.2** (released 2026-06-26). **QA-audit P1 backlog batch — combat-resolution correctness.** Seven more P1 fixes, each live-verified on rt-smoke: **QA-121** Stun resolves RAW (WS-gate + defender 1d10 + head-AP doubling for unarmed/Primitive); **QA-078** Encumbered penalties applied (−1 AgB to movement + Initiative, −10 to movement skills); **QA-104** Reload action (weapon-sheet button refills the clip); **QA-071** (partial) All Out Attack forfeits Dodge/Parry + Guarded Attack +10 Dodge/Parry (per-Round `system.combat` flags cleared on the actor's turn; opponent-facing riders deferred); **QA-094** canon Degrees of Success/Failure (band method `floor((target−roll)/10)` — was the DH2 `1+tens-diff`; auto-fire Full=per-DoS / Semi=per-2-DoS re-derived; `getOpposedDegrees` made success-aware since a bare success is now 0 DoS); **QA-113** Dodge/Parry Reactions integrated into the attack card (`rollReaction` spends the budget + honours Stunned/All-Out lockouts; a successful Reaction negates the hit and disables assign-damage). New pure helpers `degreesOfSuccess`/`degreesOfFailure`/`stunDefenceBonus`/`attackerConditionModifier`/`reactionsLocked`. (Memory: `project_combat_engine_fixes`.)

**0.8.1** (released 2026-06-25). **QA-audit "psychology cluster" — condition-layer completion + Pinning + Fear/Shock + Insanity/Corruption degradation.** The follow-up to 0.8.0's condition-layer increment 1, closing the RT psychology/automation gaps from the QA audit. **(1) Condition layer complete (QA-080 inc.2-4 + 2b):** combat outcomes attach `[Apply: X]` buttons that `toggleStatusEffect` on the target (Knock Down→Prone, Stun→Stunned, Flame→On Fire — button-driven because the attacker's client can't write to the target), Critical-Hit effects AUTO-apply (deterministic; `conditionsFromCriticalText` scans the crit prose GM-side), recurring On-Fire/Blood-Loss damage re-keyed onto `actor.statuses` (fixes **QA-095**, the orphaned handler; On Fire self-clears on a WP success), and Stunned/Helpless/Unconscious lock Dodge/Parry Reactions (`reactionsLocked`). **Design rule: test-gated outcomes → button; deterministic outcomes → auto-apply.** **(2) Pinning (QA-082):** Pinned shooter −20 BS (`attackerConditionModifier`); Suppressing Fire/Overwatch surface a "Roll Pinning Test (−X)" button that rolls the target's WP and applies Pinned on failure. **(3) Fear/Shock (QA-081):** `src/module/rules/fear.mjs` — `rollFearTest` (WP at Table 10-3 mod, Insanity auto-immunity), combat fail draws the (previously orphaned) Shock RollTable at +10×DoF, non-combat fail-by-30 → +1d5 Insanity; "Fear Test" sheet header control + `game.rt.fearTest()`. **(4) Insanity/Corruption degradation (QA-083):** `src/module/rules/degradation.mjs` — `preUpdateActor`/`updateActor` hook pair auto-fires a Trauma Test per 10 IP (→ Mental Traumas at +10×DoF), a Malignancy Test per 10 CP (→ Malignancies), a Mutation prompt per 30 CP (→ Mutations), + Disorder notices at IP 40/60/80; **3 new RollTables authored** (Mental Traumas 10-6, Malignancies 10-8, Mutations 14-3 — logged to data-vendor-queue, Table 14-3 OCR-normalised). All live-verified on rt-smoke; 183 node tests. Authored tables remain unreviewed for RT-rules correctness (`.ralph/data-vendor-queue.md`). (Memory: `project_combat_engine_fixes`.)

**0.8.0** (released 2026-06-25). **RT 1e combat-engine correctness overhaul + QA-audit pass + condition layer + Origin-Path backgrounds automation; chargen wizard shelved.** Merges the `ralph/effects-audit`, `ralph/backgrounds`, and `ralph/qa-audit` lines plus the chargen Stage A/B work into one release on `main`. **(1) 13 engine bug-fixes** (repo-root `BUGS.md`, each with root cause / file:line / canon / live-verify): BUG-001 DoF tens-diff (DH2 `1+` dropped — DoF only; DoS left for a combat-math pass), 002 Paranoia +2 Init, 003 Weapon Master +10/+2/+2, 004 Fatigue (TB threshold, −10 all tests), 005 Assign Damage (`armour.total` + wound-write crash), 006 psychic-range prose normaliser, 007 opposed-power prompt card, 008 Righteous Fury RAW, 009/010 item-grants (fresh-actor-ref fix), 011 Focus Power **+5×Psy Rating**, 012 Acquisition **Table 9-35**, 013 Primitive weapons (full dice + defender armour-doubling, not the DH2 die-cap). **(2) QA-audit Ralph loop** (discovery-only, branch `ralph/qa-audit`): catalogued **161 findings** in `QA_FINDINGS.md` (4 P0, 42 P1, 65 P2, 49 P3) across DH2-leftovers + automation gaps; **17 P1s fixed + live-verified** (Commerce ×2/DoS, Daemonic `(TB X)` soak, thrown +SB, Unnatural Speed move, hit-location zero-pad, Push phenomena, Overheats-never-jam, Surprise +30, Unbraced Heavy −30, Flame no-BS cone, class-changing ammo, Weapon Training −20, Overheat self-damage, +Reaction modifier). **(3) Condition layer (QA-080 increment 1)**: RT conditions (Stunned/Prone/Pinned/Blinded/Deafened/On Fire/Blood Loss/Unconscious/Helpless/Unaware) registered in `CONFIG.statusEffects` (clickable token HUD + `actor.statuses`); the to-hit test reads the target's condition (Stunned +20 / Unaware/Helpless +30 / Prone +10 melee −10 ranged) via `src/module/rules/conditions.mjs`. Apply-from-outcomes, recurring On-Fire/Blood-Loss damage, and reaction-lockout are follow-up increments. **(4) Origin-Path backgrounds**: ~95 origin-path stub traits → real compendium items with ActiveEffects (drag-drop, AE auto-applies; resolved at `commit.mjs` by exact name). **(5) Chargen wizard SHELVED** reversibly (`renderActorDirectory` button hook commented; `AcolyteSheetV2.CHARGEN_UI_ENABLED=false`) — Stage A/B code intact. **Open before wider distribution:** authored content (backgrounds + effect wiring) is logged to `.ralph/data-vendor-queue.md` for RT-rules review; the remaining **25 P1s** + ship-combat cluster + condition-layer increments 2-4 are tracked in `BUGS.md` / `QA_FINDINGS.md`. (Memory: `project_combat_engine_fixes`.)

**0.7.32** (released 2026-06-09). **SCSS cleanup — fix doubled-wrapper bug in table grid rules.** `src/scss/components/_grids.scss:178` and `:280` used `&--settingstoggle + #{&}--grouped` — the `#{&}` interpolation embeds the full `.rt-wrapper .rt-table--X .table-cell` parent chain into the middle of the selector, compiling to `.rt-wrapper .rt-table--X .table-cell--settingstoggle + .rt-wrapper .rt-table--X .table-cell--grouped` (impossible match). Same root cause as the `_ship_role.scss` bug fixed in 0.7.31, different syntax (`#{&}` interpolation vs `& &` compound). Replaced with explicit `.table-cell--grouped`. No user-visible change — the targeted DOM combination doesn't actually occur in the rendered sheets — but it clears the last hit from `grep -E '\.rt-wrapper [^,{]+ \.rt-wrapper' build/.../rogue-trader-3rd.css`. Codified in `reference_foundry_v2_sheet_gotchas.md` #12.

**0.7.31** (2026-06-09): **Ship Role detail-box layout fix.** The 0.7.30 detail box rendered each row in a `10em label | 1fr value` grid; values are paragraphs (skills list, mechanical benefit, multi-sentence description) and looked cramped against the narrow value column. Rewritten as a stacked layout — small-caps label above value, value gets full panel width; source line collapsed to a small inline footer. SCSS bug discovered + fixed: BEM compound modifiers (`&__row--benefit &__value`) inside a partial imported under `.rt-wrapper { @import ... }` compile to `.rt-wrapper .x .rt-wrapper .y` (Sass substitutes the full parent chain into each `&` independently) — the resulting selector can never match in the real DOM. Rewritten as nested blocks with explicit `.ship-role-detail__value` references. Codified in `reference_foundry_v2_sheet_gotchas.md` #12. Verified on rt-smoke via Playwright: 23 dropdown options, benefit text intact for ranks 1/3/4, notes round-trip persists.

**0.7.30** (2026-06-09): Into the Storm Ship Roles on the character sheet. New panel on the Bio tab (`ship-role-panel.hbs`) backed by all 22 Bridge Officer roles from ItS Ch.VII pp.224-232 (Lord-Captain through Ship's Steward, across the 4 ItS ranks). Dropdown to select the role; a read-only detail box renders the role's rank, eligible careers, important skills, mechanical benefit, description, and source citation; per-character notes field for subordinates and house-rule effects. Data lives in `src/module/rules/ship-roles.mjs`; schema added at `system.bio.shipRole = {value, notes}`; backfilled in `_ensureOriginPath()` for pre-existing actors. No automation of the role benefits yet (most target ship-combat Extended Actions the engine doesn't currently orchestrate) — the panel is the display/selection layer.

## 0.7.x series — ApplicationV2 actor sheets + content pipeline
- **0.7.32** — **SCSS cleanup — fix doubled-wrapper bug in table grid rules.** `src/scss/components/_grids.scss:178` and `:280` used `&--settingstoggle + #{&}--grouped`. The `#{&}` interpolation embeds the full `.rt-wrapper .rt-table--X .table-cell` parent chain into the middle of the selector, compiling to `.rt-wrapper .rt-table--X .table-cell--settingstoggle + .rt-wrapper .rt-table--X .table-cell--grouped` — that adjacent-sibling pair can never match (no nested `.rt-wrapper` in the real DOM). Same root cause as the `_ship_role.scss` bug fixed in 0.7.31, different syntax (`#{&}` interpolation here vs `& &` compound there). Replaced both with explicit `.table-cell--grouped`. No user-visible change — the targeted DOM combination doesn't actually occur in the rendered sheets — but it clears the last `.rt-wrapper ... .rt-wrapper` hit from the doubled-wrapper detection grep, leaving the build CSS clean. Codified in `reference_foundry_v2_sheet_gotchas.md` #12.
- **0.7.31** — **Ship Role detail-box layout fix.** The 0.7.30 detail box used a `10em label | 1fr value` grid which left paragraph values (skills list, mechanical benefit, multi-sentence description) crammed into a narrow column. Replaced with a stacked layout: small-caps label above value, value gets full panel width. Source line collapses to a small inline footer with a `::after` `: ` separator. Benefit row stays bold; description row italic. SCSS bug discovered + fixed along the way: BEM compound modifiers `&__row--benefit &__value` inside a partial imported under `.rt-wrapper { @import ... }` compile to `.rt-wrapper .x .rt-wrapper .y` because Sass substitutes the full parent chain into each `&` independently — that selector can never match the real DOM. Rewritten as nested blocks with explicit `.ship-role-detail__value` references. Codified in `reference_foundry_v2_sheet_gotchas.md` #12. Detection one-liner: `grep -E '\.rt-wrapper [^,{]+ \.rt-wrapper' build/.../rogue-trader-3rd.css` (two pre-existing hits remain in `.rt-table--no-border` / `.rt-table--border` rules — same pattern, separate fix). Verified on rt-smoke via Playwright (`tests/e2e/verify_ship_role_panel.py`): 23 dropdown options, benefit text correct for ranks 1/3/4 (lordCaptain/masterHelmsman/twistcatcher), notes round-trip persists.
- **0.7.30** — **Into the Storm Ship Roles on the character sheet.** New Bio-tab panel (`ship-role-panel.hbs`) listing all 22 Bridge Officer Roles from ItS Ch.VII pp.224-232. Dropdown selects the role; selected role's rank, eligible careers, important skills, mechanical benefit, description, and source render in a read-only detail box; per-character notes field. Data file `src/module/rules/ship-roles.mjs` is the canonical list (`shipRoleOptions()` + `getShipRole(key)`). Schema: `system.bio.shipRole = {value, notes}` on acolyte template, backfilled in `_ensureOriginPath()`. `CONFIG.rt.shipRoles` exposes the full list; `shipRole` added to `DarkHeresy.bio.skip` so the generic bio loop ignores it. Two new Handlebars helpers: `findByKey` (for the detail-box lookup) and `eq` (for the `<option selected>` test). SCSS at `src/scss/components/_ship_role.scss`. No automation of the role benefits — most target ship-combat Extended Actions (Hold Fast!, Triage, Evasive Manoeuvres, Lock on Target, etc.) that the engine doesn't currently orchestrate. The +X bonuses are documented in the panel for the GM/player to apply manually. Smoke-tested on rt-smoke via Playwright: 22 roles + ranks 1-4 present, default backfill is `{value: '', notes: ''}`, role/notes update round-trips, sheet renders the panel with the correct benefit text for the selected role.
- **0.7.0** — `AcolyteSheetV2` extending `HandlebarsApplicationMixin(ActorSheetV2)`. Pop-out windows on v13/v14. Templates migrated to V2 `data-action` event-dispatch syntax.
- **0.7.1** — characteristic table 10→9 columns (Influence removed), advance-select font shrunk to 8.5pt so Intermediate/Proficient fit.
- **0.7.2** — vehicle + voidship V2 sheets. Extracted shared `ActorContainerSheetV2` base (item/effect CRUD, drag/drop, toggle-visibility); voidship has a `_processSubmitData` override that routes `items.<id>.<path>` form keys through `updateEmbeddedDocuments`. `TYPES.Actor.{npc,vehicle,voidship}` localization added so window titles resolve. **Key SCSS fix:** moved `@import 'sheets/actor'` out of `.rt-wrapper` scope in `rogue-trader-3rd.scss` — in V2 both classes land on the same `<section class="window-content rt-actor rt-wrapper">` element, so the descendant combinator `.rt-wrapper .rt-actor` never matched and the grid was silently never applied (voidship panels stacked full-width instead of 3-up).
- **0.7.3** — NPC V2 sheet as a thin subclass of `AcolyteSheetV2` (only redeclares `npc` frame class and template path).
- **0.7.29** — **Style-unify pass across all 334 NPC portraits.** New tool `tools/npc_pipeline/comfy_npc_stylize.py` runs each portrait through ComfyUI img2img at denoise 0.30 with `Oldhammer.safetensors @ 0.55` (no faction LoRAs — they would partly re-draw the source). Fixed style prompt: `"warhammer 40k character portrait, oil painting by John Blanche and Karl Kopinski, FFG black library illustration, rich saturated color, dramatic chiaroscuro lighting, painted detail, sharp focus, gothic atmosphere"`. Low denoise preserves face/pose/composition; the LoRA + prompt pull palette and brushwork toward a single FFG-style anchor. 1024×1024, 26 steps, dpmpp_2m karras, deterministic seed `1337 + hash(slug)`. 334 portraits / 100.3 min / 0 errors. **Whisperx rebuild fallout fixes** also shipped here: (a) pipeline scripts updated to use `~/comfyui/input/` (single `comfyui/`) and the dashed `Juggernaut-XL_v9_RunDiffusionPhoto_v2.safetensors` filename — the rebuilt VM canonicalised both paths from their pre-rebuild forms; (b) `nDimensional` faction LoRAs (Aeldari/Ork-Boyz/Tau/Astartes/Oldhammer) re-acquired from HuggingFace and placed at `/data2/comfyui/models/loras/`; (c) cuDNN init crash on the rebuilt VM (cu13 stack sitting on top of cu12; driver 535 can't speak CUDA 13) resolved by purging the cu13 packages from the comfyui-venv and force-reinstalling the cu12 ones. All recorded in [[reference_comfyui_whisperx]].
- **0.7.28** — **Manual NO_FACE portrait swap.** The 0.7.27 reframe pass left 81 portraits classified as NO_FACE by the Haar audit. Triaged all 81 by hand at `/mnt/project_data/RT/no_face_review/` (numbered PNGs + `INDEX.md` mapping each number → repo path). 21 were swapped in with hand-picked replacement art; the other 60 confirmed correct (creatures/xenos where Haar can't detect a face but the art is fine). Re-audit: GOOD 190→**193**, NO_FACE 81→62 (19 swaps gained a detectable face; 16 of those landed as FACE_TINY for now — full-figure replacements but with the face visible). Net: GOOD up, NO_FACE down, every NPC still has art.
- **0.7.27** — **Portrait-bust reframe pass.** New tool `tools/npc_pipeline/comfy_npc_reframe.py` consumes the `/tmp/framing.json` produced by `audit_face_framing.py --json`, filters for FACE_TINY/FACE_LOW/FACE_EDGE_LR/FACE_CROPPED_*, and txt2img-regenerates each at **1024×1024** (square aspect matches Foundry's square token crop) with a composition-first prompt: `"portrait bust, head and shoulders, face centered upper third, dramatic close-up, looking at viewer, intense gaze"` plus new negatives (`full body shot, action pose, wide shot, distant figure, tiny face, face cropped out, looking away`). Always txt2img — even where an img2img ref was available — because the existing book-scan refs anchor full-body composition, which is the whole reason FACE_TINY happened. Per-NPC seed = `137 + hash(slug)` so each rerun gets fresh draws. Faction LoRA picked from a slug/book heuristic (`Eldar`/`Ork`/`Tau`). 397 min, 223 ok, 0 errors. Re-audit: **GOOD 54→190** (+136), FACE_TINY 197→53 (-144), FACE_LOW 24→9, FACE_EDGE_LR 2→1. NO_FACE 57→81 (+24) — some FACE_TINY humans were replaced by creature/xenos compositions where Haar doesn't detect a face; not necessarily worse, just unmeasurable by the cascade. Zero FACE_CROPPED in either pass. `BAD_FRAMING.md` regenerated; the remaining 63 flagged are mostly creatures (NO_FACE) plus a long tail of stubborn FACE_TINY where the model resists the bust prompt.
- **0.7.26** — **Backfill NPC portraits + framing audit.** Generated portraits for the 158 NPCs that were missing one (47% of the 338-NPC compendium had `img: ''`). `comfy_npc_batch.py` against `whisperx` SDXL pipeline (JuggernautXL + faction LoRAs + Oldhammer @ 0.5); reference upload for img2img where the source-book art was available, txt2img otherwise. 277 min, 158 ok, 0 errors. `build_all_npc_yamls.py` re-linked `img:` fields across all `*npcs/*.yml`. New audit script `tools/npc_pipeline/audit_face_framing.py` (opencv-python-headless, Haar `frontalface_default` + `profileface` + mirrored-profile fallback) classifies each portrait by face position/size: GOOD 54, NO_FACE 57 (mostly xenos/creatures), FACE_TINY 197, FACE_LOW 24, FACE_EDGE_LR 2, FACE_CROPPED_TOP/BTM **0**. Per-NPC report at `tools/npc_pipeline/BAD_FRAMING.md`. **Portrait-bust rerun pass queued** for the 223 framing-flagged entries.
- **0.7.25** — **NPC math audit: fix `_computeSkills` crash on orphan-skill NPCs.** The 0.7.17 skill split (`athletics`→`climb`+`swim`, `stealth`→`concealment`+`silentMove`+`shadowing`, `linguistics`→`speakLanguage`+…, `operate`→`drive`+`pilot`) left orphan skill keys on hundreds of NPC YAMLs. Those orphan keys had no `characteristic` and no `characteristics` array. `_computeSkills` in `acolyte.mjs` line 337 unconditionally accessed `skill.characteristics[0]`, throwing for every affected NPC and aborting `prepareData()` before `_computeArmour` / `_computeMovement` / `_computeEncumbrance` could run. The 0.7.17 changelog had asserted these orphans were "harmless" — they weren't. Defensive guard: `if (!skill.characteristic && (!Array.isArray(skill.characteristics) || skill.characteristics.length === 0)) continue;`. Audit script `/tmp/audit_npcs.py` against rt-smoke: before fix — 9 NPCs with Natural Armour trait showing body=0, 253 movement_mismatches; after fix — 0 armour-with-trait-zero, 124 movement mismatches (all source-book special-creature overrides or upstream Unnatural-Agility extraction bugs, not math bugs). All 308 unique weapon damage formulas across 338 NPCs parse correctly. Characteristic bonus math `bonus = floor((base + advance*5 + modifier)/10) + unnatural` verified correct for every NPC. Remaining data-quality issues (not math): 27 NPCs with `wounds.max=0`, 261 weapon items with empty `damage` field — these are upstream OCR/extraction bugs in `tools/npc_pipeline/`, separate pass.
- **0.7.24** — **Character Entry Guide JournalEntry + new Guides compendium.** New compendium pack registered in `system.json`: `{name: "guides", type: "JournalEntry", label: "Guides"}` backed by `src/packs/guides/character-entry-guide.yml`. Single JournalEntry doc (`Character Entry Guide`) with 10 text pages, each one a tab on the Explorer sheet (Overview, Header, Combat, Main / Characteristics-Skills-Talents, Bio / Origin Path, Gear, Psychic Powers, Advances, Social, Rolls & Combat Actions). Pages cross-reference RT Core page numbers throughout. Terminology purged of DH-isms — "Acolyte" replaced with "Explorer" per RT 1e. Smoke-tested via Playwright against `rt-smoke` world: `game.packs.get('rogue-trader-3rd.guides').getDocuments()` returns 1 doc / 10 pages, all type=text with non-empty HTML, no "Acolyte" leakage.
- **0.7.23** — **Psychic-powers audit pass + damage roll dice notation in chat card.** Chat damage row now reads `1d10[4] + 5 = 9` instead of `4 + 5` — new `rollFormula` Handlebars helper in `handlebars-helpers.mjs` expands each Foundry Die term to `{count}d{faces}[{results}]` and OperatorTerm/NumericTerm pass through unchanged; works on both live Roll instances and serialized chat-history JSON. Penetration row gets the same treatment when the weapon has a penetration roll. **Psychic-powers audit pass.** Full content audit of all 79 powers in `src/packs/psychic-powers/psychic-powers.yml` against RT Core (CoreBook-1-200.pdf markdown) and Into the Storm. **P0 (8 clearly-wrong values):** Puppet Master cost 500→**300** xp (canon Table 6-4 + body line "Value: 300 xp"); Force Bolt damage `1d10+2+2*@pr`→`1d10+2*@pr` (spurious flat +2); Telekinetic Crush `1d10+2*@pr`→`1d10+@pr`; Telekinetic Weapon `1d10+3*@pr`→`1d10+@pr`; Force Shards `1d10+2*@pr`→`1d10+@pr`; Storm of Force `2d10+3*@pr`→`2d10+2*@pr`; Psychic Scream `penetration: 99` sentinel→`0` (canon text only mentions Shocking, no armour-bypass); Thought Sending range `10m`→`1km` × Psy Rating (10m was the Mind's-Eye-boosted value, not base). Earlier 0.5.0 and 0.7.12 changelogs both claimed these formulas were canonical — they weren't. **P1 (33 prereq fields):** Telepathy 12 (Astral Telepathy / Mental Bond / Mind Scan / Psychic Scream / Terrify / Compel / Dominate / Puppet Master / Reprogram / Sensory Deprivation / Chaos Mind / Mental Alacrity), Divination 7 (Augury / Psychometry / Divining the Future / Walking the Path / Blessed by the Emperor / Auto-Séance / The Action Again), Telekinesis 7 (Telekinetic Weapon / Telekinetic Shield / Force Shards / Storm of Force / Death Grip / Levitation / Psycho-Kinetic Shield), Theosophamy 3 (Banishment / Sanctuary / Warp Weapon), Astropath starship actions 4 (The Ties That Bind / Divining the Way / Taking the Shot / Inspiring Presence). Uniform extraction omission — every chained power shipped with `prerequisite: ''` despite canon listing one. **P2:** Mind's Eye reclassified as passive — Table 6-4 lists Focus Time / Sustain as `N/A` (it's an always-on ×10 range multiplier on all other Telepathy ranges, not a cast). Audit methodology: grep against pre-extracted RT-DOCS markdown rather than NotebookLM round-trips (much faster, citation-able). Audit report archived at `.claude/worktrees/agent-ae60376ee174f6e57/PSYCHIC_AUDIT_REPORT.md`. Smoke-tested via Playwright against `rt-smoke` world (`game.packs.get('rogue-trader-3rd.psychic-powers').getDocuments()` round-trip — 17 field assertions pass). **Schema limitation noted (not fixed):** per-DoS damage scaling (Banishment, Scourge of the Red Tide), Navigator Novice/Adept/Master tier progression, and "ignores armour unless warded" clauses live in description text only — the `damage`/`penetration` Roll-formula fields can't express them without engine support.
- **0.7.22** — **Combat-action audit + modifier-sum bug fix.** (1) `RollData.calculateTotalModifiers()` was routing through a Foundry `Roll('0 + @attack + @Best Craftsmanship')` formula; spaces and hyphens in `@key` identifiers caused the formula to throw, the catch block silently zeroed out the entire modifier total, and ALL modifiers (including clean ones like `attack`) were dropped. Rewrote to sum integers directly with ±60 clamp. Affected `Best Craftsmanship`, `target-size`, `Red-Dot`, `Custom-Grip`, `Modified-Stock`, `Motion-Predictor`, `Poor/Good Craftsmanship`, `explosive arrows`, `Cannot Parry`, `Weapon Parry Bonus`. (2) `Standard Attack` no longer grants +10 (was DH2 carryover; RT Core p.246 specifies no bonus). (3) RT Core Table 9-4 audit: added **Multiple Attacks** (Full, Attack, Melee or Ranged), renamed **Guarded Action → Guarded Attack** (+ added `Concentration` subtype), marked **Swift Attack** and **Lightning Attack** as `legacy: true` (RT lists them as Talents not actions; hidden from attack dropdown but retained for backwards compat). Parry subtype gained `Defence`. Smoke-tested on `rt-smoke` world via Playwright — melee dropdown now shows 10 actions (was 12) with no DH2-only entries.
- **0.7.21** — **Armour display fix + Parry corrections.** (1) Removed TB from `armour.location.total` — was `toughness.bonus + traitBonus`, now just `traitBonus`. The display panel shows raw AP per location; TB shown separately. Damage code (`assign-damage-data.mjs`) already used `value`+`toughnessBonus` independently — unaffected. Stun attack code updated to explicitly add TB (`headArmour.total + headArmour.toughnessBonus`). (2) Parry at advance 0 now uses full WS (RT 1e Reaction), not half WS (was treated as untrained Basic). (3) Removed craftsmanship bonuses (Poor -10/Good +5/Best +10) from Parry rolls — RT Core specifies these apply to attack tests only, not defensive reactions. Parry modifiers now: Balanced +10, Defensive +15, Unwieldy/Unbalanced blocks, weapon `parryBonus`.
- **0.7.20** — **Skill sheet split into three panels + "Treat as Basic" + data fixes.** Replaced Bonuses panel with separate Basic Skills / Advanced Skills / Specialist Skills panels (both acolyte and NPC sheets). Basic panel shows only Basic skills; Advanced panel shows non-specialist Advanced skills with an "As Basic" dropdown option (advance=-1) for Origin Path traits that let you treat an Advanced Skill as Basic (tests at half-char, not blocked). `skill.treatAsBasic` flag in `_computeSkills()` + `rollSkill()` allows rolling. Specialist panel also has "As Basic" option. **Data fixes:** Security default characteristic corrected to Ag (was Int). Performance specialties added: Dancer, Musician, Singer, Storyteller. New template `skills-advanced-panel.hbs` registered in Handlebars preload.
- **0.7.19** — **Skill sheet shows Basic vs Advanced distinction.** Each skill row displays a superscript `B` or `A` tag next to the name. Untrained Basic skills show their half-characteristic value in italic at 60% opacity. Untrained Advanced skills show `—` at 35% opacity with the entire row dimmed to 55% opacity (`skill-locked` class). Trained skills display normally regardless of type. `skill.isBasic` flag added to `_computeSkills()` for template access. Specialist skill panel also dims untrained specialties with `—`. SCSS in `_actor.scss`: `.skill-type-tag`, `.skill-half`, `.skill-locked-val`, `.skill-locked`.
- **0.7.18** — **Complete RT 1e skill list (49 skills).** Added 10 missing standalone skills: `barter` (Fel/Basic), `blather` (Fel/Advanced), `chemUse` (Int/Advanced), `contortionist` (Ag/Basic), `disguise` (Fel/Basic), `evaluate` (Int/Basic), `gamble` (Int/Basic), `invocation` (WP/Advanced), `search` (Per/Basic), `tracking` (Int/Advanced). Split `operate` → `drive` (Ag/specialist: Ground Vehicle, Skimmer/Hover, Walker) + `pilot` (Ag/specialist: Flyer, Personal, Space Craft). Skill count 38→49. BASIC_SKILLS now 21, Advanced 28. All updated in template.json, SKILL_LABELS, Talented options, NPC pipeline SKILL_MAP. No code referenced `operate` programmatically — ship rolls use crew skills not pilot/drive.
- **0.7.17** — **Split DH2 consolidated skills back to RT 1e canon.** Removed `athletics` (→ `climb` S/Basic + `swim` S/Basic), `stealth` (→ `concealment` Ag/Basic + `silentMove` Ag/Basic + `shadowing` Ag/Advanced), `linguistics` (→ `speakLanguage` Int/specialist with 7 languages + `secretTongue` Int/specialist with 7 tongues + `ciphers` Int/specialist with 5 ciphers + `literacy` Int/non-specialist). Skill count 32→38. All updated in `template.json`, `BASIC_SKILLS`, `SKILL_LABELS`, Talented pickable options, NPC pipeline `SKILL_MAP`. Existing actors gain new skills at advance 0 via template merge; old consolidated keys become orphan data (harmless).
- **0.7.16** — **RT 1e skill advance system.** Replaced DH2's 5-level system (Unknown/Known/Trained/Experienced/Veteran with -20/0/+10/+20/+30) with RT 1e's 4-level system (Untrained/Trained/+10/+20 — hard cap at +20, max 3 acquisitions per RT Core p.76). Untrained Basic skills test at half governing characteristic (floor); untrained Advanced skills cannot be attempted at all (blocked with UI notification). `BASIC_SKILLS` set in `acolyte.mjs`: athletics, awareness, carouse, charm, command, deceive, dodge, inquiry, intimidate, logic, parry, scrutiny, stealth (13 skills). All other skills (19) are Advanced. `_computeSkills()` sets `skill.untrained` flag and computes `skill.current` accordingly. `rollSkill()` blocks untrained Advanced with `ui.notifications.warn()`. NPCs inherit identically (zero overrides in `npc.mjs`). Old advance=4 values on existing actors safely clamp to +20.
- **0.7.15** — **Add missing Carouse and Wrangling skills.** Carouse (Toughness, basic — RT Core p.78) and Wrangling (Intelligence, basic — RT Core p.100) were never added to the data model despite being canonical RT 1e skills. Added to `template.json` skill definitions, `SKILL_LABELS` in talent-choice picker, Talented talent's pickable options, and NPC pipeline `SKILL_MAP`. Both auto-render on character sheets (skills panel iterates dynamically). Existing actors gain both skills at default (advance 0) on next template merge. Skill count: 30→32.
- **0.7.14** — **Fix NPC Natural Armour/Machine trait levels.** `_computeArmour()` switch only matched American spelling "Natural Armor" — added fallthrough `case 'Natural Armour':` for British spelling used by all 9 NPC trait items. Set correct `level`/`hasLevel` on 9 NPC traits across 5 supplement packs from source book stat blocks (Monster In The Maze NA 8, Dweller In The Heights NA 5, Clawed Fiend×2 NA 4, Killian's Bane NA 5, Terrorax NA 6, Thornmaw NA 2, Vaporian Glasshawk Machine 3, Razorwing NA 2). Full NPC mechanics audit confirmed: NPCs inherit all Acolyte computation identically, all 11 Talented talents have correct ActiveEffects, unnatural characteristics correctly populated on 60+ NPCs.
- **0.7.13** — **Fix Deathdealer double-application on Righteous Fury.** The RF-specific Deathdealer block inside the Righteous Fury trigger (lines 136-144 of `damage-data.mjs`) granted full Perception bonus as `modifiers['deathdealer']`, while the unconditional Deathdealer code (lines 176-180 melee, 229-233 ranged) already applied the correct `Math.ceil(perBonus / 2)` as `modifiers['deathdealer melee']`/`modifiers['deathdealer ranged']`. Both used different modifier keys so they stacked — on RF hits the character got ~1.5x their Perception bonus. Fix: removed the RF-specific block. Comprehensive personal combat math audit confirmed all other formulas correct: hit/DoS calculation, additional hits (Semi/Full/Swift/Lightning), fire rate capping, jam/overheat thresholds, all damage specials (Tearing/Accurate/Scatter/Primitive/Proven/Maximal/las modes), talent bonuses (Crushing Blow/Mighty Shot/Deathdealer), penetration modifiers (Lance/Razor Sharp/Melta/Hammer Blow/Eye of Vengeance/Maximal/Overload), damage assignment (armour-pen-TB reduction, wound overflow, True Grit), hit locations (reversed d100), combat action modifiers (Table 9-1), ammo consumption.
- **0.7.12** — **Psychic power math fixes (RT 1e alignment).** Removed DH2 PR modifier from Focus Power Test — the ±10 per PR point difference between base rating and effective PR (`modifiers['bonus'] = 10 * Math.floor(rating - pr)`) was a DH2 mechanic not present in RT 1e. In RT Core p.157-158, Fettered/Unfettered/Push changes effective PR and phenomena/perils triggers but does NOT modify the Focus Power Test target. Previously a Fettered psyker with PR 4 got +20 to their test. "Rating Bonus" display row removed from psychic prompt template. Fixed Telekinetic Weapon and Force Shards penetration from 0 to `"@pr"` — both should equal effective Psy Rating per RT Core errata. The `@pr` formula evaluates dynamically via Foundry's Roll class against `rollData.pr`. All 79 psychic powers audited: damage formulas, phenomena/perils triggers, Focus Power bonus, range handling all verified correct.
- **0.7.11** — **Starship combat overhaul + component bonus system.**
  - **Ship combat math**: Lance weapons now bypass armour entirely — all lance hits auto-penetrate per RT Core p.216. Void shields implemented — macrobattery hits absorbed before armour check, lances bypass shields, shield count decremented on target actor. Fixed `calculateResultVoidship()` broken if/else chain (turrets also triggered spurious crew check). Ship weapon range bands computed dynamically from max range (Short ≤Range/3, Medium ≤Range*2/3, Long ≤Range) — was always resolving to Long Range -20 because `rangeSmall`/`rangeMedium` were 0 in all weapons. Component armour bonuses now included in penetration calculation.
  - **Ship compendium data**: All ship weapon/component `hitPoints` 0→2 (critical damage system was broken — `assign-damage-data.mjs` filters by `hitPoints > 0`, so no components were ever targetable for critical effects). Star-flare Lance reclassified from Turret/Macrocannon to Lance/Lance with Heavy size. 9 bridges corrected from componentType "Void Shield" to "Bridge", 4 void shields to "Void Shields" (plural), 4 warp engines from "Warp Drive" to "Warp Engine" — all now match critical damage table keys. Crit table key renamed from "Ship\`s Bridge" to "Bridge". Plasma Drive Explosion removed (all-zero placeholder). 6 weapon descriptions fixed (3 concatenated OCR artifacts, 3 index-page garbage from ItS).
  - **Component bonus system (NEW)**: `bonuses` schema added to `shipComponent` template in `template.json` (maneuverability, hullIntegrity, morale, armour, armourProw, turrets, detection, speed, bsShipWeapons). `_computeComponentBonuses()` in `voidship.mjs` sums bonuses from installed components (skips Destroyed/Unpowered). Maneuver/Detection rolls include bonuses. Turret count includes bonus. Ship weapon attacks include bridge BS modifier. Penetration check includes armour bonuses. Sheet panels display `(+N)` indicators. 18 components wired: Command/Armoured Command Bridge (+5 BS), Ship Master's Bridge (+10 BS), Logis-targeter (+5 BS), Augmented Retro-thrusters/Gravity Sails/Gyro-stabilisation Matrix (+5 manoeuvrability), Armour Plating (+1 armour/-2 manoeuvrability), Armoured Prow (+4 prow), Reinforced Interior Bulkheads (+3 hull), Micro Laser Defence Grid (+1 turrets), Exploration Bridge (+5 detection), Temple-shrine (+3 morale), Ancient Life Sustainer (+2), Supply Vaults/Observation Dome (+1), Pressed-crew Quarters (-2), Luxury Quarters (-3).
- **0.7.10** — **Combat action modifier fixes.** Guarded Action was missing `attack: { modifier: -10 }` and `Attack` subtype — never appeared in the weapon attack prompt and applied no penalty. Now correctly applies -10 WS/BS and appears for both melee and ranged. Overwatch was missing `attack: { modifier: -20 }` — now applies -20 BS per RT Core combat action table.
- **0.7.9** — **Craftsmanship mechanics + Parry wiring + missing weapons.**
  - **Craftsmanship mechanics** (RT Core pp.116, 138): Weapon craftsmanship now affects combat rolls. **Ranged**: Poor adds Unreliable (cancels Reliable if present), Good adds Reliable (cancels Unreliable), Best never jams or overheats (jam threshold set to 101 in `action-data.mjs`, overheat suppressed). **Melee**: Poor -10 / Good +5 / Best +10 attack via `specialModifiers` in `attack-specials.mjs`; Best +1 damage via `modifiers['best craftsmanship']` in `damage-data.mjs`. **Armour**: Best +1 AP per location (tracked per winning piece in `_computeArmour` via `maxArmourCraft`), Best half weight via `totalWeight` getter in `item.mjs`. Good armour (+1 AP first attack/round) not automated — requires per-round state tracking. Poor armour (-10 Ag) not automated.
  - **Parry roll wiring**: `rollSkill('parry')` in `acolyte.mjs` now looks up the first equipped melee weapon and applies: Balanced +10, Defensive +15, Unwieldy/Unbalanced -999 (blocks parry), craftsmanship bonuses (Poor -10, Good +5, Best +10), and `system.parryBonus` for weapon-specific pattern bonuses. Falls back to `system.special` flags if embedded attack-special items haven't migrated yet.
  - **New weapons (5)**: Power Sword (Mordian) with `parryBonus: 5`, Bolas (Primitive/Snare/Inaccurate thrown, was missing from corebook), Force Axe/Staff/Sword (Naduesh) from Into the Storm. `parryBonus: 5` also added to Ghost Sword and Parrying Dagger.
  - **OCR name fixes**: Macusta→Lacusta Hammer, Hoi→Loi Pattern Power Axe.
- **0.7.8** — **Compendium data audit — weapons, cybernetics, armour.**
  - **Weapons (40 fixes)**: Systematic build-script bug where the special-quality substring matcher mapped `Inaccurate` → `accurate: true`, `Unreliable` → `reliable: true`, `Unbalanced` → `balanced: true`. 34 flag fixes across 23 weapons (Ork Shoota/Slugga, Flintlock Pistol, Musket, Blunderbuss, Big Shoota, Burna, Rokkit Launcha, Deffgun, Grenade Launcher Voss, Razorweb Launcher, Mole Mortar, both Mortars, Naval Shotcannon, Plasma Cannon, Power Axe, Omnissian Axe, Ork Choppa, Great Weapon, Improvised Weapon, Big Choppa, Hoi Pattern Power Axe, Disposable Handgun). Storm Bolter rebuilt from stub data to correct corebook p.119 stats (S/2/4, 1d10+5 X, Pen 4, Clip 60, Storm+Tearing, Extremely Rare). Individual stat fixes: Autogun reload Full→2 Full, Meltagun (Mars) weight 7.5→40, Meltagun (Mezoa) weight 8.5→46, Multi-Melta burst RoF 0→3, Graviton Gun clip 2→3 + pen 3→0 (values were swapped). Full Auto Burst description corrected from "one hit plus one additional hit per DoS" to "one hit per DoS" to match the code and RT Core rules.
  - **Cybernetics**: All 30 availability values corrected from schema-default `common` to corebook Table 5-16 / ItS values. Armour-point wiring added: Bionic Heart `hasArmourPoints: true` + body AP 1, Cranial Armour `hasArmourPoints: true` + head AP 1, Subskin Armour `hasArmourPoints: true` + arms/body/legs AP 2 — all three now mechanically stack with worn armour via `_computeArmour()`. Three missing corebook cybernetics added: Voidskin (Scarce), Volitor Implant (Rare), Vox Implant (Scarce). Utility Mechadendrite truncated description completed.
  - **Armour**: Squighide AP 1→3 (all locations), weight 3→4 kg, availability Common→Scarce, type Other→Primitive with note. Eldar Forceshield weight 0.2→2.0 kg. Archeotech Blurshield description corrected (was mangled with Mirror Shield reflect mechanic), type Field→Other (not a force field), weight 2.0→3.0 kg.
- **0.7.7** — **Weapon specials audit + traits expansion + NPC talent wiring.**
  - **Weapon special fixes**: Storm now doubles all hits (initial + additional) on every fire mode — was only doubling `additionalHits` inside burst/auto branches, so single-shot Storm weapons scored 1 hit instead of 2. Ammo doubled universally via `ammoPerShot *= 2`. Lance penetration fixed from `pen*(1+DoS)` to `pen*DoS` (modifier was `pen*dos`, should be `pen*(dos-1)` since base pen is already included). Razor Sharp pen doubling on 3+ DoS moved out of the melee-only block so it works for ranged weapons too. Unreliable jam threshold implemented at 91+ (was silently using normal 96+). Normal jam threshold fixed from `> 96` to `>= 96` so rolling exactly 96 correctly jams per RT Core p.238.
  - **Traits compendium** expanded from 33 → 45: 12 new Into the Storm traits (Kroot: Eaters of the Dead, Fieldcraft, Kroot Beak, Kroot Physiology, Instinctual Understanding, Shamanic Powers; Ork: 'Ard, Made Fer Fightin', Make It Work, Might Makes Right, Mob Rule; General: Improved Natural Weapons). Fixed truncated OCR descriptions for Incorporeal, Machine, Mechanicus Implants.
  - **NPC talent wiring**: `to_item_stub()` in `build_npc_yaml.py` now deep-copies `effects` and `flags` from compendium talents (was sharing dict references — first NPC's `_apply_npc_talent_choice` deleted `pickable` from the shared dict, silently breaking all subsequent NPCs). New `_apply_npc_talent_choice()` extracts parenthetical choices from NPC stat-block names, stamps `system.choice`, builds ActiveEffects for `kind=skill` (Talented → +10 to skill modifier), updates conditional bonus labels with specific targets, and strips `pickable` flag.
- **0.7.6** — **Talent mechanics overhaul.** Three new patterns landed on top of the existing pack:
  - **Always-on Active Effects**: Machinator Array, Master Chirurgeon, Hotshot Pilot, Infused Knowledge carry transferable AEs that auto-bump characteristic/skill modifiers. Schema gained `skill.modifier` field + `_computeSkills` runs twice (before and after `super.prepareData()`) so AEs feed into `skill.current`.
  - **Pickable talents**: 10 talents (`Talented`, `Hatred`, `Peer`, `Good Reputation`, `Enemy`, `Rival`, `Weapon Master`, `Exotic Weapon Training`, `Psychic Technique`, `Warp Eye`) tagged with `flags.rt.pickable: {kind, options}` and prompt a DialogV2 on `createItem` (registered in `hooks-manager.mjs`). Choice stored on `system.choice` + appended to item name. Only `kind=skill` (Talented) builds a dynamic ActiveEffect targeting the chosen skill's modifier — the other 9 are conditional.
  - **Optional-per-roll bonuses**: 23 conditional talents tagged with `flags.rt.conditionalBonuses[]` describing (`applies.skills`, `applies.characteristics`, `value`, `label`). `BaseActor.collectOptionalBonuses({skill, characteristic})` returns matches; `simple-roll-prompt.hbs` renders each as a checkbox; the roll callback sums checked values into `rollData.modifiers.modifier`. Covers Hatred, Peer, Rival, Resistance variants, Berserk Charge, Frenzy (multi-entry: +10 WS/S/T/WP AND -20 BS/Int), Disturbing Voice, Iron Discipline, Void Tactician, etc.
- **0.7.5** — **Career Special Abilities** added to the talents pack. 13 new entries covering each Career's signature ability per RT corebook Ch.III: Exceptional Leader (Rogue Trader), Weapon Master (Arch-militant), Soul-Bound to the Emperor + See Without Eyes (Astropath Transcendent), Explorator Implants (Explorator), Warp Eye + The Boons of Lineage + Navigator Mutations (Navigator), Seeker of Lore (Seneschal), and the four Void-master Masteries (Space / Gunnery / Augurs / Small Craft). Each entry tagged with `flags.rt.category: "Career Special Ability"` and `flags.rt.career` for filtering. Pure Faith (Missionary) was already in the pack pre-0.7.5.
- **0.7.4** — **NPC compendium fleet.** 19 new packs (`<book>-npcs`) registered in `system.json` with GM-only `ownership: {PLAYER: "NONE", ASSISTANT: "OWNER"}` so players can't browse them. 334 NPCs total, extracted by `tools/npc_pipeline/extract_all_npcs.py` from Mistral-OCR'd RT supplement markdown. Token sizes derive from each NPC's `Size (X)` trait per `size.md` (Miniscule/Puny 0.5×0.5, Average/Hulking 1×1 with Hulking art-scaled 1.2×, Enormous 2×2 up to Titanic 8×8). 176 portraits live: 145 colorized via low-denoise (0.18) ComfyUI img2img against canonical FFG references, 31 hand-curated overrides from `/mnt/project_data/RT/RT-NPC/` for NPCs flagged BAD by the strict-check sweep. 158 NPCs ship without a portrait — Foundry falls back to its default `mystery-man.svg`.

V2 conversion gotchas codified in `~/.claude/projects/-home-ahermon/memory/reference_foundry_v2_sheet_gotchas.md`.

## 0.6.0 — Origin Path (Path A lite)
0.6.0 shipped **Path A (lite)** of the character-creation decision: a six-stage Origin Path panel (Home World / Birthright / Lure of the Void / Trials and Travails / Motivation / Career) with canonical RT Core + Into the Storm dropdowns and free-text notes per stage, plus a coordinated skill-specialty list migration to RT canon (Common Lore 14, Forbidden Lore 11, Scholastic Lore 16, Linguistics 12, Trade 13). The DH2-fork bio fields (homeWorld/background/role/elite/divination) remain in `template.json` for data continuity but are no longer rendered on the sheet. No advance-table model or wizard — players write their effects into the per-stage notes field.

## 0.5.x — pre-character-creation punch list
0.5.0 closed the "everything before the A/B character-creation decision" punch list — Accurate damage fix (1d10 per 2 DoS), ship-weapon damage rolling (1d10 + listed Damage per hit, RT corebook p.215), 17 psychic-power damage formulas, ammo + Arms Coffer errata (v1.4), four talent OCR fill-in corrections (Frenzy / Rite of Fear / Catfall / Unarmed Warrior), and the canonical RT Table 8-12 Critical Hits + Catastrophic Damage RollTables. 0.5.1 refreshed inventory icons with 98 Flux-dev-generated bucket icons (520 entries rewired). 0.5.2 fixed the psychic-power damage-roll gate (`subtype: 'Concentration Attack'`), the tables-icon path, and the chainsword bucket art.

## Test environment (foundrySB)

Proxmox LXC for live-testing this system in Foundry.

| Property | Value |
|---|---|
| CTID | 211 (foundrySB) |
| IP | 192.168.11.36 |
| Foundry | v14.361 |
| World | rt-smoke |
| Node.js | v24.15.0 |
| App path | /opt/foundry |
| Data path | /var/lib/foundrydata |
| Log | /var/log/foundry.log |
| URL | http://192.168.11.36:30000 |
| SSH | `ssh -i ~/.ssh/foundry_test project@192.168.11.36` |

### Deploy loop

After `npm run build`, rsync each subdirectory of `build/rogue-trader-3rd/` to the matching subdirectory under `/var/lib/foundrydata/Data/systems/rogue-trader-3rd/`. **Rsync gotcha:** never pass two source dirs into one destination — they collide. Sync each subdir to its own dest.

Then restart: `sudo systemctl restart foundry.service`.

### Disk discipline — release zips + old artifacts live on the mount, NOT local `/`

The dev LXC's root `/` is a **10 GB ZFS subvol that runs ~97% full** (`.git` ≈1.4 GB, `build/` ≈550 MB). A single release zip is ~567 MB, so it will NOT fit on `/` — and `gulp createArchive` (gulp-zip buffers the whole archive in memory) gets **OOM-killed** in the 2 GB RAM box anyway. Rules:

- **Build release zips straight onto the mount** `/mnt/project_data/RT/rt-vtt-archive/` (17 TB `data2` pool), never into the repo's `./archive/`. There is no `zip`/`unzip` binary on this host — stream with Python:
  ```bash
  python3 - <<'PY'
  import os, zipfile
  src="build/rogue-trader-3rd"; dest="/mnt/project_data/RT/rt-vtt-archive/rogue-trader-3rd-vtt-<VER>.zip"
  with zipfile.ZipFile(dest,"w",zipfile.ZIP_DEFLATED,compresslevel=6) as z:
      for r,_,fs in os.walk(src):
          for f in fs: z.write(os.path.join(r,f), os.path.relpath(os.path.join(r,f), src))
  PY
  ```
  Entries are relative to `build/rogue-trader-3rd/` so `system.json` sits at the zip root (matches the gulp-zip layout Foundry expects).
- **Always move old/large artifacts off `/` to the mount when done** — release zips, stale `build/` copies, big scratch outputs. The archive of past releases (0.7.25–0.7.32, ~4 GB) already lives at `/mnt/project_data/RT/rt-vtt-archive/`.
- ZFS free-space accounting lags after a cross-filesystem `mv` (snapshot/pool delay), so `df /` may not drop immediately — that's expected, not a failed move.

### Compendium changes need a cache-clear (Foundry V14 gotcha)

Foundry V14 auto-migrates each system NeDB `.db` compendium into a **LevelDB cache** as a subdirectory next to the .db file on first read:

```
<data>/systems/<system>/packs/<pack>.db       ← source (NeDB, what we deploy)
<data>/systems/<system>/packs/<pack>/         ← LevelDB cache (000005.ldb, CURRENT, MANIFEST-*, LOG)
```

After the cache exists, Foundry **ignores `.db` file changes** even across `systemctl restart`. It only re-migrates if the LevelDB subdirectory is absent. No error, no warning — the server quietly serves stale data, and `pack.clear()` + `pack.getDocuments()` returns the cached version.

After any compendium change, the deploy step MUST also drop the caches:

```bash
sudo systemctl stop foundry.service && \
sudo rm -rf /var/lib/foundrydata/Data/systems/rogue-trader-3rd/packs/*/ && \
sudo systemctl start foundry.service
```

The `*/` glob removes only LevelDB subdirectories (which are root-owned because Foundry creates them), leaving the `.db` files (project-owned) intact. Codified in `~/.claude/projects/-home-ahermon/memory/reference_foundry_v14_leveldb_cache.md`.

### Headless debugging (Playwright)

Playwright is installed in `~/.venvs/playwright/`. Use it for direct page-context inspection of Foundry. Template script: `/tmp/inspect_compendium.py` — navigates to `/join`, logs in as Gamemaster (empty password while debugging — user clears + resets), runs JS in the page context, dumps the result to stdout. The test world's GM seat is single-occupancy, so the user must log out of their browser tab before a Playwright session can claim it.

## Adjacent infrastructure — whisperx (ComfyUI image gen)

The `whisperx` Proxmox LXC at `192.168.11.22` hosts the SDXL image-generation pipeline used for NPC portraits (see "NPC compendium pipeline" / "Image generation" sections). Connection details:

| Property | Value |
|---|---|
| SSH | `ssh ahermon@192.168.11.22` (ed25519 key auth, passwordless from dev LXC) |
| Alt access | `ssh project@192.168.11.22` (password `project`, has sudo) for system admin / model installs |
| ComfyUI API | `http://192.168.11.22:8188` (LAN-only — iptables INPUT restricts to `192.168.11.0/24` + `127.0.0.0/8`) |
| Install | `/home/ahermon/comfyui/ComfyUI/`, venv at `/home/ahermon/comfyui-venv/` (Python 3.10, torch 2.12) |
| Service log | `/var/log/comfyui/comfyui.log` |
| GPU | RTX 3060 12GB |
| Workflows | `tools/npc_pipeline/comfy_npc_img2img.py` (in this repo) is the production entry point |

**Security note (2026-05-18):** the LXC was compromised before this date with a crypto-miner + supply-chain RCE attack — a fake `comuifyConfig` custom node that fetched `45.130.22.219/comfyUI<MMDD>.elf` on every ComfyUI start and dropped systemd-user-timer backdoors with innocent names (`gnome-X11`, `mail-sync`, `nano`, `ssh.config`, `ssh-proxy`). Cleaned up, payloads quarantined to `/root/forensic/`. **ComfyUI-Manager has been removed (had remote install endpoints — the RCE vector) — don't reinstall it.** The C2 IP `45.130.22.219` is permanently blocked at iptables OUTPUT. If a compendium-style supply-chain attack pattern reappears (suspicious URLs in custom_node `__init__.py`, unfamiliar `~/.config/systemd/user/*.service` with `ExecStart=/home/ahermon/.somewhere/...`), repeat the audit pattern from `~/.claude/projects/-home-ahermon/memory/reference_comfyui_whisperx.md`.

## Build

- `npm run build` — full build into `build/rogue-trader-3rd/`
- `npm run packs` — rebuild compendium `.db` files from `src/packs/*/[name].yml`
- `npm run watch` — gulp watch
- `npm run scss` — SCSS only

Compendium source-of-truth is the YAML files under `src/packs/<pack>/<pack>.yml`. The `.db` files (NeDB) are generated artifacts.

## Schema notes

Talent items use a DH-2e-inspired schema (`aptitudes`, `tier`, `prerequisites`, `benefit`, `cost`, plus `description`/`source`) — but the *content* now reflects RT 1e talents, which don't natively have aptitudes/tiers. Convention in this repo: `aptitudes: ''` and `tier: '1'` for RT-sourced talents. The `tier` field is currently not consumed by any sheet code.

Weapon items: `class` (Pistol/Basic/Heavy/Melee/Thrown), `type` (Bolt/Las/SP/Melta/Plasma/Flame/Chain/Power/Shock/Primitive/Exotic/Launcher), `damage`, `damageType` (Energy/Explosive/Impact/Rending), `penetration`, `range` (m), `rateOfFire: {single, burst, full}`, `clip: {max, value}`, `reload`, `special: {accurate, balanced, blast, ...}`, `weight`, `availability`, `craftsmanship`.

Armour items: `armourPoints: {head, leftArm, rightArm, body, leftLeg, rightLeg}`, `maxAgility`, `type` (Primitive/Basic/Carapace/Power/Field/Other), `weight`, `availability`.

Item type registry is in `src/template.json`.

## NPC compendium pipeline (in flight, 2026-05-19)

Building the first **Actor**-type compendium for this system: `src/packs/npcs/npcs.yml` → `npcs.db`. Registered in `system.json` as `{ "name": "npcs", "type": "Actor", "label": "NPCs" }`. PoC currently has the four xenos NPCs from corebook Ch.XIV (Eldar Corsair, Ork Freebooter, Kroot Mercenary, Warp Predator/Ebon Geist).

**Extraction:** `/tmp/build_npcs.py` parses NPC stat blocks from `/mnt/project_data/RT/RT-DOCS/CoreBook-201-401.pdf/markdown.md`. Handles:
- Both table formats: dual-header (`| Profile | | …` + separator + `| WS BS S T …` + data row, like Eldar) and single-header (just `| WS BS S T …` + separator + data row, like Ebon Geist).
- Multi-line cells where the markdown converter split `(N)\nvalue` (Unnatural Characteristic notation) across two lines.
- Paragraph splits caused by inline image markdown breaking up a single skill/talent/trait list — merged back into one section.
- Footnote bleed: lines like `". †..."` or `". Daemonic Presence:..."` after a list are cut at the period.
- Stat-block skill name → `template.json` skill key mapping (consolidated: Barter→commerce, Climb→athletics, Silent Move/Concealment→stealth, Speak Language→linguistics, etc.).

**Assembly:** `/tmp/build_npc_yaml.py` builds the Foundry actor YAML, resolving talent/trait names against `src/packs/{talents,traits}/*.yml` to embed full benefit/description data. Inline weapons/armour parsing:
- Weapon spec like `(60m; S/3/10; 1d10+4 R; Pen 6; Clip 100; Reload 2Full; Reliable)` → structured `system.{range,rateOfFire,damage,damageType,penetration,clip,reload,special:{reliable:true}}`.
- Armour `(Body 5, Head 4, Arms 2, Legs 2)` → `system.armourPoints` with Arms/Legs split into left/right.
- Compendium-weapon lookup with qualifier-stripping (`xeno-crafted laspistol` → also tries `laspistol`) and hard aliases (`shoots`→`ork shoota`).

**Foundry V14 embedded-item gotchas** (learned the hard way; cost ~half a day):
- Embedded weapon/armour items need ALL schema-required template fields populated (`physicalItem`: `inBackpack`; `attack`: `attackType`, `attackBonus`; `action`: `target`, `effect`; `backpack: {inBackpack}`). Missing fields cause silent doc rejection during compendium load — no error log, items just disappear. Standalone Item compendiums get a relaxation pass; embedded items don't.
- `range` and `penetration` are declared as strings (`""`) in `template.json` and must be strings on embedded items even though they hold numeric values.
- `_id` must be 16-char mixed-case alphanumeric (`[A-Za-z0-9]{16}`), Foundry-style randomID. Lowercase hex works for the .db row itself but is risky.
- And THE big one: deploying a new `.db` file doesn't update Foundry until you `rm -rf packs/<name>/` LevelDB cache. See "Deploy loop" / `reference_foundry_v14_leveldb_cache.md`.

**Image generation (current path — ComfyUI img2img against canonical refs):**

`flux/dev` via fal-ai was the first pass and was *not 40K-accurate enough* — SDXL/flux bases have weak priors for niche xenos (Kroot, Ebon Geist) and drift into generic fantasy. As of 2026-05-18 the pipeline runs locally on the `whisperx` LXC:

| Property | Value |
|---|---|
| ComfyUI API | `http://192.168.11.22:8188` (firewalled to `192.168.11.0/24`) |
| Base model | **JuggernautXL_v9** — photorealistic painted SDXL. *Avoid Illustrious-XL for these portraits — its anime bias fights the oil-paint aesthetic.* |
| Style LoRA | **`Oldhammer.safetensors` @ 0.45–0.60** — stacked on every portrait. The vintage GW oil-paint style anchor. ⭐ |
| Faction LoRAs | `Aeldari` (Eldar), `Ork-Boyz` (Ork), `Tau` (Tau-allied — avoid for pure Kroot), `Astartes` (Space Marines). All from nDimensional/HuggingFace. |
| Reference art | RT corebook NPC illustrations live at `/mnt/project_data/RT/RT-DOCS/CoreBook-201-401.pdf/pages/page-NNN/img-NN.jpeg`. Upload to whisperx's `~/comfyui/ComfyUI/input/rt_npc_refs/` first. |
| Workflow | **img2img**: `LoadImage → ImageScale 832×1216 → VAEEncode → KSampler(denoise=0.50–0.65, dpmpp_2m, karras, cfg=6, 30 steps) → VAEDecode → SaveImage`. The reference's painted body + species anatomy anchors the result; the prompt + LoRAs repaint the detail. |
| Denoise rule of thumb | `0.50` preserves canon anatomy almost verbatim (use when species drift is the failure); `0.65` lets the prompt dominate (use when reference composition is fine but you want a fresh repaint) |
| Script | `tools/npc_pipeline/comfy_npc_img2img.py` (callable as `python3 comfy_npc_img2img.py [eldar|ork|kroot|geist]`) |

**Strict-check discipline (REQUIRED for canon-anchored generation):**

Every generated portrait must be visually compared against the canonical published reference before being committed. Do not ship the first plausible result — for niche species the base model has no real prior and will drift to nearest-neighbor (Kroot → orc-faced humanoid, Aztec tribal warrior, Saurian Space Marine, literal cartoon crow — all observed during the 2026-05-18 pass). Name each drift mode explicitly and counter with a targeted negative prompt before re-rolling. Codified in `~/.claude/projects/-home-ahermon/memory/feedback_canon_strict_check.md`.

Typical negative-prompt counters seen during the Kroot iteration:
- Wrong species → `human face, orc, ork, goblin, snarl, tusks, troll, ogre`
- Wrong faction → `yellow power armor, imperial fists, space marine, ceramite shoulder pad`
- Wrong anatomy → `(extra arms:1.5), three arms, four arms, multiple weapons, feathered body, full bird, mascot, cartoon`
- Wrong style → `anime style, cell shaded, chibi, photograph, modern, contemporary`
- Costume drift → `skull mask, bone mask, tribal mask, headdress, native american, aztec`

**Style consistency across a batch:** the Oldhammer LoRA does the heavy lifting. Fixed seed (42 default) gives composition reproducibility but is less load-bearing than the style LoRA.

**flux/dev historical pass:** the four initial portraits were generated with flux/dev (style-front prompts + NotebookLM canonical descriptions + fixed seed 42 + square_hd). Result was 3/4 stylistically OK, 1/4 (Kroot) off-canon. Documented for posterity in case we revisit fal-ai pipelines.

**Known stubs:** creature-unique talents/traits not in the compendium (Mob Rule, Consume Life, Daemonic Presence, Hard Target, the `Common Lore (Ork)`/`Speak Language (X)` meta-talents) embed as name-only items — they render with their name but no benefit text until authored.

## Compendium canon (as of 2026-05-14, version 0.4.0)

All 14 packs are sourced exclusively from:

1. FFG **Rogue Trader Core Rulebook** (2009)
2. **Into the Storm** supplement
3. **Rogue Trader Living Errata v1.4**

Errata corrections are baked into the data; affected entries note the change in their description/source field. Items from other supplements (Hostile Acquisitions, Faith and Coin, Edge of the Abyss, etc.) and any DH-2e-only items have been removed.

Data extraction now prefers local RT-DOCS markdown (see "Content extraction pipeline" below). NotebookLM (notebook `cd87d917-4cea-45b3-b27c-a149070b1826`) is the fallback for synthesis or image OCR.

Extraction scripts live in `/tmp/rt_ship/` during a working session (not committed). To regenerate a pack, re-run the corresponding script and rebuild.

## Not yet done

### Other packs still mixed-source
The following packs were **not** touched in the talents/weapons/armour rebuild and still contain a mix of DH-2e, other supplements, and possibly stale entries:

- `ammo` — special ammunition variants
- `aptitudes` — DH-2e-only concept; whole pack may be removable if going pure RT 1e
- `cybernetics` — RT calls these "Bionic Replacements"
- `psychic-powers` — RT has its own psychic discipline structure (Telepathy/Telekinesis/Divination/Biomancy/Pyromancy) plus Navigator powers and Astropathic Choirs
- `tools` — general gear; needs cross-referencing with RT Chapter V "Gear"
- `traits` — creature/character traits; should align with RT corebook traits chapter
- `weapon-mods` — RT weapon upgrades on pp. 133–135 of corebook + Into the Storm Ch.III
- `consumables` — drugs, stimms, medical supplies, food, etc.
- `attack-specials` — special weapon qualities (Tearing, Flame, etc.); the canonical list lives in RT corebook pp.142–145
- `tables` — lookup tables; depends on the system code's usage

### Known gaps in the rebuilt packs

**Talents:**
- Page numbers all collapse to `Rogue Trader Core Rulebook` without page (NotebookLM didn't extract per-talent pages reliably). Could be filled in with a targeted re-query.
- Three Into-the-Storm Traits were excluded (Lostok Augmentation, Instinctual Understanding, Shamanic Powers, Sanctioned Xenos) — they belong in `traits`, not `talents`.
- A handful of corebook benefit texts were filled in from general knowledge where the OCR was truncated: Psy Rating, Rite of Fear, Frenzy (partial), Catfall (tail), Luminen Shock (middle), Unarmed Warrior (middle), Swift Attack (tail). Verify against the printed book.

**Weapons:**
- `special` flags now support parameter levels — e.g. `special.blast: 3` carries the Blast radius. Backfilled for the 33 Blast weapons from their descriptions. Other parameterized qualities (Snare/Concussive/Toxic/etc.) still need a targeted re-extraction pass; their flags are currently `true` with no level.
- Special-quality keyword matcher in `/tmp/build_weapons.py` was fuzzy (substring match) — 0.7.8 fixed the Inaccurate/Unreliable/Unbalanced inversion bug across 23 weapons, but rare qualities like Spray, Snare, Sanctified, Force, Volatile may still not be recognized and silently dropped from the `special` object. Re-check against descriptions if writing automation.
- "Bomb Squig" / "'Sploding Squig" stats are weird-by-design; sanity-check.
- ItS page numbers for some entries (Bolter Cane p.100, Wrath Plasma Pistol p.101) may be off — ItS Extended Armoury runs roughly pp.108–146.

**Armour:**
- Force fields encode Protection Rating in a `protectionRating` field (added during the 0.7.x series) plus in `description`. Field overload chances reference "Table 3-10" in description but the table itself isn't in the pack.

**Cybernetics:**
- 16 cybernetics have mechanical bonuses described in text but no Active Effects or system wiring (Bionic Arm +10 Ag/Str, Bionic Locomotion Sprint talent, Calculus Logi +10 Literacy/Logic/Scholastic Lore, Synthetic Muscle Grafts +1 SB, etc.). The three armour-granting cybernetics (Bionic Heart, Cranial Armour, Subskin Armour) were wired in 0.7.8; the rest remain description-only.
- "Implant Systems" entry is a section header from the corebook, not an acquirable item. Harmless but could confuse users.

### Errata not yet applied
The errata document was queried for talent and armor changes. **Weapon errata was queried inline during stat extraction**, but the comprehensive errata sweep should also be applied to:
- Ammo (e.g. Backpack Ammo Pack/Power Pack clarification on p.135 of errata)
- Career advance tables (errata adds/removes talents from career schedules — not relevant to compendium content but matters for character creation rules)
- Starship components (Excess Void Armour, Overload Shield Generators, Null Bay — if/when ship-component packs are built)

### Validation not performed
- The generated YAML has not been built into `.db` form via `gulp packs` — verify it round-trips without errors.
- The system has not been loaded in Foundry to confirm sheets render correctly with the new data.
- No diffing was done against the prior compendium to verify which items players in active games might lose access to (i.e. characters referencing removed items).

## Source-of-truth conventions

- For RT corebook entries: `source: "Rogue Trader Core Rulebook, p.XX"` (or no page if not extracted).
- For Into the Storm entries: `source: "Into the Storm, p.XX"`.
- Page numbers come from NotebookLM; some are approximate where the OCR was poor.

## Content extraction pipeline

Two sources for RT content; prefer the local one:

1. **`/mnt/project_data/RT/RT-DOCS/`** (local, fast — moved out of repo 2026-05-14) — pre-extracted markdown of CoreBook (split as `CoreBook-1-200.pdf/markdown.md` + `CoreBook-201-401.pdf/markdown.md`), Into the Storm, and Errata v1.4. Each PDF also has a `pages/page-N/` directory for per-page lookups. `grep` and direct `Read` work here, no API latency. Use this first for any new content pass.
2. **NotebookLM** (notebook `cd87d917-4cea-45b3-b27c-a149070b1826`) — only when you need synthesis across sources or the markdown OCR is too noisy for a specific extract. Auth via `nlm login --manual --file /home/ahermon/cookies.txt`; if `cookies.json` ends up corrupt, see `notebooklm_mcp_setup.md` memory for the flat-dict-rewrite fix.

### RT-DOCS quirks (learned the hard way)

- **CoreBook-1-200.pdf has descriptions only.** Tabular stat blocks were dropped by the PDF→markdown converter on pp.1–200. Hulls came through fine because their stats are in prose form, not tables.
- **CoreBook-201-401.pdf has the actual stat tables** as proper markdown tables (`| col | col |`). TABLE 8-3 (essential components), supplemental, weapons, archeotech, xenotech — all parseable. Same goes for ItS part 2.
- **Component stats live in part 2, descriptions in part 1** for early Ch.VIII components (drives, engines, shields, bridges). Any parser must search across both files.
- **Some section headers lose their `#` prefix** in conversion (e.g. macrobattery entries on p.202 appear as plain text). Plain-line fallback (name on its own line with blank lines around) works.
- **British/American spelling drift** appears between table cells and descriptive headers (Gellar/Geller, compartmentalised/compartmentalized). Build a spelling-variant matcher when name matching fails.

---

## 2026-05-13 — System audit findings ("what needs correction for a workable RT")

A read-only audit of the code + schema + packs against the DH2→RT mechanical-delta reference (`DH2_to_RT_Mechanical_Changes.md`). The repo's *content* in talents/weapons/armour was rebuilt for RT, but the *engine* is largely an unmodified DH2 fork. Below are concrete drift points to fix.

### A. Combat-action modifiers — DONE 2026-05-13 (commit c67c9ca) + 2026-05-14

All combat-action modifiers and types match RT 1e corebook Table 9-1 (pp.147-148):

- All Out Attack: +20 WS ✓
- Charge: +10 WS ✓
- Full Auto Burst: +20 BS, type Full ✓
- Semi-Auto Burst: +10 BS, type Full (fixed 2026-05-14 — the audit missed the type)
- Stun: -20 WS, type Full (fixed 2026-05-14 — the audit missed the type)
- Delay: type Half ✓
- Guarded Action: type Full ✓ (note: RT calls this "Guarded Attack" — name kept as "Guarded Action" for now to avoid breaking saved character data)
- Tactical Advance: removed (DH2-only) ✓
- Evasion split into Dodge (Ag) and Parry (WS) Reactions ✓

### B. Characteristic + skill schema (`src/template.json`)

- **`characteristics.influence`** (lines 99–107) is the DH2 10th characteristic. RT has 9 characteristics and uses a **group-wide Profit Factor** instead. Action: remove `influence` from `base.characteristics`, add a `system.profitFactor` field at the warband/group level (a Party actor, a world setting, or an `acolyte` field that the GM treats as shared).
- **Skill list is hybrid DH2/RT**: it keeps DH2's consolidated `athletics`, `stealth`, but uses RT-style sub-specialties on `operate` (Surface/Walkers/Aeronautica/Voidship) and `navigate` (Surface/Stellar/Warp). For pure RT 1e, the canonical skill list is the DH1/RT split (Pilot, Drive, Search separate from Awareness, Concealment + Silent Move + Shadowing instead of Stealth, etc.). Current state is workable but unfaithful to RT; document the deviation or rewire.
- **`parry`** is defined as a skill (line 483). In RT 1e, Parry is a Reaction (WS test, no separate skill ranks). Either drop the skill or document it as a deliberate DH2-style extension.
- **`acolyte.aptitudes`** (line 808) — DH2-only concept. RT uses Career advance tables. The whole aptitude system is dead weight here.
- **`acolyte.bio`** has `homeWorld`/`background`/`role`/`elite`/`divination` — DH2's 3-axis creation. RT uses Origin Path (Home World → Birthright → Lure of the Void → Trials and Travails → Motivation → Career). No Origin Path fields exist.
- **Specialties drift**: skill specialty lists like `commonLore` (Faith/Fleet/Imperium/Koronus Expanse/Military/Tactics/Tech/Underworld) are partially RT-flavored already — good direction, but cross-check against the corebook's exact specialty list.

### C. Aptitudes system is baked into character data path

- `src/module/documents/acolyte.mjs:57` exposes `get aptitudes()`.
- `src/module/rules/homeworlds.mjs`, `roles.mjs`, `backgrounds.mjs` all carry DH2 aptitude pairs (Offence/Finesse/Defence/Psyker/Tech/Knowledge/Leadership/Fieldcraft/Social) for every entry.
- `src/packs/aptitudes/aptitudes.yml` contains 19 aptitude entries.
- For RT this entire pipeline should either be removed or replaced with Career-rank advance tables (Arch-militant, Astropath Transcendent, Explorator, Missionary, Navigator, Rogue Trader, Seneschal, Void-master).

### D. Character creation rules (DH2 lists, not RT)

- `src/module/rules/homeworlds.mjs` — entries are DH2 (Feral, Forge, Highborn, Hive, Shrine, Voidborn). RT 1e uses: Death World, Forge World, Frontier World, Highborn, Hive World, Imperial World, Schola Progenium, Shrine World, Void Born. No RT homeworlds are present.
- `src/module/rules/backgrounds.mjs` — DH2 Backgrounds (Adeptus Administratum, Arbites, Astra Telepathica, Mechanicus, Ministorum, Imperial Guard, Outcast). RT has no "Background" concept; closest equivalent is the Birthright/Lure/Trials/Motivation chain.
- `src/module/rules/roles.mjs` — DH2 Roles (Assassin, Chirurgeon, Desperado, Hierophant, Mystic, Sage, Seeker, Warrior). RT replaces these with Careers (see above).
- `src/module/rules/divinations.mjs` — DH2 Emperor's Tarot divinations. RT does not use this mechanic.
- `src/module/rules/elite-advances.mjs` — DH2 elite advances. RT uses a different advance system.

### E. Compendium pack state (audit of all 13 packs)

| Pack | Items | Sourcing | State |
|---|---|---|---|
| `talents` | 195 | 170 RT Core + 25 ItS | **Clean** (per CLAUDE.md) |
| `weapons` | 202 | 104 RT Core + 98 ItS | **Clean** |
| `armour` | 42 | 22 RT Core + 20 ItS | **Clean** |
| `psychic-powers` | 79 | RT Core (39) + ItS (40) | **Clean as of 2026-05-13.** Rebuilt from RT corebook Ch.VI + Into the Storm + Errata v1.4. See "Psychic-powers rebuild" section below for breakdown and caveats. |
| `ship-components` | 73 | RT Core (48) + ItS (25) | **Clean as of 2026-05-14.** Essential, supplemental, archeotech, xenotech. |
| `ship-weapons` | 18 | RT Core (10) + ItS (8) | **Clean as of 2026-05-14.** Macrobatteries, lances, archeotech weaponry. |
| `ship-traits` | 34 | RT Core (28) + ItS (6) | **Clean as of 2026-05-14.** 14 hull patterns + 10 Machine Spirit Oddities + 10 Past Histories. Hulls embed full stat block in description (no `shipHull` item type). |
| `attack-specials` | 26 | RT Core (25) + ItS (1) | **Clean as of 2026-05-14.** All 25 corebook weapon qualities + ItS `Force`. |
| `weapon-mods` | 36 | RT Core (33) + ItS (3) | **Refreshed 2026-05-14.** Corebook weapon upgrades + ItS Ch.III additions (Calamity Vents, Exterminator Cartridge, Tox Dispenser). Some entries are craftsmanship sub-rules nested under parents — worth a manual review pass. |
| `ammo` | 12 | RT Core (3) + ItS (9) | **Refreshed 2026-05-14.** RT specialty rounds + ItS Ch.III Unusual Ammunition (Acid Shells, Airtorch Canister, Microburst Flask, Nephium Fuel Tank, Organgrinder Rounds, Snare Shells, Toxic Shot, Tracer Shells, Void Rounds). |
| `tools` | 77 | RT Core (47) + ItS (30) | **Refreshed 2026-05-14.** Clothing, void suits, gear, tools, equipment from corebook Ch.V + ItS Ch.III Gear/Tools (Aquila Magnificus, Targeting Monocle, Glidewing, Stasis Pod, Promethium, Psycrystal, etc.). |
| `consumables` | 29 | RT Core (21) + ItS (8) | **Refreshed 2026-05-14.** Drugs, stimms, recaf, sacred unguents + ItS additions (Blush, Ploin Juice, Raenka, Attention Spanner, Cold Fire, Spur, White Void, Wideawake). |
| `cybernetics` | 28 | RT Core + ItS | **Audited 2026-05-24.** Bionic Replacement Limbs + Implant Systems + Mechadendrites from corebook p.131+ and 6 ItS entries. All availability values corrected. Bionic Heart, Cranial Armour, Subskin Armour wired with `hasArmourPoints`. 3 missing corebook items added (Voidskin, Volitor Implant, Vox Implant). |
| `traits` | 33 | RT Core | **Refreshed 2026-05-14.** Creature/character traits from corebook Ch.XIV — Auto-Stabilised, Bestial, Daemonic, From Beyond, Phase, Warp Instability, Mechanicus Implants, etc. Replaces the DH2 fork content. |
| `aptitudes` | — | DROPPED | **Removed 2026-05-14.** DH2-only concept; no RT 1e equivalent. Pack unregistered from system.json. |
| `aptitudes` | 19 | DH2 | **Drop entirely** if going pure RT 1e (or leave as documentation-only). |
| `traits` | 41 | No `source:` field — unmodified DH2 fork | Most traits port to RT, but no RT-specific entries (Auto-Stabilised gun mount, Brute, Daemonic, From Beyond, Phase, Quadruped, Sanctioned, Stuff of Nightmares, Touched by the Fates — all present but unverified). Spot-check against RT corebook traits chapter. |
| `tools` | ~50 | No `source:` field | Mix of DH2 tools (Combi-tool, Auspex, Multi Compass, Glow-globe). Most overlap with RT gear (Ch.V) but not verified. |
| `weapon-mods` | ~35 | No `source:` | DH2 fork; RT weapon upgrades on pp.133–135 + ItS Ch.III need cross-check. |
| `consumables` | 13 | No `source:` | DH2 fork (Frenzon, Obscura, Recaf, Slaught, Spook, Stimm, Tranq) — most exist in RT but profiles may differ. |
| `cybernetics` | ~27 | No `source:` | DH2 fork. RT calls these **Bionic Replacements** and the corebook list is much shorter. Needs full rebuild from RT corebook. |
| `ammo` | ~30 | No `source:` | DH2-flavored. RT corebook has its own specialty ammo list. |
| `attack-specials` | ~35 | No `source:` | DH2-style weapon qualities. Most qualities are shared (Accurate, Balanced, Blast, Flame, Power Field, Tearing — though I don't see Tearing in the list) but `Force`, `Sanctified` profiles and parameter handling differ. RT-specific qualities like **Storm**, **Spray**, **Snare**, **Tangle**, **Volley**, **Boarding Action**, **Tearing** need verification. |
| `tables` | 3 (Psychic Phenomena, Perils of the Warp, Fear) | DH2 RollTables | Sparse. RT has many GM tables (Stars of Inequity systems, Endeavours, etc.) — none present. |

### F. Voidship subsystem — code exists, content missing

- Code: `src/module/documents/voidship.mjs` (161 lines), `src/module/sheets/actor/voidship-sheet.mjs` (88 lines), `src/module/rules/voidship-critical-damage.mjs` (346 lines), `src/module/rules/voidship-hit-locations.mjs`, `src/module/rules/ship-facings.mjs`, plus voidship panels in `src/templates/actor/panel/voidship-*.hbs`. Schema in `template.json` covers hull/armour/shields/crew/morale/troops/boarding/operator/weaponSlots.
- **No compendium content**: zero ship components, zero ship weapons, zero ship traits, zero hull profiles in any pack. Users must build every Plasma Drive, Warp Engine, Macrocannon, Lance, void shield, hull pattern by hand. The RT corebook Ch.VII (pp.182–229) defines ~80 components and ~30 weapons — needs a full content pass.
- Strategic Round / VU map / boarding action flow: prompts exist (`crew-prompt.mjs`, `ship-weapon-prompt.mjs`) but there's no explicit Strategic Round turn structure or VU positional tracking visible.

### G. Economy / acquisition — DONE 2026-05-14

RT's group-wide Profit Factor + Acquisition Test (corebook p.270) is now implemented:

- **`profitFactor` world setting** (default 30) registered via `RogueTraderSettings`. GM-editable in the Configure Settings dialog. Use `RogueTraderSettings.getProfitFactor()` / `setProfitFactor()` to read or write programmatically.
- **`game.rt.acquisition()`** opens an Acquisition Test dialog with dropdowns for Availability / Craftsmanship / Scale (with per-tier modifiers baked in from `src/module/rules/acquisition.mjs`), an optional Commerce DoS bonus, and an extra modifier field. Rolls 1d100 vs `PF + mods` and posts a chat card with DoS/DoF.
- Modifier tables: AVAILABILITY_MODS (+30 Ubiquitous … −70 Unique), CRAFTSMANSHIP_MODS (Poor +10 … Best −30), SCALE_MODS (Trivial +30 … Vast −30) — verbatim from RT corebook p.270.

DH2 Influence + Requisition remains unimplemented (correct — RT uses PF instead).

**Sheet integration:** any item with an `availability` or `craftsmanship` field (weapons, armour, ammo, tools, etc.) now shows an "Acquire" button in its sheet header. Clicking it opens the Acquisition Test dialog with the item's name, availability, and craftsmanship pre-filled.

### H. Psychic mechanic — DONE 2026-05-14

`PsychicRollData` (src/module/rolls/roll-data.mjs:374) and `checkForPerils` (src/module/rolls/action-data.mjs:25) now implement RT 1e Fettered/Unfettered/Push behaviour:

- **`psy.strength` enum** on the actor sheet (`fettered` / `unfettered` / `push`) sets the default PR for the roll prompt — Fettered = ⌈rating/2⌉, Unfettered = rating, Push = rating + pushCap.
- **`psy.class` is a select** on the actor sheet (`sanctioned` / `unsanctioned` / `bound` / `unbound`). DH2 bound/unbound are kept as legacy aliases so existing actors don't break.
- **Push cap** (`PsychicRollData.pushCap` getter) — Sanctioned +3, Unsanctioned +4 per RT corebook.
- **Phenomena trigger:** Fettered (`pr < rating`) skips Phenomena entirely; Unfettered (`pr === rating`) triggers on doubles; Push (`pr > rating`) triggers on any non-doubles.
- The existing `+10 per step below max PR` bonus and `-10 per step above max PR` penalty are preserved (they were already correct).

Future work: auto-roll the Psychic Phenomena and Perils of the Warp tables (in `src/packs/tables/`) when phenomena trigger, instead of just flagging in chat.

### I. Other observations

- **Subtlety**: not present anywhere. Correct — it's DH2-only.
- **Reinforcement Characters**: not present. Correct.
- **NPC Threat Threshold**: `npc.threatLevel` field exists in template.json, but no encounter builder. Drop or leave as a flavor stat.
- **Endeavours / Exploration Challenges / Renown**: nothing in the codebase. These are RT systems with no current support.
- **Fate vs. Fate Threshold**: schema uses `fate.max` + `fate.value`, which works for both editions.
- **Aim** values (+10 Half / +20 Full) match RT.
- **Standard Attack** +10 matches RT.
- **Called Shot** −20 matches RT.

### J. Stale/branding things

- `system.json` describes the system as "Rogue Trader 3rd Edition system for Foundry VTT" — accurate, given the iteration-of-this-system naming convention.
- Many sheet class names still say `DarkHeresy*` (e.g. `DarkHeresyPsychicPowerSheet`, `DarkHeresyItemContainerSheet`) — purely cosmetic but reflects fork origin.
- Three uncommitted modifications: `src/packs/armour/armour.yml`, `src/packs/talents/talents.yml`, `src/packs/weapons/weapons.yml` — the canon-rebuilt content not yet committed.

### Psychic-powers rebuild (2026-05-13)

79 powers across 7 discipline tags:

| Discipline | Count | Source |
|---|---|---|
| Telepathy | 22 | Core 18 + ItS 4 |
| Telekinesis | 12 | Core 8 + ItS 4 |
| Divination | 12 | Core 9 + ItS 3 |
| Navigator | 18 | Core 9 + ItS 9 |
| Theosophamy | 7 | ItS only (sanctioned-psyker faith discipline) |
| Astropath Starship Action | 5 | ItS — ship combat actions, not strictly psychic powers |
| Navigator Starship Action | 3 | ItS — ditto |

**Important corrections to the earlier audit notes:**
- **RT corebook has only 3 player psyker disciplines** (Telepathy, Divination, Telekinesis), NOT 5. Biomancy and Pyromancy are DH2/supplement-only and were correctly absent from RT 1e — the prior CLAUDE.md/RT_CORRECTION_CHECKLIST.md text saying "5 core disciplines" was wrong.
- **Astropathic Choirs is a relay-assistance rule** (corebook p.162; Errata v1.4 raised max bonus from +5 to +10), not a purchasable power — not extracted as a compendium entry. The only Astropath-exclusive power is **Astral Telepathy** which is in the Telepathy discipline.

**Schema caveats (updated 0.5.0):**
- 17 attack powers now carry RT-canon Roll formulas in `damage` (e.g. `1d10+@pr`, `1d10+3*@pr`). The remaining 62 powers don't deal direct damage and keep `damage: 0`. WP-scaling powers (Lidless Stare, Scourge of the Red Tide) carry a base `1d10` and explain the WB/DoS scaling in description text.
- Navigator XP costs are `0` placeholders — RT Navigator powers are tied to Lineage talents/career advances, not flat XP. Worth a targeted re-query.
- `penetration: 99` is a sentinel for "Ignores Armour" (Psychic Scream).
- Errata corrections from v1.4 are baked into the descriptions (with `Errata:` prefix when an entry was modified).

## Documented deviations from RT 1e (preserved into 0.5.0)

These are deliberate carry-overs from the DH2 fork that are NOT being changed in 0.5.0. They're either tightly coupled to the A/B character-creation decision or low-priority cosmetic. Listed so future passes don't "rediscover" them as bugs.

1. **Parry is a skill, not just a Reaction.** `src/template.json` defines `parry` as a WS-based skill. RT 1e treats Parry as a Reaction (a WS test, no separate skill ranks). Keeping the skill lets characters spend XP advances on +10/+20 Parry — useful as a house-rule extension, but unfaithful to RT. As of 0.7.9, the Parry roll reads the equipped melee weapon for Balanced/Defensive/Unwieldy/Unbalanced modifiers, craftsmanship bonuses, and weapon-specific `parryBonus`. As of 0.7.16, Parry is classified as a Basic skill (can attempt untrained at half WS).
2. ~~**`athletics` and `stealth` are consolidated skills.**~~ **RESOLVED in 0.7.17.** DH2 consolidations split back to RT 1e canon: Athletics→Climb+Swim, Stealth→Concealment+Silent Move+Shadowing, Linguistics→Speak Language+Secret Tongue+Ciphers+Literacy.
3. **DarkHeresy* sheet class names** (`DarkHeresyPsychicPowerSheet`, `DarkHeresyItemContainerSheet`, etc.). Purely cosmetic; renaming would touch many template files for zero functional gain. Defer.
4. **`npc.threatLevel` field exists but no encounter-budget tool consumes it.** DH2-only concept; RT relies on GM judgment. Field is harmless flavor data; will be removed if the A/B pass cleans `template.json`.
5. **Skill specialty lists drift from RT canon.** Audit (0.5.0): Common Lore 8 vs 14, Forbidden Lore 6 vs 11, Scholastic Lore 9 vs 16, Linguistics is a DH2 consolidation (RT splits into Speak Language + Literacy + Secret Tongue), Trade 10 vs 13. Changing these is a breaking schema migration (specialty keys are referenced by existing actors); deferred to the A/B pass so it can be done as one coordinated migration.
6. **Voidship critical-damage subsystem is homebrew.** `voidship-critical-damage.mjs` uses a Nonpenetrating/Penetrating/Critical × Component × 1d10 matrix that doesn't match RT 1e Table 8-12. The canonical RT table is now in the `tables` pack as two RollTables (`Critical Hits to Starships`, `Catastrophic Damage`) for manual GM use. The engine still uses the homebrew tables; replacing them would require rewriting `assign-damage-data.mjs` callers and is out of scope for 0.5.0.
7. **`getCriticalDamage` and voidship hit-location data** (Main/Prow/Bridge/Rear) describe which arc was hit *from* but are not the RT 1e Critical Hit table targets. See #6 above; documented for clarity.

### Bottom line (as of 0.5.0)

The repo is now a **DH2-derived engine with RT 1e mechanics + RT-canon content across all 14 compendia + canonical critical-hit RollTable + corrected damage rolling**. Three layers, two done:

1. **Combat math fixes** ✓ 2026-05-14. All 6 audit fixes plus 2 the audit missed (Semi-Auto Burst type, Stun type). Evasion split into Dodge/Parry. 0.5.0 added the Accurate fix (1d10 per 2 DoS, not flat).
2. **Content rebuild** ✓ 2026-05-14. ~890 entries across 15 packs. 0.5.0 added the canonical RT Critical Hits and Catastrophic Damage tables; the rest of the `tables` pack remains sparse (Stars of Inequity / Edge of the Abyss not local — would need re-extraction).
3. **Character creation Path A vs B** — **OPEN, next major decision**. The DH2 creation pipeline (Home World × Background × Role × Aptitudes) still ships in `src/module/rules/{homeworlds,backgrounds,roles,divinations}.mjs` and `acolyte.aptitudes` is still in `template.json`. Decide whether to (A) replace the data in those modules with RT equivalents but keep the 3-axis UI, or (B) rip the lot out and build an Origin Path + Career advance-table model with a creation wizard. The skill-specialty drift (deviation #5 above) should be addressed in the same pass.

See `RT_CORRECTION_CHECKLIST.md` for the punch list with completed items checked off.
